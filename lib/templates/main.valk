
use valk:gc

fn __valk_main__() i32 $entrance {
    gc:GcShared.init()
    let gc = gc:GcLocal.init()
    
    @allocas

    #loop classes T
    @pool(T) = gc:Pool.init[T]()
    #end

    main()

    gc.shutdown()
    return 0
}
