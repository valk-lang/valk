
use valk:gc

fn __valk_main__() i32 $entrance {
    gc:GcShared.init()
    let gc = gc:GcLocal.init()
    
    @allocas

    #loop classes T
    @vtable_nr(T) = ++gc:vtable_count
    #end

    main()

    gc.shutdown()
    return 0
}
