
use valk:mem

fn __valk_main__() i32 $entrance {
    mem:GcShared.init()
    let gc = mem:GcLocal.init()
    
    @allocas

    #loop classes T
    @vtable_nr(T) = ++mem:vtable_count
    #end

    main()

    gc.shutdown()
    return 0
}
