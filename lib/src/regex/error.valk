// `syntax`: the pattern is malformed; `unsupported`: it uses a feature this
// engine does not have (backreferences, lookaround, \p classes);
// `too_large`: the compiled program exceeds the size limit
error RegexError (syntax, unsupported, too_large) payload {
    message: String
    position: uint (0)
}
