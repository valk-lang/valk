use io

// `invalid_input`: the data is not a valid stream. `checksum`: the stream's
// checksum or length trailer does not match. `truncated`: the stream ends
// early. `too_large`: the output would exceed the caller's limit
error CompressError (invalid_input, checksum, truncated, too_large) extends(io.IoError)
