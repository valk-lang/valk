
// This is a fake class to store GC objects as a `ptr` but still have it tracked by the GC 
// E.g.

// class MyClass {
//     tracked: GcPtr
// }
// let ob = MyClass {
//     tracked: mybool ? (classA @as GcPtr) : (classB @as GcPtr)
// }

class GcPtr {
    data: uint (0)
}
