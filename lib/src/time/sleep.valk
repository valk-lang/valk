
use coro
use thread

+ fn sleep_ns(ns: uint) {
    let current = coro:current_coro
    if isset(current) : current.sleep(ns)
    thread:suspend_ns(ns)
}

+ fn sleep_ms(ms: uint) {
    sleep_ns(ms * 1000 * 1000)
}

