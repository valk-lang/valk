
@unsafe

#if OS == "macos" || OS == "bsd"
use ext
use mem

fn aio() *ext.aiocb {
    let a: *ext.aiocb = mem.calloc(size_of(ext.aiocb))
    return a
}

fn aio_free(cb: *ext.aiocb) {
    mem.free(cb)
}
#end
