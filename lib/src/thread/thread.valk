
class Entry[T] {
    #loop types FUNC_ARG_TYPES(T)
    #end
}

class Thread {

    static fn start(func: $TYPE[T: CALLABLE], $ARGS(...FUNC_ARG_TYPES(T))) Entry[T] {
        let c = class {
        } 
    }
}


fn test(str: String) {
    println(str)
}
Thread.start(test, "hello")
