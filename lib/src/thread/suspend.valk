
use ext

+ fn suspend_ns(ns: uint) {
	#if OS == "win"
	ext:Sleep(1)
	#else
    let nsec = ns.to(int)
    let ts : ext:libc_timespec = @undefined
    let rem : ext:libc_timespec = @undefined
    ts.tv_sec = nsec / 1_000_000_000
    ts.tv_nsec = (nsec % 1_000_000_000)
    ext:nanosleep(@ref(ts), @ref(rem))
	#end
}

+ fn suspend_ms(ms: uint) {
    suspend_ns(ms * 1000 * 1000)
}
