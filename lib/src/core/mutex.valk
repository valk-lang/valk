
use ext
use coro
use mem
use io

#if OS == "win"
// Plain data on purpose: the array stores these inline, so registering a
// waiter allocates nothing, and sharing a Mutex does not pull the waiting
// coroutines into shared memory.
struct MutexWait {
    coro: ptr
    iocp: ext:HANDLE
}
#end

+ class Mutex {
    #if OS == "linux" || OS == "macos"
    - pipe: [i32 x 2] ({ -1... })
    - buf: uint (0)
    #elif OS == "win"
    - locked: bool (false)
    - mutex: SyncMutex (.new())
    - waits: Array[MutexWait] (.new(128))
    #end

    + static fn new() SELF !InitError {
        let m = SELF {}
        m.init() !>
        return m
    }

    fn init() !InitError {
        #if OS == "linux" || OS == "macos"
        let res = ext:pipe(@ref(this.pipe))
        if res != 0 : throw .init
        io:write_string(this.pipe[1], "v") ! {
            io:close(this.pipe[0])
            io:close(this.pipe[1])
            this.pipe = { -1... }
            throw .init
        }
        #if OS == "macos"
        io:set_non_block(this.pipe[0], true)
        #end
        #end
    }

    + fn clone() SELF $clone {
        return this
    }

    #if OS == "win"
    fn wait_entry() MutexWait @shared_unsafe {
        let current = coro:current_coro
        if !isset(current) : panic("Waiting on a mutex outside a coroutine")
        current.iocp_ready = false
        return MutexWait {
            coro: current.@cast(ptr)
            iocp: io:iocp()
        }
    }
    #end

    + fn await_unlock() @shared_unsafe {
        #if OS == "linux" || OS == "macos"
        io:read_to_ptr(this.pipe[0], @ref(this.buf), 1, -1) ! panic("Mutex wait failed")
        io:write_string(this.pipe[1], "v") ! panic("Mutex wake failed")
        #elif OS == "win"
        this.mutex.lock()
        if !this.locked {
            this.mutex.unlock()
            return
        }

        this.waits.append(this.wait_entry())
        this.mutex.unlock()
        coro:yield()
        #end
    }

    + fn lock() @shared_unsafe {
        #if OS == "linux" || OS == "macos"
        io:read_to_ptr(this.pipe[0], @ref(this.buf), 1, -1) ! panic("Mutex lock failed")
        #elif OS == "win"
        while true {
            this.mutex.lock()
            if !this.locked {
                this.locked = true
                this.mutex.unlock()
                break
            }
            this.waits.append(this.wait_entry())
            this.mutex.unlock()
            coro:yield()
        }
        #end
    }

    + fn unlock() @shared_unsafe {
        #if OS == "linux" || OS == "macos"
        io:write_string(this.pipe[1], "v") ! panic("Mutex unlock failed")
        #elif OS == "win"
        this.mutex.lock()
        if !this.locked {
            this.mutex.unlock()
            return
        }
        each this.waits as w {
            if !io:wake_coro(w.iocp, w.coro) {
                this.mutex.unlock()
                panic("PostQueuedCompletionStatus failed")
            }
        }
        this.waits.clear()
        this.locked = false
        this.mutex.unlock()
        #end
    }

    fn gc_free() {
        #if OS == "linux" || OS == "macos"
        if this.pipe[0] >= 0 : io:close(this.pipe[0])
        if this.pipe[1] >= 0 : io:close(this.pipe[1])
        #end
    }
}
