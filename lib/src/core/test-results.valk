
class TestResults {
    entries: Array[TestEntry] (Array[TestEntry].new())

    fn print() {
        println("--------- TEST RESULTS ---------")

        println("--------------------------------")
    }
}
