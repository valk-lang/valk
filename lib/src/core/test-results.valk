
class TestResults {
    entries: Array[TestEntry] (Array[TestEntry].new())

    fn add_entry(entry: TestEntry) {
        this.entries.push(entry)
    }

    fn print() {
        println("--------- TEST RESULTS ---------")

        println("--------------------------------")
    }
}
