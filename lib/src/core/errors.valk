
// Stores the code when throwing an error
global error_code : u32 (@undefined)

// Common used error codes
error AnError (error) $global
error ExternError (extern) $global
error IterError (end) $global
error InitError (init) $global
error LookupError (missing, exists, range, empty) $global
error SyntaxError (syntax) $global
