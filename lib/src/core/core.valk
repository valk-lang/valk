
header "sys" as sys

rdo global error_code : u32
rdo global error_msg : String = ""
global cli_args : Array[String] = @undefined

pub fn exit(code: i32) $exit {
    sys:exit(code @as i32)
}

pub fn panic(msg: String) $exit {
    println(msg)
    exit(1)
}

