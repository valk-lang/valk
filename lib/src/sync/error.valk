// `closed`: the channel is closed (and, for a receive, drained).
// `timeout`: the wait ran out. `cancelled`: the cancellation token fired.
// `empty` / `full`: a try_recv / try_send that could not proceed
+ error SyncError (closed, timeout, cancelled, empty, full, init)
