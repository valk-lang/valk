
use valk:json

fn convert_to_markdown(doc: json:Value) String {
    let str = ""

    let classes = doc.get("classes").array()
    let funcs = doc.get("functions").array()
    let globals = doc.get("globals").array()

    return str
}