
struct Class {
    unit: Unit
    build: Build
    nsc: Nsc
    export_name: String
    display_name: String
    //
    size: uint (0)
    props: Map[Prop] (map[Prop]{})
    chunk: Chunk
}

struct Prop {
    name: String
    type: Type
}
