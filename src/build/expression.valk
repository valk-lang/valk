
enum EXP {
    func
}

class Exp {
    type: EXP
    str1: ?String
    exp1: ?Exp
    exp2: ?Exp
    sub_exps: ?Array[Exp]

    static fn parse(b: Build, src: Source) Array[Exp] {
        let res = Array[Exp]{}
        // Init parser
        let tok = Tokenizer.init(b, Pos.init(src))

        //


        //
        return res
    }
}
