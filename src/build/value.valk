use helper
use valk.fs


enum CALL_KIND : u8 {
    direct
    closure
    interface_dispatch
}

enum VAL : u8 {
    undefined
    int
    float
    bool
    string
    null
    func     // resolved function reference; value1 = optional bound receiver
    interface_wrap
    is_a
    member   // resolved receiver + builtin/meta member name
    decl     // local / argument variable (Decl)
    prop     // object property access; value1 = receiver; prop = Prop
    global   // global / shared variable (Global)
    property_meta // compile-time Prop value bound by #loop properties
    type     // compile-time type value (of_type); rett is String; props like .$is_inline
    op       // binary op (VALUE_OP in .op); left/right in value1/value2
    unary    // unary op (VALUE_OP in .op); operand in value1
    call     // callee in value1; args in values; CALL_KIND in op
    error_success
    cast     // value.@cast(Type); value1 = on, rett = target Type
    multi    // argument list only (values array); not a runtime value alone
    class_init
    // Fixed-array construction: { v0, v1, ... } — values = elements; rett = array type
    // bool_val: the last element repeats to fill the length (`v...`)
    array_init
    match
    error_value
    // Element access / @ptrv: value1 = base, value2 = index; rett = element type
    ptrv
    // Raw pointer byte offset: value1 = pointer, value2 = signed byte offset
    ptr_offset
    // @ref(lvalue): value1 = address-of target; rett = ptr
    ptr_of
    // Load/copy an inline value through ptr/ref; value1 = pointer, rett = inline type
    load
    // cond ? a : b  /  a ?? b  — value1=cond (or isset), value2=then, values=[else]
    this_or_that
    // isset(v) — value1=operand; rett=bool; may set .issets
    isset
    // Type-only wrap (nullable strip/add); value1=inner
    wrap
    // Read-only shared view; value1=ordinary class value, published to shared GC
    shared_view
    // Atomic read/modify/write primitives. atomic_op stores VALUE_OP in `.op`.
    atomic_op
    atomic_store
    atomic_load
    // Raw memory copy: values = [source ptr, destination ptr, byte length].
    memcpy
    error_handler
    // `co <call>` — value1 = the launched call; rett = TYPE.co wrapping its return/error
    co
    await
    // ---- Function-body / control-flow values (typically rett = void) ----
    assign   // lvalue op= rvalue; op = ASSIGN_OP
    if       // value1=cond; values=then body; value2=else block (optional VAL.block)
    while    // value1=cond; values=body
    block    // sequential values (nested `{ ... }` or then/else body)
    return   // value1=optional return value
    throw    // value1=thrown value
    break
    continue
    // $entrance-only marker: emit the function's collected stack allocations here.
    allocas
    init_allocators
    // Execute the function's runtime stack of deferred closures in LIFO order.
    run_defers
    // `lock` block: value1 = acquire prelude, value2 = release call, values = body
    lock_block
    ir_comment
    // Value scope `<{...}`: values = body statements; value1 = phi of return values
    vscope
    // Phi of multiple control-flow values (vscope returns, …); values = arms
    phi
}

class ValueMeta {
    name: String ("")
    string_val: String ("")
    allocator: ?Allocator (null)
    group: ?Group (null)
    class_ref: ?Class (null)
    of_type: ?Type (null)
    issets: ?Array[Decl] (null)
    not_issets: ?Array[Decl] (null)
    error_type: ?ErrorType (null)
    pre_value: ?Value (null)
    checked_read: ?Value (null)
    // Original graph before lowering into temporary storage.
    graph_source: ?Value (null)
    publishes_graph: bool (false)
    // An Array element read out by value: writes into it are lost
    copied_element: bool (false)
    // An element read through a read-only borrow: its slot may not be written
    readonly_element: bool (false)
    publication_holds: ?Array[?Decl] (null)
    publication_store: ?Value (null)
    init_prop: ?Prop (null)
    implicit_borrow: bool (false)
    reads_storage: bool (false)
    // A struct receiver borrowed out of a closure's captured copy
    captured_receiver: ?Decl (null)
}

class Value {
    type: VAL
    rett: Type
    // Source expression (for errors / spans)
    exp: ?Exp (null)
    // Children
    value1: ?Value (null)
    value2: ?Value (null)
    values: ?Array[Value] (null)
    op: u8 (0)
    // Typed views of `op`, one per VAL family
    get value_op: VALUE_OP $inline { return this.op.@cast(VALUE_OP) }
    get assign_op: ASSIGN_OP $inline { return this.op.@cast(ASSIGN_OP) }
    get call_kind: CALL_KIND $inline { return this.op.@cast(CALL_KIND) }
    get errh_kind: ERRH_KIND $inline { return this.op.@cast(ERRH_KIND) }
    // Literal payloads
    int_val: int (0)
    float_val: f64 (0)
    bool_val: bool (false)
    meta: ?ValueMeta (null)
    // VAL.decl — single decl reference
    decl: ?Decl (null)
    // VAL.prop — object field (`on.prop`); receiver is value1
    prop: ?Prop (null)
    // VAL.global — global / shared storage
    global: ?Global (null)
    // VAL.func — resolved function declaration
    func: ?Func (null)
    // Fallible call metadata (before error handler trims it)
    can_error: bool (false)
    never_returns: bool (false)

    fn require_result() !Error {
        if this.rett.is_void_type() && !this.never_returns && this.type != VAL.undefined {
            this.error("A void expression does not produce a value")
        }
    }

    fn get_graph_source() ?Value {
        let data = this.meta
        return isset(data) ? data.graph_source : null
    }
    fn set_graph_source(value: Value) { this.ensure_meta().graph_source = value }

    fn reads_storage() bool {
        let data = this.meta
        return isset(data) && data.reads_storage
    }
    fn set_reads_storage() {
        this.ensure_meta().reads_storage = true
    }

    fn ensure_meta() ValueMeta $inline {
        let data = this.meta
        if isset(data) : return data
        let created = ValueMeta {}
        this.meta = created
        return created
    }

    fn get_name() String $inline {
        let data = this.meta
        if isset(data) && data.name != "" : return data.name
        let t = this.type
        if t == VAL.func {
            let func = this.func
            if isset(func) : return func.name
        } else if t == VAL.decl {
            let decl = this.decl
            if isset(decl) : return decl.name ?? ""
        } else if t == VAL.prop || t == VAL.property_meta {
            let prop = this.prop
            if isset(prop) : return prop.name ?? ""
        } else if t == VAL.global {
            let global = this.global
            if isset(global) : return global.name
        }
        return ""
    }

    fn set_name(name: String) $inline { this.ensure_meta().name = name }
    fn get_init_prop() ?Prop $inline {
        let data = this.meta
        return isset(data) ? data.init_prop : null
    }
    fn set_init_prop(prop: Prop) $inline { this.ensure_meta().init_prop = prop }
    fn get_string() String $inline {
        let data = this.meta
        return isset(data) ? data.string_val : ""
    }
    fn set_string(value: String) $inline { this.ensure_meta().string_val = value }
    fn get_allocator() ?Allocator $inline {
        let data = this.meta
        return isset(data) ? data.allocator : null
    }
    fn set_allocator(value: ?Allocator) $inline { this.ensure_meta().allocator = value }
    fn get_group() ?Group $inline {
        let data = this.meta
        return isset(data) ? data.group : null
    }
    fn set_group(value: ?Group) $inline { this.ensure_meta().group = value }
    fn get_class_ref() ?Class $inline {
        let data = this.meta
        return isset(data) ? data.class_ref : null
    }
    fn set_class_ref(value: ?Class) $inline { this.ensure_meta().class_ref = value }
    fn get_of_type() ?Type $inline {
        let data = this.meta
        return isset(data) ? data.of_type : null
    }
    fn set_of_type(value: ?Type) $inline { this.ensure_meta().of_type = value }
    fn get_issets() ?Array[Decl] $inline {
        let data = this.meta
        return isset(data) ? data.issets : null
    }
    fn set_issets(value: ?Array[Decl]) $inline { this.ensure_meta().issets = value }
    fn get_not_issets() ?Array[Decl] $inline {
        let data = this.meta
        return isset(data) ? data.not_issets : null
    }
    fn set_not_issets(value: ?Array[Decl]) $inline { this.ensure_meta().not_issets = value }
    fn get_error_type() ?ErrorType $inline {
        let data = this.meta
        return isset(data) ? data.error_type : null
    }
    fn set_error_type(value: ?ErrorType) $inline { this.ensure_meta().error_type = value }
    fn get_pre_value() ?Value $inline {
        let data = this.meta
        return isset(data) ? data.pre_value : null
    }
    fn set_pre_value(value: ?Value) $inline { this.ensure_meta().pre_value = value }
    fn get_checked_read() ?Value $inline {
        let data = this.meta
        return isset(data) ? data.checked_read : null
    }
    fn set_checked_read(value: Value) $inline { this.ensure_meta().checked_read = value }

    fn is_implicit_borrow() bool $inline {
        if this.rett.has_borrowed_refs() : return true
        let data = this.meta
        if isset(data) && data.implicit_borrow : return true
        if this.type != VAL.decl : return false
        let decl = this.decl
        return isset(decl) && decl.implicit_borrow
    }

    fn mark_implicit_borrow() $inline { this.ensure_meta().implicit_borrow = true }

    // Report an error at this value's source span when available.
    fn error(msg: String) !Error $throw {
        let e = this.exp
        if isset(e) : e.error(msg)
        // Fallback without span: still report, never fail silently
        this.rett.build.error(msg)
    }

    fn bug(msg: String) !Error $throw {
        this.error("(Compiler bug) " + msg)
    }

    fn assignable() bool {
        let t = this.type
        if t == VAL.prop {
            let prop = this.prop
            return isset(prop) && !prop.is_slice_data
        }
        return t == VAL.decl || t == VAL.global || t == VAL.ptrv
    }

    fn unroll() Array[Value] {
        let vals = this.values
        if this.type == VAL.multi && isset(vals) && this.rett.is_tuple_type() : return vals
        // A phi of multi values unrolls through its first arm.
        if this.type == VAL.phi && isset(vals) {
            let first = vals.get(0) !? null
            if isset(first) && first.type == VAL.multi : return first.unroll()
        }
        let parts = this.multi_parts()
        if isset(parts) : return parts
        return Array[Value]{ this }
    }

    // One extract per subtype of a multi-valued producer.
    fn multi_parts() ?Array[Value] {
        if !this.rett.is_tuple_type() : return null
        let subs = this.rett.get_members() ?! return null
        let out = Array[Value]{}
        each subs as st, i {
            let part = Value {
                type: VAL.multi
                int_val: i.@cast(int)
                value1: this
                rett: st
                exp: this.exp
            }
            part.set_name("part")
            out.append(part)
        }
        return out
    }

    // Explicit success projection of a fallible value.
    fn success_value() Value {
        if !this.can_error : return this
        return Value {
            type: VAL.error_success
            value1: this
            rett: this.rett
            exp: this.exp
        }
    }

    // Class / type id on the left of `Type { fields }` init.
    fn as_class_for_init() ?Class {
        if this.type != VAL.type : return null
        let t = this.get_of_type()
        if isset(t) {
            let c = t.class
            if isset(c) : return c
            return t.get_non_nullable().class
        }
        return this.get_class_ref()
    }

    // Extract Type from a compile-time VAL.type value.
    fn as_type() ?Type {
        if this.type == VAL.type : return this.get_of_type()
        return null
    }

    // Extract a name from a compile-time string value.
    fn as_name() ?String {
        if this.type == VAL.string : return this.get_string()
        return null
    }

    // Constant string form for folding == / != (null if not foldable as string).
    fn fold_string() ?String {
        if this.type == VAL.string : return this.get_string()
        if this.type == VAL.bool : return this.bool_val ? "1" : "0"
        if this.type == VAL.int {
            let buf = ByteBuffer.new()
            buf.write_int_ascii(this.int_val)
            return buf.to_string()
        }
        return null
    }

    // Resolve the Func being called from this callee Value, if known.
    fn resolve_call_func() ?Func {
        if this.type != VAL.func : return null
        return this.func
    }
}

////////////////////////////
// Expression dispatch
////////////////////////////

fn value_from_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    if b.nest_depth >= MAX_NESTING {
        exp.error("Expression nesting too deep (limit " + MAX_NESTING + ")")
    }
    if b.stack_exhausted() : exp.error("Expression nesting too deep")
    b.nest_depth++
    let did_return = scope.did_return
    scope.did_return = false
    let value = value_from_exp_inner(exp, scope, ctx) ! {
        b.nest_depth--
        scope.did_return = did_return
        throw E
    }
    b.nest_depth--
    value.never_returns = value.never_returns || scope.did_return
    scope.did_return = did_return || value.never_returns
    let group = value.get_group()
    if isset(group) && !ctx.is_group_receiver {
        exp.error("'" + group.name + "' is a group of functions, not a value. Use one of its functions instead")
    }
    return value
}

fn value_from_exp_inner(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    if exp.type != EXP.value : exp.bug("Expected EXP.value")
    let kind = exp.value_kind
    let b = ctx.build

    if kind == VALUE_KIND.paren {
        let inner = exp.get(VALUE_EXP.left)
        if !isset(inner) : exp.bug("Missing paren inner value")
        // Parentheses preserve the suggested type
        return value_from_exp(inner, scope, ctx) !>
    }

    if kind == VALUE_KIND.number || kind == VALUE_KIND.hex || kind == VALUE_KIND.octal {
        let v = value_from_int_literal(exp, b) !>
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.float {
        let v = value_from_float_literal(exp, b) !>
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.bool {
        let v = vgen_bool(exp.bool1, type_bool(b))
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.null {
        let nt: ?Type = null
        let type_exp = exp.get(VALUE_EXP.left)
        if isset(type_exp) {
            nt = type_from_exp(type_exp, scope) !>
        } else {
            nt = ctx.suggested_type
        }
        if !isset(nt) {
            exp.error("Missing type hint. This is required to generate a 'null' value. You can also use 'null[Type]' to suggest a type (e.g. null[?String])")
        }
        let null_t = nt.is_nullable_type() ? nt : nt.nullable()
        let v = vgen_null(null_t)
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.undefined {
        let v = vgen_undefined(type_void(b))
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.noop {
        return vgen_block(Array[Value]{}, b, exp)
    }
    if kind == VALUE_KIND.memcpy {
        return value_from_memcpy_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.clone {
        return value_from_clone_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.hash {
        return value_from_hash_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.string {
        return value_from_string_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.char {
        let v = vgen_int(char_literal_byte(exp.str1), type_char(b))
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.id {
        let v = value_from_id_exp(exp, scope, ctx) !>
        v.exp = exp
        lsp_hover(exp, v) !>
        return v
    }

    // size_of(Type) → VAL.int with the type's byte size
    if kind == VALUE_KIND.size_of {
        let type_exp = exp.get(VALUE_EXP.left)
        if !isset(type_exp) : exp.bug("Missing size_of type operand")
        let t = type_from_exp(type_exp, scope) !>
        let bytes = t.byte_size() !>
        let v = vgen_int(bytes.to(int), type_int(b))
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.unary {
        let operand_exp = exp.get(VALUE_EXP.left)
        if !isset(operand_exp) : exp.bug("Missing unary operand")
        let op = exp.number.@cast(VALUE_OP)
        let operand = vgen_undefined(type_void(b), exp)
        if op == VALUE_OP.ref_of && operand_exp.type == EXP.value && operand_exp.value_kind == VALUE_KIND.offset {
            // `&sequence[i]` and `&sequence[a..b]`: an element of an array,
            // a slice, or `&[T]` is borrowed with the storage block as owner.
            let base_exp = operand_exp.get(VALUE_EXP.left) ?! operand_exp.bug("offset is missing base")
            let on = value_from_exp(base_exp, scope, ctx.without_suggested_type()) !>
            let element_borrow = value_borrow_sequence_element(on, operand_exp, scope, ctx, exp, exp.bool1) !>
            if isset(element_borrow) : return element_borrow
            operand = value_from_offset_on(on, operand_exp, scope, ctx) !>
        } else {
            operand = value_from_exp(operand_exp, scope, ctx.without_suggested_type()) !>
        }
        if op == VALUE_OP.ref_of {
            if !operand.assignable() {
                exp.error("The '&' operator can only be used on assignable storage")
            }
            act_check_assign(operand, exp, scope) !>
            value_check_shared_address(operand, exp) !>
            return value_borrow_of(operand, scope, ctx, exp, exp.bool1) !>
        }
        // Fold constants when possible
        operand = value_enum_representation(operand) !>
        if op == VALUE_OP.neg {
            if !operand.rett.is_integer_type() && !operand.rett.is_float_type() {
                exp.error("Expected an integer or float after unary '-', the value type we found was: " + operand.rett.compat_display_name())
            }
            if operand.type == VAL.int {
                // A negative literal is never unsigned: only the signed minimum survives negation
                if !operand.rett.is_signed_type() && exp_is_untyped_number(operand_exp) {
                    if operand.int_val != (-9223372036854775807 - 1) : exp.error("Integer literal is out of range")
                    let v = vgen_int(operand.int_val, type_int(b))
                    v.exp = exp
                    return v
                }
                operand.int_val = fold_integer_to_width(0 - operand.int_val, operand.rett)
                operand.exp = exp
                return operand
            }
            if operand.type == VAL.float {
                // Flip the sign bit: '0 - x' would turn -0.0 into +0.0
                operand.float_val = operand.float_val * -1.0
                operand.exp = exp
                return operand
            }
        }
        if op == VALUE_OP.not && operand.type == VAL.bool {
            let v = vgen_bool(!operand.bool_val, type_bool(b))
            v.exp = exp
            return v
        }
        if op == VALUE_OP.bit_not && operand.type == VAL.int {
            // Two's-complement bitwise not (host may not support `~` token)
            operand.int_val = fold_integer_to_width((0 - 1) ^ operand.int_val, operand.rett)
            operand.exp = exp
            return operand
        }
        if op == VALUE_OP.not {
            if !operand.rett.is_bool_type() {
                exp.error("Expected a value of type 'bool' after '!', the value type we found was: " + operand.rett.compat_display_name())
            }
            let v = vgen_unary(operand, op, type_bool(b))
            v.exp = exp
            // Flip issets when only one side is tracked (matches old !isset behavior)
            let is_list = operand.get_issets()
            let not_list = operand.get_not_issets()
            let is_count = (isset(is_list) ? is_list.length : 0) + (isset(not_list) ? not_list.length : 0)
            if is_count == 1 {
                v.set_issets(not_list)
                v.set_not_issets(is_list)
            }
            return v
        }
        let v = vgen_unary(operand, op, operand.rett)
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.op {
        return value_from_op_exp(exp, scope, ctx) !>
    }

    if kind == VALUE_KIND.is_a {
        let on_exp = exp.get(VALUE_EXP.left)
        let type_exp = exp.get(VALUE_EXP.right)
        if !isset(on_exp) || !isset(type_exp) : exp.bug("is_a is missing an operand")
        let on = value_from_exp(on_exp, scope, ctx.without_suggested_type()) !>
        let interface = on.rett.get_non_nullable().get_class_or_null()
        if !isset(interface) || interface.kind != CLASS_KIND.interface {
            on_exp.error("The left side of 'is_a' must be an interface value")
        }
        let target = type_from_exp(type_exp, scope) !>
        if target.is_nullable_type() : type_exp.error("The type after 'is_a' cannot be nullable")
        let concrete = target.get_class_or_null()
        if !isset(concrete) || concrete.kind != CLASS_KIND.class {
            type_exp.error("The type after 'is_a' must be a class")
        }
        resolve_class_interfaces(concrete) !>
        let adapter: ?Class = null
        if class_implements_interface(concrete, interface) {
            adapter = interface.interface_adapters.get(interface_adapter_key(concrete)) !? null
        }
        return vgen_is_a(on, adapter, b, exp)
    }

    if kind == VALUE_KIND.call {
        return value_from_call_exp(exp, scope, ctx) !>
    }

    if kind == VALUE_KIND.ternary {
        return value_from_ternary_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.null_coalesce {
        return value_from_null_coalesce_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.null_exit {
        return value_from_null_exit_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.isset {
        return value_from_isset_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.is_defined {
        let id = exp.get(VALUE_EXP.left)
        if !isset(id) : exp.bug("Missing is_defined identifier")
        let defined = false
        let ns_name = id.str2
        if isset(ns_name) {
            let ns_idf = scope.get_scope_idf(ns_name)
            if isset(ns_idf) {
                let ns_scope = ns_idf.scope
                if !isset(ns_scope) : exp.bug("Scope idf is missing scope")
                defined = isset(ns_scope.get_idf(id.str1))
            }
        } else {
            defined = isset(scope.get_idf(id.str1)) || id.str1 == "__PATH__" || id.str1 == "__FILE__" || id.str1 == "__DIR__" || id.str1 == "__LINE__"
        }
        let v = vgen_bool(defined, type_bool(b))
        v.exp = exp
        return v
    }
    if kind == VALUE_KIND.error_handler {
        return value_from_error_handler_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.match {
        return value_from_match_exp(exp, scope, ctx) !>
    }

    if kind == VALUE_KIND.cast {
        let on_exp = exp.get(VALUE_EXP.left)
        let type_exp = exp.get(VALUE_EXP.right)
        if !isset(on_exp) : exp.bug("Missing cast value")
        if !isset(type_exp) : exp.bug("Missing cast type")
        let cast_t = type_from_exp(type_exp, scope, ctx.suggested_type) !>
        // Suggest cast target for the value (`null.@cast(Node)` needs a type for null)
        let cast_ctx = cast_t.get_non_nullable().is_shared ? ctx.without_suggested_type() : ctx.with_suggested_type(cast_t)
        let on = value_from_exp(on_exp, scope, cast_ctx) !>
        if cast_t.get_non_nullable().is_fat_borrow() || on.rett.get_non_nullable().is_fat_borrow() {
            let borrowed = value_cast_borrow(on, cast_t, scope, ctx, exp) !>
            if isset(borrowed) {
                borrowed.exp = exp
                return borrowed
            }
        }
        let v = vgen_cast(on, cast_t)
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.dot {
        let v = value_from_dot_exp(exp, scope, ctx) !>
        lsp_hover(exp, v) !>
        return v
    }

    if kind == VALUE_KIND.typeof {
        // typeof(value) → VAL.type of the value's result type (rett)
        let inner_exp = exp.get(VALUE_EXP.left)
        if !isset(inner_exp) : exp.bug("Missing typeof operand")
        let inner = value_from_exp(inner_exp, scope, ctx.without_suggested_type()) !>
        let v = value_as_rett_type(inner, exp, b) !>
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.multi {
        // Multi-value `(a, b, …)` or bare multi list (call args)
        let args = Array[Value]{}
        let i: uint = 0
        // Suggested multi type suggests each part (return / let / assign)
        let suggested_type = ctx.suggested_type
        let part_types: ?Array[Type] = null
        if isset(suggested_type) && suggested_type.is_tuple_type() {
            part_types = suggested_type.get_members()
        }
        while true {
            let a = exp.get(i)
            if !isset(a) : break
            let part_ctx = ctx.without_suggested_type()
            let pt: ?Type = null
            if isset(part_types) {
                pt = part_types.get(i) !? null
                if isset(pt) : part_ctx = ctx.with_suggested_type(pt)
            }
            let av = value_from_exp(a, scope, part_ctx) !>
            av.require_result() !>
            // Convert each part toward suggested multi slot (e.g. int → String via $to)
            if isset(pt) && !av.rett.is_void_type() && !pt.is_void_type() {
                av = value_try_convert(av, scope, ctx, pt) !>
            }
            args.append(av)
            i++
        }
        let v = vgen_grouped_values(b, args)
        v.exp = exp
        return v
    }

    if kind == VALUE_KIND.incr || kind == VALUE_KIND.decr {
        let operand_exp = exp.get(VALUE_EXP.left)
        if !isset(operand_exp) : exp.bug("Missing ++/-- operand")
        let operand = value_from_exp(operand_exp, scope, ctx.without_suggested_type()) !>
        if isset(operand.rett.get_enum()) {
            exp.error("Cannot increment or decrement an enum; assign a declared enum value")
        }
        if operand.rett.is_nullable_type() && !value_stores_non_null(operand, scope) {
            exp.error("Cannot increment or decrement a nullable value; check it with isset first")
        }
        if value_is_copied_element(operand) {
            exp.error("Cannot change an element copied out of an Array; borrow it with '&mut arr[i]' or store the element back with '.set'")
        }
        if value_in_readonly_view(operand, ctx) : exp.error("Cannot change storage through a read-only borrow; " + value_readonly_hint(operand, ctx))
        value_check_captured_write(operand, exp) !>
        value_mark_summary_write(operand)
        if operand.type == VAL.prop || operand.type == VAL.ptrv {
            let owner = operand.value1
            if isset(owner) && !operand.rett.is_shared_writable() : value_note_receiver_mutation(owner, ctx)
        }
        act_check_assign(operand, exp, scope) !>
        value_mark_mutable(operand)
        value_check_shared_store(operand, exp) !>
        let op = kind == VALUE_KIND.incr ? VALUE_OP.add : VALUE_OP.sub
        if value_is_shared_storage(operand) && operand.rett.is_integer_type() {
            let one = vgen_int(1, operand.rett)
            let atomic = vgen_atomic_op(operand, one, op, exp)
            if exp.bool1 : return vgen_op(atomic, one, op, operand.rett)
            return atomic
        }
        // Result type matches operand (int-like); full assignability checks later
        let v = Value {
            type: VAL.op
            op: op
            value1: operand
            rett: operand.rett
            exp: exp
            bool_val: exp.bool1 // prefix flag stashed for IR later
        }
        return v
    }

    if kind == VALUE_KIND.error_code {
        let suggested_type = ctx.suggested_type
        if isset(suggested_type) {
            let class = suggested_type.get_non_nullable().get_class_or_null()
            if isset(class) {
                let func = class.get_func(exp.str1)
                if isset(func) && func.is_static {
                    let member = vgen_func(func)
                    member.exp = exp
                    return member
                }
            }
        }

        // Bare `.code` — implied ErrorType comes from the enclosing function's `!ErrorName`.
        let func = ctx.func
        if !isset(func) : exp.error("An error code ('." + exp.str1 + "') can only be used inside a function")
        let et = func.error_type
        if !isset(et) : exp.error("Throwing an error but your function has no error type")
        return value_from_error_code_item(et, exp.str1, exp) !>
    }

    if kind == VALUE_KIND.class_init {
        return value_from_class_init_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.slice_init {
        return value_from_slice_init_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.offset {
        return value_from_offset_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.ptrv {
        return value_from_ptrv_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.ref_of {
        return value_from_ref_of_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.vscope {
        return value_from_vscope_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.func_lit {
        return value_from_func_lit_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.co {
        return value_from_co_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.await {
        return value_from_await_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.error_is {
        return value_from_error_is_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.embed {
        return value_from_embed_exp(exp, scope, ctx) !>
    }
    if kind == VALUE_KIND.embed_dir {
        return value_from_embed_dir_exp(exp, scope, ctx) !>
    }
    exp.bug("Unknown value kind: " + kind)
}

////////////////////////////
// Embeds and coroutines
////////////////////////////

fn embed_base_dir(ctx: Context) String !Error {
    let func = ctx.get_func() !>
    let dir = func.ns.pkg.dir
    return dir ?? (fs.cwd() ! func.exp.error("Cannot find the current directory"))
}

fn value_from_embed_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let base = embed_base_dir(ctx) !>
    let full = helper.resolve(exp.str1, base)
    if !fs.exists(full) {
        exp.error("Embed file not found: " + full)
    }
    let content = fs.read(full) ! exp.error("Failed to read embed file: " + full)
    b.watch_files.append(full, true)
    let v = vgen_string(content, type_string(b))
    v.exp = exp
    return v
}

fn value_from_embed_dir_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let base = embed_base_dir(ctx) !>
    let dir = helper.resolve(exp.str1, base)
    if !fs.is_dir(dir) {
        exp.error("Embed directory not found: " + dir)
    }

    let idf = b.scope.get_idf("HashMap")
    if !isset(idf) : exp.bug("HashMap class not found (needed for #embed_dir)")
    if idf.type != IDF.class : exp.bug("HashMap is not a class")
    let map_base = idf.class
    if !isset(map_base) : exp.bug("HashMap idf is missing class")
    let str_t = type_string(b)
    let map_class = map_base.specialize(Array[Type]{ str_t, str_t }, exp) !>
    let rett = map_class.get_type() !>

    let construct = class_default_construct(map_class, rett, scope, ctx, exp) !>
    let set_f = map_class.get_set_key_hook()
    if !isset(set_f) : exp.bug("HashMap has no '$set_key' hook")
    if !set_f.parsed_types : parse_func_types(set_f) !>

    let files = fs.files_in(dir, true, true, false, true) ! exp.error("Failed to list embed directory: " + dir)
    // The directory itself too: its mtime changes when files are added/removed
    b.watch_files.append(dir, true)
    let chain = class_init_hook_base(ctx, construct)
    let hooks = Array[Value]{}
    each files as file {
        let full = fs.add(dir, file)
        let content = fs.read(full) ! exp.error("Error reading embed file: " + full)
        b.watch_files.append(full, true)
        let kv = vgen_string(file.replace("\\", "/"), str_t)
        let vv = vgen_string(content, str_t)
        let callee = vgen_func(set_f, class_init_hook_receiver(chain))
        let args = Array[Value]{ kv, vv }
        let call = vgen_call(ctx, callee, args, set_f.get_rett(), scope)
        call.exp = exp
        hooks.append(call)
        if class_init_hook_chains(chain, call, rett) : chain = call
    }

    let v = Value {
        type: VAL.class_init
        value1: chain
        values: hooks
        rett: rett
        exp: exp
    }
    return v
}

// Hook-style initializers (`Map{ k => v }`, `Array{ a, b }`, `#embed_dir`)
// call `$set_key` / `$append` on a fresh allocation. Those hooks allocate, so
// a collection can run in the middle of the chain; a heap receiver must be
// rooted in a frame buffer until the last hook is done, exactly like the
// receiver of a hand-written `Map{}.set(k, v).set(k2, v2)` chain. Inline
// aggregates (slices, structs) keep nesting: their hooks return a new value.
fn class_init_hook_base(ctx: Context, construct: Value) Value {
    let func = ctx.func ?! return construct
    if func.no_buffers || !construct.rett.is_gc_type() : return construct
    return buffer_value(ctx, func, construct)
}

// Each hook reads the rooted buffer; only an unbuffered chain nests the calls.
fn class_init_hook_receiver(chain: Value) Value {
    let decl = chain.decl ?! return chain
    if chain.type != VAL.decl : return chain
    return vgen_buffer_decl(decl, chain.rett)
}

fn class_init_hook_chains(chain: Value, call: Value, rett: Type) bool {
    if chain.type == VAL.decl && isset(chain.decl) : return false
    return !call.rett.is_void_type() && call.rett.compat(rett)
}

fn value_from_co_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let enclosing = ctx.get_func() !>
    enclosing.note_gc_allocation()
    enclosing.note_suspend()
    let call_exp = exp.get(VALUE_EXP.left)
    if !isset(call_exp) : exp.bug("'co' is missing a call expression")
    if call_exp.type != EXP.value || call_exp.value_kind != VALUE_KIND.call {
        call_exp.error("'co' can only be used on a function call")
    }
    let call = value_from_call_raw(call_exp, scope, ctx, true) !>
    let callee = call.value1
    if !isset(callee) : exp.bug("'co' call has no callable")
    value_check_co_lock_escape(call, callee, exp) !>
    if callee.rett.get_non_nullable().kind == TYPE.closure {
        call.value1 = buffer_value(ctx, enclosing, callee)
        call.op = CALL_KIND.closure
    }

    let error_type = call.get_error_type()
    validate_coroutine_error_type(error_type, exp) !>
    let co_type = type_callable(ctx.build, TYPE.co, Array[Type]{}, call.rett, error_type)
    let helper_name = "valk.coro." + enclosing.unit.name + "." + enclosing.unit.coro_count++
    let v = Value {
        type: VAL.co
        value1: call
        rett: co_type
        exp: exp
    }
    v.set_name(helper_name)
    return v
}

fn value_check_co_lock_escape(call: Value, callee: Value, at: Exp) !Error {
    let receiver = callee.type == VAL.func ? callee.value1 : null
    if isset(receiver) : value_check_co_arg(receiver, at) !>
    if callee.type != VAL.func {
        // A bound stack method reached through a variable or a parameter
        value_check_co_arg(callee, at) !>
        // The coroutine keeps the callable past the call
        value_mark_actual_escape(callee)
    }
    let args = call.values
    if !isset(args) : return
    each args as arg : value_check_co_arg(arg, at) !>
}

fn value_check_co_arg(arg: Value, at: Exp) !Error {
    if arg.rett.has_locked_refs() {
        at.error("A locked view cannot be handed to a coroutine; it is only valid inside its lock block")
    }
    // The coroutine may outlive the frame that owns the borrowed storage
    if arg.is_implicit_borrow() {
        at.error("A stack borrow cannot be handed to a coroutine; it may outlive the frame that owns the storage. Use an owned borrow ('&object.field') or pass a copy")
    }
}

fn value_from_error_is_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let code_exp = exp.get(VALUE_EXP.left)
    if !isset(code_exp) : exp.bug("'error_is' is missing a code value")
    let code = value_from_exp(code_exp, scope, ctx.without_suggested_type()) !>
    let et = code.rett.get_non_nullable().get_error_type()
    if !isset(et) {
        code_exp.error("'error_is' expects an error-code value as its first argument, got: " + code.rett.compat_display_name())
    }
    let names = exp.list
    if !isset(names) : exp.bug("'error_is' is missing code names")

    let result: ?Value = null
    each names as name {
        let candidate = value_from_error_code_item(et, name, exp) !>
        let cmp = vgen_op(code, candidate, VALUE_OP.eq, type_bool(b))
        cmp.exp = exp
        result = isset(result) ? vgen_op(result, cmp, VALUE_OP.or, type_bool(b)) : cmp
    }
    let v = result ?? vgen_bool(false, type_bool(b))
    v.exp = exp
    return v
}

////////////////////////////
// Closures and captures
////////////////////////////

fn func_prepare_closure(f: Func) !Error {
    if isset(f.capture_class) : return

    each f.capture_decls as decl {
        if decl.type.has_borrowed_refs() {
            f.exp.error("You cannot bind stack borrows in closures. Type not allowed: " + decl.type.compat_display_name())
        }
    }

    let class = Class {
        unit: f.unit
        ns: f.ns
        name: "closure_env_" + helper.ctxhash(func_ir_name(f))
        display_name: "<closure binding data>"
        act: ACT.private
        kind: CLASS_KIND.class
        scope: f.arg_scope
        exp: f.exp
    }
    if isset(f.class) && !f.is_static {
        let receiver_arg = f.args.get(0) !? null
        if !isset(receiver_arg) : f.exp.bug("Instance closure target has no receiver argument")
        class.add_prop(Prop {
            class: class
            name: "receiver"
            act: ACT.private
            exp: f.exp
            type: receiver_arg.type
        })
        each f.capture_args as capt {
            capt.capture_index = capt.capture_index + 1
        }
    }
    each f.capture_args as decl, i {
        class.add_prop(Prop {
            class: class
            name: "capture_" + i
            act: ACT.private
            exp: f.exp
            type: decl.type
        })
    }
    let capture_size = class.compute_body_size() !>
    let header: u32 = f.is_shared ? 0 : f.ns.build.options.target.ptr_size.to(u32)
    f.capture_class = class
    if capture_size > 0 {
        if header > 0 {
            each class.props as prop {
                prop.offset = prop.offset + header
            }
        }
        class.size = capture_size + header
        f.capture_header_size = header
        let allocator = allocator_for_class(Context.for_func(f), class, f.exp) !>
        f.capture_allocator = allocator
        if !f.is_shared : ensure_closure_clone_func(f) !>
        allocator.env_clone = f.capture_clone_func
    }
}

// The Func a value directly references.
fn value_resolve_func_ref(v: Value) ?Func {
    if v.type != VAL.func : return null
    return v.func
}

fn func_capture_decl(func: Func, decl: Decl, name: String, at: Exp) Decl !Error {
    if decl.owner == func : return decl
    if !func.is_literal {
        at.bug("A non-literal function cannot capture declaration: " + name)
    }

    let parent_scope = func.arg_scope.parent
    if !isset(parent_scope) : at.bug("Closure has no enclosing scope: " + name)
    let parent = parent_scope.func
    if !isset(parent) : at.bug("Closure has no enclosing function: " + name)

    let source = decl
    if source.owner != parent {
        source = func_capture_decl(parent, source, name, at) !>
    }
    if source.type.get_non_nullable().is_locked {
        at.error("A locked view cannot be captured by a closure; it is only valid inside its lock block")
    }
    source.is_captured = true
    value_mark_actual_escape(vgen_decl(source))
    if source.implicit_borrow {
        at.error("An implicit pointer borrow cannot be captured; use '@ref' explicitly")
    }

    let local_idf = func.arg_scope.identifiers
    if isset(local_idf) {
        let existing_idf = local_idf.get(name)
        if isset(existing_idf) && existing_idf.type == IDF.decl {
            let existing = existing_idf.decl
            if isset(existing) && existing.owner == func && existing.is_bind {
                return existing
            }
        }
    }

    let captured_type = source.type
    if func.is_shared && (source.type.has_gc_refs() || source.type.get_non_nullable().kind == TYPE.closure)
        && !source.type.get_non_nullable().is_shared
    {
        at.error("A shared function cannot capture non-shared data of type '" + source.type.compat_display_name() + "'; a mutable alias remains")
    }
    let captured = Decl.new(func, captured_type, true, name)
    captured.exp = source.exp
    captured.is_bind = true
    captured.is_shared_capture = func.is_shared
    captured.capture_index = func.capture_args.length
    func.capture_free = false
    func.capture_decls.append(source)
    func.capture_args.append(captured)
    // Freshly created, so it cannot already be listed.
    func.all_decls.append(captured)
    func.arg_scope.set_idf(name, Idf.for_decl(captured))

    if parent_scope.not_null_check(source) : func.arg_scope.mark_not_null(captured)
    return captured
}

fn value_from_func_lit_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let enclosing = ctx.get_func() !>
    let args_exp = exp.get(FUNC_LIT_EXP.args)
    if !isset(args_exp) : exp.bug("Function literal is missing an argument list")

    let suggested = ctx.suggested_type
    let shared = isset(suggested) && suggested.get_non_nullable().kind == TYPE.closure && suggested.get_non_nullable().is_shared
    let f = Func {
        unit: enclosing.unit
        ns: enclosing.ns
        name: "<anonymous>_" + helper.ctxhash(func_ir_name(enclosing))
        act: ACT.private
        exp: exp
        arg_scope: scope.sub_scope()
        args_exp: args_exp
        rett_exp: exp.get(FUNC_LIT_EXP.type)
        error: exp.get(FUNC_LIT_EXP.error)
        body: exp.get(FUNC_LIT_EXP.body)
        is_literal: true
        is_shared: shared
    }
    f.arg_scope.func = f
    f.info_used = enclosing.info_used
    f.unit.functions.append(f)
    parse_func_types(f) !>
    if shared {
        let thread_bound = type_has_coroutine_handle(f.get_rett(), Array[Class]{})
        each f.args as a : if type_has_coroutine_handle(a.type, Array[Class]{}) : thread_bound = true
        if thread_bound : exp.error("A shared function cannot take or return a coroutine handle; it is bound to the thread that created it")
    }
    parse_func_ast(f) !>
    if !f.capture_free : enclosing.note_gc_allocation()

    let arg_types = Array[Type]{}
    each f.args as a : arg_types.append(a.type)

    // Store the natural return type: void, T, or a multi-value type.
    let sub_type = f.get_rett()

    let fn_type = type_callable(b, TYPE.closure, arg_types, sub_type, f.error_type, shared)

    let v = vgen_func(f)
    v.rett = fn_type
    v.exp = exp
    return v
}

////////////////////////////
// Value scopes and raw memory
////////////////////////////

// `<{ ... }` — body re-parsed as statements; returns form the value (phi).
fn value_from_vscope_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.left)
    if !isset(body) : exp.bug("vscope is missing body")
    if body.type != EXP.body : exp.bug("vscope body must be EXP.body")

    let returns = Array[Value]{}
    let sub = scope.sub_scope()
    sub.vscope_returns = returns
    // Seed result type from the suggested type (assign / !? alt / let type / …)
    let suggested_type = ctx.suggested_type
    if isset(suggested_type) : sub.vscope_rett = suggested_type

    let state = ParseState.new()
    let stmts = Array[Value]{}
    let p = body_parser(body) !>
    let members = read_func_stmts(p) !>
    each members as mem {
        let v = body_value_from_exp(ctx, sub, state, mem) !>
        if isset(v) : stmts.append(v)
    }
    if state.has_open_if() || state.is_skipping() {
        exp.error("Missing compile macro '#end' in value scope")
    }
    // The body always falls through into its returned value
    let removed = sub.not_null_removed
    if isset(removed) : each removed as decl : scope.clear_not_null(decl)

    let rett = sub.vscope_rett
    if returns.length == 0 && sub.did_return {
        // The body always exits (throw/panic/return/...): no value is needed
        scope.did_return = true
        let phi = vgen_phi(Array[Value]{}, rett ?? type_void(ctx.build), exp)
        return vgen_vscope(stmts, phi, exp)
    }
    if !isset(rett) || returns.length == 0 || !sub.did_return {
        exp.error("Value scope did not return a value")
    }
    let retvs = Array[Value]{}
    let all_exit = true
    each returns as ret {
        let rv = ret.value1
        if !isset(rv) : exp.bug("Value scope return has no value")
        if !rv.never_returns : all_exit = false
        retvs.append(rv)
    }
    if all_exit : scope.did_return = true
    let phi = vgen_phi(retvs, rett, exp)
    return vgen_vscope(stmts, phi, exp)
}

// @ptrv(on, Type, offset?) — body re-parsed as value, type, optional value.
fn value_from_ptrv_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("@ptrv is missing body")
    if body.type != EXP.body : exp.bug("@ptrv body must be EXP.body")

    let b = ctx.build
    let p = body_parser(body) !>

    let on_exp = read_value_exp(p, true, true) !>
    p.expect(",", true, true) !>
    let type_exp = read_type_exp(p, true, true) !>

    let offset_exp: ?Exp = null
    let t = p.tok(true, true, false) !>
    if t == TOK.sign && p.word_is(",") {
        p.tok(true, true) !>
        let next = p.tok(true, true, false) !>
        if next == TOK.sign && p.word_is(")") {
            p.error("Trailing comma in @ptrv arguments")
        } else {
            if next == TOK.sign && p.word_is(",") {
                p.error("Empty item in @ptrv arguments")
            }
            offset_exp = read_value_exp(p, true, true) !>
            if read_comma_list_next(p, ")", "@ptrv arguments") !> {
                p.error("@ptrv expects at most three arguments")
            }
        }
    } else {
        p.expect(")", true, true) !>
    }

    let on = value_from_exp(on_exp, scope, ctx.without_suggested_type()) !>
    on = value_through_borrow(on, ctx)
    if !on.rett.is_pointer() {
        if !on.rett.is_inline_type() {
            on_exp.error("Expected a pointer value, not: '" + on.rett.compat_display_name() + "'")
        }
        let pointer_t = on.rett.adr_ref()
        on = value_try_convert(on, scope, ctx, pointer_t, true, true) !>
        pointer_t.compat_check(on.rett, on_exp) !>
    }

    let elem = type_from_exp(type_exp, scope) !>
    // Reading through a pointer that came out of shared storage keeps the view,
    // the same as indexing a shared sequence does.
    elem = on.rett.storage_view_of(elem)

    let offset = vgen_int(0, type_int(b))
    if isset(offset_exp) {
        offset = value_from_exp(offset_exp, scope, ctx.with_suggested_type(type_int(b))) !>
        if !offset.rett.is_integer_type() {
            offset_exp.error("Expected a number value, not: " + offset.rett.compat_display_name())
        }
    }

    return vgen_ptrv(on, offset, elem, exp)
}

// `$clone(value)` — deep-copy intrinsic.
fn value_from_clone_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let arg_exp = exp.get(VALUE_EXP.left)
    if !isset(arg_exp) : exp.bug("$clone is missing its argument")
    let arg = value_from_exp(arg_exp, scope, ctx.without_suggested_type()) !>
    let at = ctx.clone_at ?? exp
    return value_clone(arg, ctx, scope, at, isset(ctx.clone_at) ? ctx.clone_check_access : true) !>
}

fn value_from_hash_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let arg_exp = exp.get(VALUE_EXP.left)
    if !isset(arg_exp) : exp.bug("$hash is missing its argument")
    let arg = value_from_exp(arg_exp, scope, ctx.without_suggested_type()) !>
    return value_hash(arg, ctx, scope, exp) !>
}

fn value_hash(value: Value, ctx: Context, scope: Scope, at: Exp) Value !Error {
    if value.rett.is_nullable_type() {
        at.error("Cannot hash a nullable type")
    }
    let nn = value.rett.get_non_nullable()
    let class = value_member_class(nn)
    if !isset(class) {
        at.error("Type '" + nn.compat_display_name() + "' has no '$hash' function")
    }
    let method = class.get_hash_hook()
    if !isset(method) {
        at.error("Type '" + nn.compat_display_name() + "' has no '$hash' function")
    }
    return value_hash_method(value, method, ctx, scope, at) !>
}

fn value_hash_method(value: Value, method: Func, ctx: Context, scope: Scope, at: Exp) Value !Error {
    if !method.parsed_types : parse_func_types(method) !>
    let uint_t = type_from_core(ctx.build, "uint") !>
    value = value_read_borrowed_receiver(value, scope, ctx, at) !>
    if value.rett.is_nullable_type() : at.error("Cannot hash a nullable type")
    method = func_for_receiver(method, value, at) !>
    let callee = vgen_func(method, value)
    let args = Array[Value]{}
    value_check_call_args(method, callee, args, scope, ctx, at) !>
    let hashed = vgen_call(ctx, callee, args, uint_t, scope)
    hashed.exp = at
    return hashed
}

fn value_clone(value: Value, ctx: Context, scope: Scope, at: Exp, check_access: bool (true), allow_pointer_alias: bool (false)) Value !Error {
    let type = value.rett
    let nn = type.get_non_nullable()
    if nn.is_shared : return value
    if nn.kind == TYPE.closure : return value_fn_clone(value, ctx, scope, at) !>
    if allow_pointer_alias && !isset(nn.class)
        && (nn.is_bare_borrow() || nn.kind == TYPE.ptr_of || nn.kind == TYPE.adr_of) : return value
    let class = value_member_class(nn)
    if isset(class) {
        let method = class.get_clone_hook()
        if isset(method) && (!check_access || act_allows(at, method.act, method.exp, false)) {
            return value_clone_method(value, method, ctx, scope, at, check_access) !>
        }
    }
    if (nn.is_raw_pointer() || nn.kind == TYPE.ptr_of || nn.kind == TYPE.adr_of || nn.is_bare_borrow()) && !allow_pointer_alias {
        at.error("Cannot clone type '" + nn.compat_display_name() + "' because it aliases memory and has no accessible '$clone' function")
    }
    if nn.is_fixed_array_type() : return value_clone_array(value, ctx, scope, at, check_access, allow_pointer_alias) !>
    if nn.kind == TYPE.union : return value_clone_union(value, ctx, scope, at, check_access, allow_pointer_alias) !>
    if isset(class) && nn.is_structural_type() {
        return value_clone_object(value, class, ctx, scope, at, check_access, allow_pointer_alias) !>
    }
    return value
}

fn value_clone_method(value: Value, method: Func, ctx: Context, scope: Scope, at: Exp, check_access: bool) Value !Error {
    if !method.parsed_types : parse_func_types(method) !>

    let body = Array[Value]{}
    let source = value_bind_once(value, ctx, at, body)
    let receiver = value.rett.is_nullable_type() ? value_force_remove_nullable(source) : source
    receiver = value_read_borrowed_receiver(receiver, scope, ctx, at) !>
    let result = receiver
    if receiver.rett.is_nullable_type() {
        result = value_clone_method(receiver, method, ctx, scope, at, check_access) !>
    } else {
        method = get_func_clone_variant(method, at, check_access) !>
        method = func_for_receiver(method, receiver, at) !>
        let callee = vgen_func(method, receiver)
        let args = Array[Value]{}
        value_fill_trailing_defaults(method, call_receiver_skip(callee, method), args, scope, ctx, at) !>
        value_check_call_args(method, callee, args, scope, ctx, at) !>
        let cloned = vgen_call(ctx, callee, args, method.get_rett(), scope)
        cloned.exp = at
        result = cloned
        if method.can_error {
            cloned.can_error = true
            cloned.set_error_type(method.error_type)
            result = value_apply_error_pass(cloned, scope, ctx, at) !>
        }
    }
    if value.rett.is_nullable_type() {
        let actual = result.rett
        result = vgen_this_or_that(
            vgen_isset_val(source, type_bool(ctx.build)),
            value_make_nullable(result),
            vgen_null(actual.nullable()),
            actual.nullable()
        )
    }
    if body.length == 0 : return result
    return vgen_scoped_value(body, result, at)
}

fn value_clone_object(value: Value, class: Class, ctx: Context, scope: Scope, at: Exp, check_access: bool, allow_pointer_alias: bool) Value !Error {
    if check_access {
        each class.props as prop : act_check(at, prop.act, prop.exp, true, "property") !>
    }
    let body = Array[Value]{}
    let source = value_bind_once(value, ctx, at, body)
    let clone_source = value.rett.is_nullable_type() ? value_force_remove_nullable(source) : source
    let clone_func = value_clone_func(value.rett.get_non_nullable(), class, ctx, at, check_access, allow_pointer_alias) !>
    let cloned = vgen_call(ctx, vgen_func(clone_func), Array[Value]{ clone_source }, clone_func.get_rett(), scope)
    cloned.exp = at
    let result = cloned
    if value.rett.is_nullable_type() {
        let nullable = clone_func.get_rett().nullable()
        result = vgen_this_or_that(
            vgen_isset_val(source, type_bool(ctx.build)),
            value_make_nullable(cloned),
            vgen_null(nullable),
            nullable
        )
    }
    if body.length == 0 : return result
    return vgen_scoped_value(body, result, at)
}

fn value_clone_func(type: Type, class: Class, ctx: Context, at: Exp, check_access: bool, allow_pointer_alias: bool) Func !Error {
    let source = access_root_source(at.src)
    let key = type_identity_key(type) + ":" + source.id + ":" + at.src.ignore_access + ":" + check_access + ":" + allow_pointer_alias
    ctx.build.cache.clone_funcs.get(key) -> cached : return cached

    let owner = ctx.get_func() !>
    let arg_scope = Scope.new(owner.ns.scope)
    let clone_func = Func {
        unit: owner.unit
        ns: owner.ns
        name: "<clone." + class.name + "." + helper.ctxhash(key) + ">"
        act: ACT.private
        exp: at
        arg_scope: arg_scope
        parsed_types: true
        parsed_ast: true
        rett: type.plain_type()
    }
    ctx.build.cache.clone_funcs.set(key, clone_func)
    arg_scope.func = clone_func
    append_func_arg(clone_func, "value", type)
    bind_func_arg_decls(clone_func) !>
    func_begin_unique_summary(clone_func)

    let body_scope = arg_scope.sub_scope()
    body_scope.func = clone_func
    clone_func.body_scope = body_scope
    clone_func.alloca_scope = body_scope.sub_scope()
    clone_func.defer_scope = body_scope.sub_scope()
    let inner_ctx = Context.for_func(clone_func)
    let value_arg = clone_func.args.get(0) ! at.bug("Clone helper is missing its value")
    let source_value = vgen_decl(value_arg.decl)
    let fields = HashMap[Prop, Value]{}
    each class.props as prop {
        let field = vgen_prop(prop, source_value, inner_ctx)
        field.rett = type.storage_view_of(prop.type)
        let cloned = value_clone(field, inner_ctx, body_scope, at, check_access, allow_pointer_alias) !>
        prop.type.compat_check(cloned.rett, at) !>
        fields.set(prop, cloned)
    }
    let result = vgen_class_init(inner_ctx, class, fields, type.plain_type(), at) !>
    // Generated bodies need the same freshness proof as source functions.
    clone_func.unique_summary_saw_return = true
    clone_func.returns_unique_graph = value_summary_graph_is_unique(result, clone_func, clone_func.unique_return_args) !>
    if !clone_func.returns_unique_graph : value_mark_summary_escape(result)
    func_finish_unique_summary(clone_func) !>
    body_scope.append_value(vgen_return(result, ctx.build, at))
    body_scope.did_return = true

    owner.unit.functions.append(clone_func)
    func_finish_ast(clone_func) !>
    if !isset(ctx.build.options.lsp) : generate_func_ir(clone_func) !>
    return clone_func
}

fn value_bind_once(value: Value, ctx: Context, at: Exp, body: Array[Value], snapshot: bool (false)) Value {
    if !snapshot && value.type == VAL.decl && !isset(value.get_pre_value()) : return value
    let func = ctx.func ?! return value
    if value.rett.has_gc_refs() {
        let buffered = buffer_value(ctx, func, value)
        let pre = buffered.get_pre_value()
        if isset(pre) : body.append(pre)
        buffered.set_pre_value(null)
        buffered.set_graph_source(value)
        return buffered
    }
    let slot = vgen_scratch_value(func, value.rett, at)
    body.append(vgen_assign(slot, value, ASSIGN_OP.set, ctx.build, at))
    slot.set_graph_source(value)
    return slot
}

fn value_clone_array(value: Value, ctx: Context, scope: Scope, at: Exp, check_access: bool, allow_pointer_alias: bool) Value !Error {
    let type = value.rett.plain_type()
    let array = type.get_non_nullable()
    let sub = array.get_sub_type() ?! at.bug("Fixed-array clone has no element type")
    let body = Array[Value]{}
    let source = value_bind_once(value, ctx, at, body)
    let clone_source = source
    if type.is_nullable_type() : clone_source = value_force_remove_nullable(source)
    let elements = Array[Value]{}
    let i: uint = 0
    while i < array.length {
        let element = vgen_multi_part(clone_source, i, value.rett.storage_view_of(sub), at)
        let cloned = value_clone(element, ctx, scope, at, check_access, allow_pointer_alias) !>
        sub.compat_check(cloned.rett, at) !>
        elements.append(cloned)
        i++
    }
    let cloned = vgen_array_init(ctx, elements, array, at)
    let result = cloned
    if type.is_nullable_type() {
        result = vgen_this_or_that(
            vgen_isset_val(source, type_bool(ctx.build)),
            value_make_nullable(cloned),
            vgen_null(type),
            type
        )
    }
    if body.length == 0 : return result
    return vgen_scoped_value(body, result, at)
}

fn value_clone_union(value: Value, ctx: Context, scope: Scope, at: Exp, check_access: bool, allow_pointer_alias: bool) Value !Error {
    let type = value.rett.plain_type()
    let union = type.get_non_nullable()
    let members = union.get_members() ?! at.bug("Tagged-union clone has no alternatives")
    let body = Array[Value]{}
    let source = value_bind_once(value, ctx, at, body)
    let result: Value = vgen_null(type)
    let i = members.length
    while i-- > 0 {
        let member = members.get(i) ! at.bug("Tagged-union clone alternative is missing")
        if member.is_null_alt() {
            let wrapped = vgen_union_wrap(source, type, i, ctx) ?! at.bug("Failed to rebuild cloned tagged union")
            result = vgen_this_or_that(vgen_union_is(source, i, at), wrapped, result, type)
            continue
        }
        let extracted = vgen_union_extract(source, member, ctx, at) !>
        extracted.rett = value.rett.storage_view_of(member)
        let cloned = value_clone(extracted, ctx, scope, at, check_access, allow_pointer_alias) !>
        member.compat_check(cloned.rett, at) !>
        let wrapped = vgen_union_wrap(cloned, type, i, ctx) ?! at.bug("Failed to rebuild cloned tagged union")
        result = vgen_this_or_that(vgen_union_is(source, i, at), wrapped, result, type)
    }
    if body.length == 0 : return result
    return vgen_scoped_value(body, result, at)
}

fn value_from_memcpy_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("@memcpy is missing its argument body")
    let arg_exps = parse_body_as_value_list(body) !>
    if arg_exps.length != 3 {
        exp.error("@memcpy expects 3 arguments, got " + arg_exps.length)
    }

    let ptr_t = type_ptr(ctx.build)
    let uint_t = type_from_core(ctx.build, "uint") !>
    let values = Array[Value]{}
    each arg_exps as arg_exp, i {
        let target = i < 2 ? ptr_t : uint_t
        let value = value_from_exp(arg_exp, scope, ctx.with_suggested_type(target)) !>
        if i < 2 && value.rett.is_inline_type() : target = value.rett.adr_ref()
        value = value_try_convert(value, scope, ctx, target, true, i < 2) !>
        target.compat_check(value.rett, arg_exp) !>
        values.append(value)
    }
    return vgen_memcpy(values, ctx.build, exp)
}

// @ref(assignable) — body is a single lvalue; result is raw ptr.
fn value_from_ref_of_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("@ref is missing body")
    if body.type != EXP.body : exp.bug("@ref body must be EXP.body")

    let on_exp = parse_body_as_value_exp(body) !>
    let on = value_from_exp(on_exp, scope, ctx.without_suggested_type()) !>
    if !on.assignable() {
        exp.error("Cannot use '@ref on this value. It can only be used on assignable values, like variable names or object properties.")
    }
    act_check_assign(on, exp, scope) !>
    value_check_shared_address(on, exp) !>

    let storage = value_prepare_borrow_storage(on, ctx)
    if !isset(storage) : storage = on
    value_clear_borrowed_local_facts(on, scope)

    // Old @ref always yields raw `ptr` (not &T).
    let rett = type_ptr(ctx.build)
    return vgen_ptr_of(storage, rett, exp)
}

////////////////////////////
// Offset access
////////////////////////////

fn parse_offset_access(body: Exp) Exp !Error {
    let p = body_parser(body) !>
    let start = read_value_exp(p, true, true) !>
    let t = p.tok(true, true, false) !>

    if t == TOK.sign && p.word_is("]") {
        p.tok(true, true) !>
        return start
    }

    if t == TOK.sign && p.word_is("..") {
        p.tok(true, true) !>
        let length = read_value_exp(p, true, true) !>
        p.expect("]", true, true) !>
        let range = p.create_exp(EXP.value)
        range.i = start.i
        range.kind = VALUE_KIND.bracket_range
        range.set(VALUE_EXP.left, start)
        range.set(VALUE_EXP.right, length)
        range.set_end(p.token_end)
        return range
    }

    p.error("Expected '..' or ']' in offset, found: '" + p.word() + "'")
}

// Call a `$range` / `$view` hook with already converted `uint` bounds.
fn value_call_range_hook(on: Value, hook: Func, start_index: Value, length: Value, scope: Scope, ctx: Context, exp: Exp) Value !Error {
    let func = func_for_receiver(hook, on, exp) !>
    let args = Array[Value]{ start_index, length }
    let callee = vgen_func(func, on)
    value_check_call_args(func, callee, args, scope, ctx, exp) !>
    let call = vgen_call(ctx, callee, args, func.get_rett(), scope)
    call.exp = exp
    if func.can_error {
        call.can_error = true
        call.set_error_type(func.error_type)
    }
    return call
}

// The two bounds of `value[start .. length]`, converted to `uint`.
fn value_range_bounds(access: Exp, scope: Scope, ctx: Context) (Value, Value) !Error {
    let uint_t = type_from_core(ctx.build, "uint") !>
    let start_exp = access.get(VALUE_EXP.left)
    let length_exp = access.get(VALUE_EXP.right)
    if !isset(start_exp) || !isset(length_exp) : access.bug("Bracket range is missing an argument")
    let start = value_from_exp(start_exp, scope, ctx.with_suggested_type(uint_t)) !>
    let length = value_from_exp(length_exp, scope, ctx.with_suggested_type(uint_t)) !>
    start = value_try_convert(start, scope, ctx, uint_t) !>
    length = value_try_convert(length, scope, ctx, uint_t) !>
    uint_t.compat_check(start.rett, start_exp) !>
    uint_t.compat_check(length.rett, length_exp) !>
    return (start, length)
}

fn type_native_sequence(type: Type) Type {
    let t = type.get_non_nullable()
    if t.kind == TYPE.ptr_of || t.kind == TYPE.adr_of {
        let class = t.get_direct_class_or_null()
        if t.is_bounded_pointer() || (t.kind == TYPE.ptr_of && isset(class) && class.kind == CLASS_KIND.unbound) : return t
        let sub = t.get_sub_type()
        if isset(sub) && sub.kind == TYPE.array {
            return sub
        }
    }
    return t
}

fn type_has_native_sequence_length(type: Type) bool {
    let t = type_native_sequence(type)
    return t.is_fixed_array_type() || t.kind == TYPE.array
        || t.is_bounded_pointer() || t.kind == TYPE.ref_slice
}

fn type_native_sequence_element(type: Type) ?Type {
    let t = type_native_sequence(type)
    let element = t.get_sub_type()
    if isset(element) : return element
    let class = t.get_class_or_null()
    return isset(class) ? class.slice_type : null
}

fn value_native_sequence_length(on: Value, type: Type, ctx: Context, at: Exp) ?Value !Error {
    let t = type_native_sequence(type)
    let uint_t = type_from_core(ctx.build, "uint") !>
    if t.is_fixed_array_type() || t.is_bounded_pointer() {
        return vgen_int(t.length.to(int), uint_t)
    }
    if t.kind == TYPE.ref_slice : return value_owned_borrow_length(on, ctx) !>
    if t.kind != TYPE.array : return null
    let class = t.get_class_or_null()
    if isset(class) {
        let prop = class.get_prop_by_name("length") !? null
        if !isset(prop) : at.bug("Sequence type has no length property")
        let value = vgen_prop(prop, on, ctx)
        value.exp = at
        return value
    }
    return vgen_ptrv(on, vgen_int(0, uint_t), uint_t, at)
}

// The owner word for element stores: the storage block behind an array.
fn value_native_sequence_owner(on: Value, type: Type, ctx: Context, at: Exp) Value !Error {
    let t = type_native_sequence(type)
    let class = t.get_class_or_null() ?! at.bug("Sequence type has no storage owner")
    let prop = class.get_prop_by_name("data") ! at.bug("Sequence type has no data property")
    let value = vgen_prop(prop, on, ctx)
    value.exp = at
    return value
}

fn value_native_sequence_data(on: Value, type: Type, ctx: Context, at: Exp) Value !Error {
    let t = type_native_sequence(type)
    if t.is_fixed_array_type() || t.kind == TYPE.ptr_of || t.kind == TYPE.adr_of : return on
    if t.kind == TYPE.ref_slice : return vgen_cast(value_owned_borrow_address(on), type_ptr(ctx.build))
    let class = t.get_class_or_null()
    if isset(class) {
        let prop = class.get_prop_by_name("data") !? null
        if !isset(prop) : at.bug("Sequence type has no data property")
        let value = vgen_prop(prop, on, ctx)
        value.exp = at
        if class.kind == CLASS_KIND.array {
            // `data` is the storage block; the elements follow its header.
            let block = vgen_cast(value, type_ptr(ctx.build))
            let offset = vgen_int((class.slice_data_offset() !>).to(int), type_from_core(ctx.build, "uint") !>)
            let elements = vgen_ptr_offset(block, offset, at)
            elements.exp = at
            return elements
        }
        return value
    }
    let uint_t = type_from_core(ctx.build, "uint") !>
    return vgen_ptrv(on, vgen_int(1, uint_t), type_ptr(ctx.build), at)
}

fn value_native_sequence_item(on: Value, index: Value, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let t = type_native_sequence(on.rett)
    let element = type_native_sequence_element(t)
    if !isset(element) : at.bug("Sequence type has no element type")
    // An element read out of shared storage keeps the shared view
    element = t.storage_view_of(element)
    let data = value_native_sequence_data(on, t, ctx, at) !>
    return value_sequence_element_read(vgen_ptrv(data, index, element, at), scope, ctx, at) !>
}

fn native_sequence_helper_base(owner: Func, name: String, key: String, on_type: Type, rett: Type, at: Exp) Func !Error {
    let arg_scope = Scope.new(owner.ns.scope)
    let func = Func {
        unit: owner.unit
        ns: owner.ns
        name: "<sequence." + name + "." + helper.ctxhash(key) + ">"
        act: ACT.private
        exp: at
        arg_scope: arg_scope
        parsed_types: true
        parsed_ast: true
        rett: rett
        is_inline: true
    }
    arg_scope.func = func
    append_func_arg(func, "sequence", on_type)
    append_func_arg(func, "index", type_from_core(owner.ns.build, "uint") !>)
    return func
}

fn native_sequence_helper_finish(func: Func) !Error {
    func_finish_unique_summary(func) !>
    let owner_unit = func.unit
    owner_unit.functions.append(func)
    func_finish_ast(func) !>
    if !isset(func.ns.build.options.lsp) : generate_func_ir(func) !>
}

fn native_sequence_get_func(on_type: Type, ctx: Context, at: Exp) Func !Error {
    let key = type_identity_key(on_type)
    ctx.build.cache.sequence_get_funcs.get(key) -> cached : return cached

    let owner = ctx.get_func() !>
    let element = type_native_sequence_element(on_type)
    if !isset(element) : at.bug("Sequence type has no element type")
    let rett = on_type.storage_view_of(element)
    let func = native_sequence_helper_base(owner, "get", key, on_type, rett, at) !>
    ctx.build.cache.sequence_get_funcs.set(key, func)
    bind_func_arg_decls(func) !>
    func_begin_unique_summary(func)

    let body_scope = func.arg_scope.sub_scope()
    body_scope.func = func
    func.body_scope = body_scope
    func.alloca_scope = body_scope.sub_scope()
    func.defer_scope = body_scope.sub_scope()
    let inner_ctx = Context.for_func(func)
    let sequence_arg = func.args.get(0) ! at.bug("Sequence helper is missing its receiver")
    let index_arg = func.args.get(1) ! at.bug("Sequence helper is missing its index")
    let sequence = vgen_decl(sequence_arg.decl)
    let index = vgen_decl(index_arg.decl)
    let length = value_native_sequence_length(sequence, on_type, inner_ctx, at) !>
    if !isset(length) : at.bug("Sequence type has no length")
    let past_end = vgen_op(index, length, VALUE_OP.gte, type_bool(ctx.build))
    let panic_call = value_panic_call(inner_ctx, "Index out of bounds", at) !>
    body_scope.append_value(vgen_if(past_end, Array[Value]{ panic_call }, null, ctx.build, at))
    let result = value_native_sequence_item(sequence, index, body_scope, inner_ctx, at) !>
    result.rett = rett
    func.unique_summary_saw_return = true
    func.returns_unique_graph = value_summary_graph_is_unique(result, func, func.unique_return_args) !>
    if !func.returns_unique_graph : value_mark_summary_escape(result)
    body_scope.append_value(vgen_return(result, ctx.build, at))
    body_scope.did_return = true

    native_sequence_helper_finish(func) !>
    return func
}

fn native_sequence_set_func(on_type: Type, ctx: Context, at: Exp) Func !Error {
    let key = type_identity_key(on_type)
    ctx.build.cache.sequence_set_funcs.get(key) -> cached : return cached

    let owner = ctx.get_func() !>
    let element = type_native_sequence_element(on_type)
    if !isset(element) : at.bug("Sequence type has no element type")
    let func = native_sequence_helper_base(owner, "set", key, on_type, type_void(ctx.build), at) !>
    func.mutates_receiver = true
    append_func_arg(func, "value", element)
    ctx.build.cache.sequence_set_funcs.set(key, func)
    bind_func_arg_decls(func) !>
    func_begin_unique_summary(func)

    let body_scope = func.arg_scope.sub_scope()
    body_scope.func = func
    func.body_scope = body_scope
    func.alloca_scope = body_scope.sub_scope()
    func.defer_scope = body_scope.sub_scope()
    let inner_ctx = Context.for_func(func)
    let sequence_arg = func.args.get(0) ! at.bug("Sequence helper is missing its receiver")
    let index_arg = func.args.get(1) ! at.bug("Sequence helper is missing its index")
    let value_arg = func.args.get(2) ! at.bug("Sequence helper is missing its value")
    let sequence = vgen_decl(sequence_arg.decl)
    let index = vgen_decl(index_arg.decl)
    let value = vgen_decl(value_arg.decl)
    let length = value_native_sequence_length(sequence, on_type, inner_ctx, at) !>
    if !isset(length) : at.bug("Sequence type has no length")
    let kind = type_native_sequence(on_type).kind
    // Through a view the array cannot grow, so the end is out of bounds
    let is_view = type_native_sequence(on_type).is_shared || type_native_sequence(on_type).is_locked
    let invalid_op = kind == TYPE.array && !is_view ? VALUE_OP.gt : VALUE_OP.gte
    let invalid = vgen_op(index, length, invalid_op, type_bool(ctx.build))
    let panic_call = value_panic_call(inner_ctx, "Index out of bounds", at) !>
    body_scope.append_value(vgen_if(invalid, Array[Value]{ panic_call }, null, ctx.build, at))

    if kind == TYPE.array && !is_view {
        let class = on_type.get_non_nullable().get_class_or_null()
        let append_func = isset(class) ? class.get_append_hook() : null
        if !isset(append_func) : at.bug("Array type has no append function")
        if !append_func.parsed_types : parse_func_types(append_func) !>
        let at_end = vgen_op(index, length, VALUE_OP.eq, type_bool(ctx.build))
        let append_callee = vgen_func(append_func, sequence)
        let append_args = Array[Value]{ value }
        value_fill_trailing_defaults(append_func, call_receiver_skip(append_callee, append_func), append_args, body_scope, inner_ctx, at) !>
        value_check_call_args(append_func, append_callee, append_args, body_scope, inner_ctx, at, false) !>
        let append_call = vgen_call(inner_ctx, append_callee, append_args, append_func.get_rett(), body_scope)
        append_call.exp = at
        body_scope.append_value(vgen_if(at_end, Array[Value]{ append_call, vgen_return(null, ctx.build, at) }, null, ctx.build, at))
    }

    let data = value_native_sequence_data(sequence, on_type, inner_ctx, at) !>
    let prop = vgen_ptrv(data, index, element, at)
    // The element slot lives in the storage block, which is its owner.
    let block_owner = value_native_sequence_owner(sequence, on_type, inner_ctx, at) !>
    if type_native_sequence(on_type).is_shared {
        body_scope.append_value(vgen_atomic_store(prop, value, at))
    } else {
        body_scope.append_value(value_owner_store(block_owner, prop, value, body_scope, inner_ctx, at) !>)
    }
    body_scope.append_value(vgen_return(null, ctx.build, at))
    body_scope.did_return = true

    native_sequence_helper_finish(func) !>
    return func
}

fn value_from_offset_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let v = value_from_offset_raw_exp(exp, scope, ctx) !>
    if v.can_error : return value_apply_offset_default(v, exp)
    return v
}

fn value_apply_offset_default(call: Value, at: Exp) Value {
    let default_v = vgen_default_value(call.rett)
    return value_error_result(call, default_v, default_v.rett, ERRH_KIND.value, at)
}

// Mode-fallthrough lookup for the `$offset` get hook.
fn resolve_offset_get_func(class: Class) ?Func {
    let f = class.offset
    if isset(f) : return f
    let for_class = class.mode_for_class
    if isset(for_class) : return resolve_offset_get_func(for_class)
    return null
}

// Mode-fallthrough lookup for the `$offset_assign` set hook.
fn resolve_offset_assign_func(class: Class) ?Func {
    let f = class.offset_assign
    if isset(f) : return f
    let for_class = class.mode_for_class
    if isset(for_class) : return resolve_offset_assign_func(for_class)
    return null
}

fn func_for_receiver(func: Func, on: Value, at: Exp) Func !Error {
    parse_func_types(func) !>
    // Inferred arguments must be concrete before checking the receiver view.
    if func.has_generic_args : return func
    if !func.is_static && value_is_enum_storage(on) {
        let receiver = func.args.get(0) !? null
        if !func.is_generic_base && !func.has_generic_args && isset(receiver)
            && receiver.type.is_stack_borrow() && !isset(receiver.enum_receiver_pos) {
            receiver.enum_receiver_pos = Pos { src: at.src, i: at.i }
        }
    }
    if func.is_static || func.threadsafe : return func
    // A struct of plain words reads out of a view as an ordinary struct;
    // its method still stores into the view's storage
    let view = on.rett.storage_view_type_of()
    let view_class = view.get_direct_class_or_null()
    let in_storage = view == on.rett.get_non_nullable() && view.is_inline
        && isset(view_class) && view_class.representation_class().kind == CLASS_KIND.struct
    let locked = view.is_locked || (in_storage && value_is_locked_storage(on))
    let shared = view.is_shared || (in_storage && !locked && value_is_shared_storage(on))
    if func.is_clone_hook && !isset(func.clone_at) && (shared || locked) {
        func = get_func_clone_variant(func, at, true) !>
    }
    if locked {
        if func.is_interface {
            at.error("Cannot call interface method '" + func.name + "' on locked data of type '"
                + on.rett.compat_display_name()
                + "'. The implementation is not known here, so it cannot be checked against the lock rules. Call it through its concrete type instead")
        }
        return get_func_locked_receiver(func, at) !>
    }
    if !shared : return func
    if func_mutates_receiver(func) !> {
        at.error("Cannot call mutating method '" + func.name + "' on shared data of type '" + on.rett.compat_display_name() + "'")
    }
    // A concrete method gets one shared-receiver variant, built on demand and
    // cached on the base (see get_func_shared_receiver). An interface call has
    // no single body to build it from and its implementations are open, so
    // there is nothing here to prove the call safe.
    if func.is_interface {
        at.error("Cannot call interface method '" + func.name + "' on shared data of type '"
            + on.rett.compat_display_name()
            + "'. The implementation is not known here, so it cannot be checked against the shared rules. Call it through its concrete type instead")
    }
    return get_func_shared_receiver(func, at) !>
}

// Whether a method changes anything reachable from its receiver beyond
// atomic words: found in its own body, or in a method it calls on that data
fn func_mutates_receiver(func: Func) bool !Error {
    if func.threadsafe : return false
    if func.mutates_receiver : return true
    if func.checking_mutation : return false
    ensure_func_unique_return_summary(func) !>
    if func.mutates_receiver : return true
    let calls = func.this_calls ?! return false
    func.checking_mutation = true
    let found = false
    each calls as callee {
        if func_mutates_receiver(callee) !> {
            found = true
            break
        }
    }
    func.checking_mutation = false
    if found : func.mutates_receiver = true
    return found
}

// Whether the method stores into its receiver, atomic words included
fn func_writes_receiver(func: Func) bool !Error {
    ensure_func_unique_return_summary(func) !>
    // A body the LSP did not load, or one still being analyzed, is not
    // known to write; only a settled summary reports
    if !func.unique_summary_ready || func_unique_summary_unavailable_in_lsp(func) : return false
    let receiver = func.args.get(0) ! return false
    return receiver.may_write
}

// True when 'value' is, or was read out of, the current method's receiver
fn value_reaches_receiver(value: Value, ctx: Context) bool {
    let func = ctx.func ?? (ctx.get_func() !? null) ?! return false
    if !isset(func.class) || func.is_static : return false
    let this_arg = func.args.get(0) !? null
    if !isset(this_arg) : return false
    each (value_scratch_escape_origins(value, value.rett.build.cache.origins_buf)) as origin {
        if origin == this_arg.decl : return true
    }
    // A struct receiver is a borrow whose storage chain names it directly
    let current: ?Value = value
    while isset(current) {
        if current.type == VAL.decl : return current.decl == this_arg.decl
        let kind = current.type
        if kind != VAL.prop && kind != VAL.ptrv && kind != VAL.wrap && kind != VAL.cast : break
        current = current.value1
    }
    return false
}

// A store into 'owner' that is not an atomic word: the enclosing method
// mutates its receiver when 'owner' comes from it
fn value_note_receiver_mutation(owner: Value, ctx: Context) {
    let func = ctx.func ?? (ctx.get_func() !? null) ?! return
    if func.mutates_receiver || func.is_shared_receiver || func.is_locked_receiver : return
    if value_reaches_receiver(owner, ctx) : func.mutates_receiver = true
}

fn value_from_offset_raw_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let left_exp = exp.get(VALUE_EXP.left)
    if !isset(left_exp) : exp.bug("offset is missing base")
    let on = value_from_exp(left_exp, scope, ctx.without_suggested_type()) !>
    return value_from_offset_on(on, exp, scope, ctx) !>
}

// `on[...]` with the base already evaluated.
fn value_from_offset_on(on: Value, exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("offset is missing body")
    if body.type != EXP.body : exp.bug("offset body must be EXP.body")
    let b = ctx.build

    // Generic class specialize: `HashMap[String, uint]` / `X[String]`
    let gen_class = offset_generic_class(on)
    if isset(gen_class) {
        let type_exps = parse_body_as_type_list(body) !>
        if type_exps.length == 0 {
            exp.error("Generic type '" + gen_class.name + "' requires type arguments")
        }
        let gtypes = Array[Type]{}
        each type_exps as te {
            gtypes.append(type_from_exp(te, scope) !>)
        }
        let c = gen_class.specialize(gtypes, exp) !>
        let v = vgen_type(c.get_type() !>, b)
        v.exp = exp
        return v
    }

    // Generic free function: `foo[T]`
    let gen_func = offset_generic_func(on)
    if isset(gen_func) {
        let type_exps = parse_body_as_type_list(body) !>
        if type_exps.length == 0 {
            exp.error("Generic function '" + gen_func.name + "' requires type arguments")
        }
        let gtypes = Array[Type]{}
        each type_exps as te {
            gtypes.append(type_from_exp(te, scope) !>)
        }
        let f = gen_func.specialize(gtypes, exp) !>
        let receiver = on.value1
        if isset(receiver) : f = func_for_receiver(f, receiver, exp) !>
        return value_bind_method(f, receiver, ctx, exp)
    }

    let access = parse_offset_access(body) !>
    if on.rett.get_non_nullable().kind != TYPE.union : on = value_try_remove_nullable(on, scope, ctx) !>
    // `&[T]` and the named slices: a `$offset` / `$range` hook on the class
    // comes first, the native bounds-checked access is the fallback.
    let slice_t = on.rett.get_non_nullable()
    if slice_t.kind == TYPE.ref_slice {
        let slice_class = slice_t.class
        let hook: ?Func = null
        if isset(slice_class) {
            hook = access.value_kind == VALUE_KIND.bracket_range ? slice_class.get_range_hook() : resolve_offset_get_func(slice_class)
        } else if access.value_kind == VALUE_KIND.bracket_range {
            // `view[a..b]` copies like every other `value[a..b]`, after the
            // native bounds check; `&view[a..b]` is the view itself
            let element = slice_t.get_sub_type() ?! exp.bug("Slice borrow has no element type")
            let bare = bare_slice_class(b, element, exp) !>
            let part = bare.get_range_hook() ?! exp.bug("core '&[T]' has no '$range' method")
            let checked = value_from_ref_slice_offset(on, access, scope, ctx, exp) !>
            let setup = Array[Value]{}
            let rooted = value_bind_once(checked, ctx, exp, setup, true)
            let uint_t = type_from_core(b, "uint") !>
            let receiver = vgen_cast(rooted, bare.get_type() !>)
            receiver.exp = on.exp
            let length = value_owned_borrow_length(rooted, ctx) !>
            let copy = value_call_range_hook(receiver, part, vgen_int(0, uint_t), length, scope, ctx, exp) !>
            // Copied out of a view, the elements keep it
            return vgen_scoped_value(setup, value_retype_bare_slice(copy, slice_t.storage_view_of(element.ref_slice().mut_type()), exp), exp)
        }
        if !isset(hook) : return value_from_ref_slice_offset(on, access, scope, ctx, exp) !>
    }
    on = value_through_borrow(on, ctx)
    let on_t = on.rett.get_non_nullable()
    if on_t.is_fixed_array_type() || on_t.kind == TYPE.ptr_of || on_t.kind == TYPE.adr_of {
        if on.rett.is_nullable_type() : exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
    }
    if on_t.kind == TYPE.ptr_of && !on_t.is_bounded_pointer() {
        require_unsafe(exp, scope, "Offset access on an unbounded pointer") !>
    }
    if access.value_kind == VALUE_KIND.bracket_range {
        let start_exp = access.get(VALUE_EXP.left)
        let length_exp = access.get(VALUE_EXP.right)
        if !isset(start_exp) || !isset(length_exp) : access.bug("Bracket range is missing an argument")
        let uint_t = type_from_core(b, "uint") !>
        let start_index = value_from_exp(start_exp, scope, ctx.with_suggested_type(uint_t)) !>
        let length = value_from_exp(length_exp, scope, ctx.with_suggested_type(uint_t)) !>
        start_index = value_try_convert(start_index, scope, ctx, uint_t) !>
        length = value_try_convert(length, scope, ctx, uint_t) !>
        uint_t.compat_check(start_index.rett, start_exp) !>
        uint_t.compat_check(length.rett, length_exp) !>
        let inline_length_is_const = length.type == VAL.int
        let inline_length: uint = inline_length_is_const && length.int_val > 0 ? length.int_val.to(uint) : 0
        let on_class = on_t.get_class_or_null()
        let range_func = isset(on_class) ? on_class.get_range_hook() : null
        if (on_t.is_fixed_array_type() && on_t.length > 0) || on_t.is_bounded_pointer() {
            let checked_start, checked_length = value_check_known_length_range(start_index, length, on_t, on_t.length, ctx, exp) !>
            start_index = checked_start
            length = checked_length
        }
        if !isset(range_func) && (on_t.is_fixed_array_type() || on_t.is_bounded_pointer()) {
            if !inline_length_is_const {
                length_exp.error("Inline range length must be a compile-time integer")
            }
            if inline_length == 0 {
                length_exp.error("Inline range length must be greater than zero")
            }
            let element = on_t.get_sub_type()
            if !isset(element) : exp.bug("Sequence type has no element type")
            let setup = Array[Value]{}
            let data = on_t.is_fixed_array_type() ? vgen_ptr_of(on, type_ptr(b), exp) : on
            data = value_bind_once(data, ctx, exp, setup, true)
            let start = value_bind_once(start_index, ctx, exp, setup, true)
            let viewed_element = on_t.storage_view_of(element)
            let values = Array[Value]{}
            let i: uint = 0
            while i < inline_length {
                let index = i == 0 ? start : vgen_op(start, vgen_int(i.to(int), uint_t), VALUE_OP.add, uint_t)
                let slot = vgen_ptrv(data, index, viewed_element, exp)
                values.append(value_sequence_element_read(slot, scope, ctx, exp) !>)
                i++
            }
            return vgen_scoped_value(setup, vgen_array_init(ctx, values, viewed_element.fixed_array(inline_length), exp), exp)
        }
        if !isset(range_func) {
            exp.error("Type: '" + on.rett.compat_display_name() + "' has no '$range' function")
        }
        if on.rett.is_nullable_type() {
            exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
        }
        return value_call_range_hook(on, range_func, start_index, length, scope, ctx, exp) !>
    }

    // Index / offset — body is a single value expression.
    let index_exp = access
    let index = value_from_exp(index_exp, scope, ctx.without_suggested_type()) !>

    // Inline fixed sequence `[T x N]`
    if on_t.is_fixed_array_type() {
        let sub = on_t.get_sub_type()
        if !isset(sub) : exp.bug("Array type is missing sub_type")
        if !index.rett.is_integer_type() {
            index_exp.error("Invalid offset, expected an integer value instead of: " + index.rett.compat_display_name())
        }
        // An element read out of shared storage keeps the shared view
        sub = on_t.storage_view_of(sub)
        let size = on_t.length
        index = value_check_known_length_index(index, on_t, size, ctx, exp) !>
        return value_sequence_element_read(vgen_ptrv(on, index, sub, exp), scope, ctx, exp) !>
    }

    // *T / &T / *[T x N] with known element type
    let direct_class = on_t.get_direct_class_or_null()
    let unbound_class = isset(direct_class) && direct_class.kind == CLASS_KIND.unbound ? direct_class : null
    let unbound_hook = isset(unbound_class) ? resolve_offset_get_func(unbound_class) : null
    if (on_t.kind == TYPE.ptr_of && !isset(unbound_hook)) || on_t.kind == TYPE.adr_of {
        let sub = on_t.get_sub_type()
        if isset(sub) {
            let element = sub
            if !index.rett.is_integer_type() {
                index_exp.error("Invalid offset, expected an integer value instead of: " + index.rett.compat_display_name())
            }
            if on_t.is_bounded_pointer() {
                index = value_check_known_length_index(index, on_t, on_t.length, ctx, exp) !>
            }
            return value_sequence_element_read(vgen_ptrv(on, index, element, exp), scope, ctx, exp) !>
        }
    }

    // A named Array may define its own indexing contract. Use the native
    // bounds-checked operation only when it has no `$offset` hook.
    let sequence_class = on_t.get_class_or_null()
    let sequence_offset = isset(sequence_class) ? resolve_offset_get_func(sequence_class) : null
    if on_t.kind == TYPE.array && !isset(sequence_offset) {
        if on.rett.is_nullable_type() {
            exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
        }
        let uint_t = type_from_core(b, "uint") !>
        index = value_try_convert(index, scope, ctx, uint_t) !>
        uint_t.compat_check(index.rett, index_exp) !>
        let get_func = native_sequence_get_func(on.rett, ctx, exp) !>
        let callee = vgen_func(get_func)
        let args = Array[Value]{ on, index }
        value_check_call_args(get_func, callee, args, scope, ctx, exp, false) !>
        let call = vgen_call(ctx, callee, args, get_func.get_rett(), scope)
        call.exp = exp
        if call.rett.is_inline_type() : call.ensure_meta().copied_element = true
        return call
    }

    // User-defined class `$offset` hook.
    let on_class = on_t.get_class_or_null()
    if isset(on_class) {
        let get_func = resolve_offset_get_func(on_class)
        if isset(get_func) {
            if on.rett.is_nullable_type() {
                let nullable_class = on.rett.get_non_nullable().class
                if !isset(nullable_class) {
                    exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
                } else if nullable_class.kind != CLASS_KIND.union {
                    exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
                }
            }
            get_func = func_for_receiver(get_func, on, exp) !>
            let arg_skip: uint = 0
            if isset(get_func.class) && !get_func.is_static : arg_skip = 1
            let index_arg = get_func.args.get(arg_skip) ! exp.bug("'$offset' function is missing its index parameter")
            index = value_try_convert(index, scope, ctx, index_arg.type) !>
            index_arg.type.compat_check(index.rett, index_exp) !>
            if on_t.is_bounded_pointer() {
                index = value_check_known_length_index(index, on_t, on_t.length, ctx, exp) !>
            }

            let callee = vgen_func(get_func, on)
            let args = Array[Value]{ index }
            value_check_call_args(get_func, callee, args, scope, ctx, exp) !>
            let call = vgen_call(ctx, callee, args, get_func.get_rett(), scope)
            call.exp = exp
            if get_func.can_error {
                call.can_error = true
                call.set_error_type(get_func.error_type)
            }
            return call
        }
    }

    exp.error("Type: '" + on.rett.compat_display_name() + "' has no '[]' get offset function")
}

// Read an element out of sequence storage. A managed slot carries collector
// tag bits, so the read goes through the property read path to strip them,
// and a non-nullable reference type is checked:
// storage can hold an empty slot (zero-filled, or cleared by a removal), and
// handing that out as a valid reference would be unsound.
fn value_sequence_element_read(slot: Value, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let read = slot
    if slot.rett.has_gc_refs() : read = value_property_get(slot, scope, ctx, at) !>
    read = value_check_present_element(read, ctx, at) !>
    if read == slot : return slot
    // Reads validate and clean the value; borrows still name the original slot.
    let result = Value {
        type: slot.type
        rett: slot.rett
        value1: slot.value1
        value2: slot.value2
        exp: at
    }
    result.set_pre_value(slot.get_pre_value())
    result.set_checked_read(read)
    return result
}

fn value_check_known_length_index(
    index: Value,
    on_type: Type,
    size: uint,
    ctx: Context,
    at: Exp
) Value !Error {
    if index.type == VAL.int {
        if index.int_val < 0 || index.int_val.@cast(uint) >= size {
            at.error("Out of bounds. You are asking for index '" + index.int_val + "' on type: " + on_type.compat_display_name() + ". The last index is: " + (size - 1))
        }
        return index
    }

    let checked_index = vgen_scratch_value(ctx.get_func() !>, index.rett, at)
    let index_decl = checked_index.decl
    if !isset(index_decl) : at.bug("Bounds-check index has no scratch declaration")
    let check_index = vgen_decl(index_decl)
    let result_index = vgen_decl(index_decl)
    let uint_t = type_from_core(ctx.build, "uint") !>
    let unsigned_index = vgen_cast(check_index, uint_t)
    let past_end = vgen_op(
        unsigned_index,
        vgen_int(size.to(int), uint_t),
        VALUE_OP.gte,
        type_bool(ctx.build)
    )
    past_end.exp = at

    let panic = value_get_valk_func(ctx.build, "core", "panic", at) !>
    if !panic.parsed_types : parse_func_types(panic) !>
    let panic_call = vgen_call(
        ctx,
        vgen_func(panic),
        Array[Value]{ vgen_string("Index out of bounds", type_string(ctx.build)) },
        panic.get_rett()
    )
    panic_call.exp = at
    result_index.set_pre_value(vgen_block(Array[Value]{
        vgen_assign(checked_index, index, ASSIGN_OP.set, ctx.build, at)
        vgen_if(past_end, Array[Value]{ panic_call }, null, ctx.build, at)
    }, ctx.build, at))
    return result_index
}

fn value_check_known_length_range(
    start: Value,
    length: Value,
    on_type: Type,
    size: uint,
    ctx: Context,
    at: Exp
) (Value, Value) !Error {
    if start.type == VAL.int && length.type == VAL.int {
        if start.int_val < 0 || length.int_val < 0
            || start.int_val.@cast(uint) > size
            || length.int_val.@cast(uint) > size - start.int_val.@cast(uint)
        {
            at.error("Out of bounds range on type: " + on_type.compat_display_name())
        }
        return (start, length)
    }

    let func = ctx.get_func() !>
    let start_slot = vgen_scratch_value(func, start.rett, at)
    let length_slot = vgen_scratch_value(func, length.rett, at)
    let start_decl = start_slot.decl
    let length_decl = length_slot.decl
    if !isset(start_decl) || !isset(length_decl) : at.bug("Range bounds have no scratch declarations")
    let uint_t = type_from_core(ctx.build, "uint") !>
    let checked_start = vgen_cast(vgen_decl(start_decl), uint_t)
    let checked_length = vgen_cast(vgen_decl(length_decl), uint_t)
    let limit = vgen_int(size.to(int), uint_t)
    let start_past = vgen_op(checked_start, limit, VALUE_OP.gt, type_bool(ctx.build))
    let remaining = vgen_op(limit, checked_start, VALUE_OP.sub, uint_t)
    let length_past = vgen_op(checked_length, remaining, VALUE_OP.gt, type_bool(ctx.build))
    let invalid = vgen_op(start_past, length_past, VALUE_OP.or, type_bool(ctx.build))

    let panic = value_get_valk_func(ctx.build, "core", "panic", at) !>
    if !panic.parsed_types : parse_func_types(panic) !>
    let panic_call = vgen_call(
        ctx,
        vgen_func(panic),
        Array[Value]{ vgen_string("Range out of bounds", type_string(ctx.build)) },
        panic.get_rett()
    )
    panic_call.exp = at
    let result_start = vgen_decl(start_decl)
    result_start.set_pre_value(vgen_block(Array[Value]{
        vgen_assign(start_slot, start, ASSIGN_OP.set, ctx.build, at)
        vgen_assign(length_slot, length, ASSIGN_OP.set, ctx.build, at)
        vgen_if(invalid, Array[Value]{ panic_call }, null, ctx.build, at)
    }, ctx.build, at))
    return (result_start, vgen_decl(length_decl))
}

fn value_from_offset_assign_exp(ctx: Context, scope: Scope, left_exp: Exp, right_exp: Exp, at: Exp) ?Value !Error {
    let base_exp = left_exp.get(VALUE_EXP.left)
    if !isset(base_exp) : left_exp.bug("offset is missing base")
    let body = left_exp.get(VALUE_EXP.right)
    if !isset(body) : left_exp.bug("offset is missing body")
    if body.type != EXP.body : left_exp.bug("offset body must be EXP.body")

    let access = parse_offset_access(body) !>
    if access.value_kind == VALUE_KIND.bracket_range {
        left_exp.error("Cannot assign to a bracket range")
    }

    let on = value_from_exp(base_exp, scope, ctx.without_suggested_type()) !>
    if on.rett.get_non_nullable().kind != TYPE.union : on = value_try_remove_nullable(on, scope, ctx) !>
    let slice_t = on.rett.get_non_nullable()
    if slice_t.kind == TYPE.ref_slice {
        let slice_class = slice_t.class
        let hook = isset(slice_class) ? resolve_offset_assign_func(slice_class) : null
        if !isset(hook) : return value_from_ref_slice_offset_assign(on, access, right_exp, scope, ctx, left_exp, at) !>
    }
    on = value_through_borrow(on, ctx)
    let on_t = on.rett.get_non_nullable()
    if on_t.is_fixed_array_type() || on_t.kind == TYPE.ptr_of || on_t.kind == TYPE.adr_of {
        if on.rett.is_nullable_type() : left_exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
    }
    if value_is_readonly_storage(on, ctx) : left_exp.error("Cannot assign through a read-only borrow; " + value_readonly_hint(on, ctx))
    if on_t.is_fixed_array_type() : value_check_captured_write(on, left_exp) !>
    if on_t.kind == TYPE.ptr_of && !on_t.is_bounded_pointer() {
        require_unsafe(left_exp, scope, "Offset access on an unbounded pointer") !>
    }
    let sequence_class = on_t.get_class_or_null()
    let sequence_offset_assign = isset(sequence_class) ? resolve_offset_assign_func(sequence_class) : null
    if on_t.kind == TYPE.array && !isset(sequence_offset_assign) {
        if on.rett.is_nullable_type() {
            left_exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
        }
        let element = type_native_sequence_element(on_t)
        if !isset(element) : left_exp.bug("Sequence type has no element type")
        // A plain element is one word: stored atomically, it is race safe.
        // Managed references follow the property rules instead.
        let plain_element = element.is_shared_writable()
        if type_native_sequence(on.rett).is_shared && !plain_element {
            left_exp.error("Cannot call mutating method 'set' on shared data of type '" + on.rett.compat_display_name() + "'")
        }
        if type_native_sequence(on.rett).is_locked && !plain_element {
            left_exp.error("Cannot assign elements holding managed references through a locked view; use '.set(index, value)'")
        }
        let index_exp = access
        let uint_t = type_from_core(ctx.build, "uint") !>
        let index = value_from_exp(index_exp, scope, ctx.with_suggested_type(uint_t)) !>
        index = value_try_convert(index, scope, ctx, uint_t) !>
        uint_t.compat_check(index.rett, index_exp) !>
        let right = value_from_exp(right_exp, scope, ctx.with_suggested_type(element)) !>
        right = value_try_convert(right, scope, ctx, element) !>
        element.compat_check(right.rett, right_exp) !>
        let set_func = native_sequence_set_func(on.rett, ctx, left_exp) !>
        let callee = vgen_func(set_func)
        let args = Array[Value]{ on, index, right }
        value_check_call_args(set_func, callee, args, scope, ctx, at, false) !>
        let call = vgen_call(ctx, callee, args, set_func.get_rett(), scope)
        call.exp = at
        return call
    }
    if on_t.is_fixed_array_type() || on_t.kind == TYPE.adr_of {
        return value_from_managed_element_assign(on, on_t, access, right_exp, scope, ctx, left_exp, at) !>
    }
    let on_class = on_t.kind == TYPE.ptr_of ? on_t.get_direct_class_or_null() : on_t.get_class_or_null()
    if !isset(on_class) : return null
    if on_t.kind == TYPE.ptr_of && on_class.kind != CLASS_KIND.unbound : return null
    let set_func = resolve_offset_assign_func(on_class)
    if !isset(set_func) : return null

    if on.rett.is_nullable_type() {
        left_exp.error("Cannot use '[]' on a nullable value: " + on.rett.compat_display_name())
    }
    set_func = func_for_receiver(set_func, on, left_exp) !>

    let index_exp = access
    let index = value_from_exp(index_exp, scope, ctx.without_suggested_type()) !>

    let arg_skip: uint = 0
    if isset(set_func.class) && !set_func.is_static : arg_skip = 1
    let index_arg = set_func.args.get(arg_skip) ! at.bug("'$offset_assign' function is missing its index parameter")
    index = value_try_convert(index, scope, ctx, index_arg.type) !>
    index_arg.type.compat_check(index.rett, index_exp) !>
    if on_t.is_bounded_pointer() {
        index = value_check_known_length_index(index, on_t, on_t.length, ctx, left_exp) !>
    }

    let value_arg = set_func.args.get(arg_skip + 1) ! at.bug("'$offset_assign' function is missing its value parameter")
    let right = value_from_exp(right_exp, scope, ctx.with_suggested_type(value_arg.type)) !>
    right = value_try_convert(right, scope, ctx, value_arg.type) !>
    value_arg.type.compat_check(right.rett, right_exp) !>

    let callee = vgen_func(set_func, on)
    let args = Array[Value]{ index, right }
    value_check_call_args(set_func, callee, args, scope, ctx, at) !>
    let call = vgen_call(ctx, callee, args, set_func.get_rett(), scope)
    call.exp = at
    if set_func.can_error {
        call.can_error = true
        call.set_error_type(set_func.error_type)
        return value_apply_error_pass(call, scope, ctx, at) !>
    }
    return call
}

// Generic class base from a value left of `[T, …]`.
fn offset_generic_class(on: Value) ?Class {
    if on.type == VAL.type {
        let base = on.get_class_ref()
        if isset(base) && base.is_generic_base : return base
        let t = on.get_of_type()
        if !isset(t) : return null
        let c = t.class
        if isset(c) && c.is_generic_base : return c
    }
    return null
}

// Generic free-function base from a value left of `[T, …]`.
fn offset_generic_func(on: Value) ?Func {
    if on.type != VAL.func : return null
    let f = on.func
    if isset(f) && f.is_generic_base : return f
    return null
}

////////////////////////////
// Class initializers
////////////////////////////

class ClassInitItems {
    fields: Array[Exp] (.{})
    field_scopes: Array[Scope] (.{})
    elems: Array[Exp] (.{})
    pairs: Array[Exp] (.{})
}

fn property_loop_type(scope: Scope, name: String, at: Exp) Type !Error {
    let idf = scope.get_idf(name)
    if !isset(idf) : at.error("Unknown type in '#loop properties': " + name)
    if idf.type == IDF.type {
        let t = idf.of_type
        if !isset(t) : at.bug("Type idf is missing of_type")
        return t
    }
    if idf.type == IDF.class {
        let class = idf.class
        if !isset(class) : at.bug("Class idf is missing Class")
        return class.get_type() !>
    }
    if idf.type == IDF.property {
        let prop = idf.prop
        if !isset(prop) : at.bug("Property idf is missing Prop")
        return prop.type
    }
    at.error("Identifier is not a type in '#loop properties': " + name)
}

fn class_init_find_loop_end(items: Exp, start: uint, at: Exp) uint !Error {
    let depth: uint = 0
    let i = start + 1
    while true {
        let item = items.get(i)
        if !isset(item) : at.error("Missing '#end' for '#loop properties'")
        if item.type == EXP.macro {
            let kind = item.kind.@cast(MACRO_KIND)
            if kind == MACRO_KIND.loop || kind == MACRO_KIND.if {
                depth++
            } else if kind == MACRO_KIND.end {
                if depth == 0 : return i
                depth--
            }
        }
        i++
    }
}

fn append_class_init_item_range(out: ClassInitItems, items: Exp, start: uint, end: uint, scope: Scope, ctx: Context) !Error {
    let state = ParseState.new()
    let i = start
    while i < end {
        let item = items.get(i)
        if !isset(item) : break
        if item.type == EXP.macro {
            let kind = item.kind.@cast(MACRO_KIND)
            if kind == MACRO_KIND.loop && !state.is_skipping() {
                let close = class_init_find_loop_end(items, i, item) !>
                let words = item.list
                if !isset(words) {
                    item.error("Expected '#loop properties TYPE as PROPERTY' or '#loop object VALUE as PROPERTY'")
                }
                let loop_kind = words.get(0) !? ""
                if words.length != 4 || (words.get(2) !? "") != "as" {
                    item.error("Expected '#loop properties TYPE as PROPERTY' or '#loop object VALUE as PROPERTY'")
                }
                let prop_name = words.get(3) ! item.bug("Missing property-loop binding")

                if loop_kind == "object" {
                    let value_name = words.get(1) ! item.bug("Missing object-loop value")
                    let idf = scope.get_idf(value_name) ?! item.error("Expected a variable after '#loop object'")
                    if idf.type != IDF.decl {
                        item.error("Expected a variable after '#loop object'")
                    }
                    let decl = idf.decl
                    if !isset(decl) : item.bug("Object-loop variable is missing its declaration")
                    let owner_class = decl.type.get_non_nullable().get_class_or_null()
                    if isset(owner_class) {
                        let owner = vgen_decl(decl)
                        each owner_class.props as prop {
                            let iter_scope = scope.sub_scope()
                            iter_scope.func = scope.func
                            iter_scope.set_idf(prop_name, Idf.for_object_property(prop, vgen_prop(prop, owner, ctx)))
                            append_class_init_item_range(out, items, i + 1, close, iter_scope, ctx) !>
                        }
                    }
                    i = close + 1
                    continue
                }
                if loop_kind != "properties" {
                    item.error("Unknown '#loop' kind in a class initializer: " + loop_kind)
                }
                let type_name = words.get(1) ! item.bug("Missing property-loop type")
                let type = property_loop_type(scope, type_name, item) !>
                let class = type.get_non_nullable().get_class_or_null()
                if isset(class) {
                    each class.props as prop {
                        let iter_scope = scope.sub_scope()
                        iter_scope.func = scope.func
                        iter_scope.set_idf(prop_name, Idf.for_property(prop))
                        append_class_init_item_range(out, items, i + 1, close, iter_scope, ctx) !>
                    }
                }
                i = close + 1
                continue
            }
            let func = ctx.get_func() !>
            parse_macro_exp(func.ns, scope, state, item) !>
            i++
            continue
        }
        if state.is_skipping() {
            i++
            continue
        }
        let ik = item.number.@cast(CLASS_INIT_ITEM)
        if ik == CLASS_INIT_ITEM.field || item.str1.length > 0 {
            out.fields.append(item)
            out.field_scopes.append(scope)
        } else if ik == CLASS_INIT_ITEM.pair {
            out.pairs.append(item)
        } else if ik == CLASS_INIT_ITEM.elem {
            out.elems.append(item)
        } else {
            item.error("Invalid class init item")
        }
        i++
    }
    if state.has_open_if() || state.is_skipping() {
        items.error("Missing '#end' inside class initializer")
    }
}

fn value_from_class_init_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("class_init is missing body")
    if body.type != EXP.body : exp.bug("class_init body must be EXP.body")

    let left_exp = exp.get(VALUE_EXP.left)
    let suggested = ctx.suggested_type
    if !isset(left_exp) && isset(suggested) {
        let suggested_nn = suggested.get_non_nullable()
        if suggested_nn.kind == TYPE.adr_of || suggested_nn.is_bare_borrow() {
            exp.error("'.{}' cannot initialize a borrowed reference; initialize an inline value and borrow it with '&'")
        }
        if suggested_nn.is_struct_object() {
            let class = suggested_nn.get_direct_class_or_null()
            let name = isset(class) ? class.pretty_name() : "T"
            exp.error("'.{}' cannot initialize a pointer; use mem.new[" + name + "]() for manual heap allocation")
        }
    }

    if !isset(left_exp) && !exp.bool1 && !ctx.is_direct_init {
        exp.error("A bare '{ ... }' initializer requires direct destination storage; use '.{ ... }' in expression contexts")
    }
    let class: ?Class = null
    let rett: ?Type = null
    let left_val: ?Value = null

    if isset(left_exp) && left_exp.type == EXP.type {
        rett = type_from_exp(left_exp, scope) !>
        if rett.is_fixed_array_type() {
            return value_from_array_init_body(exp, body, rett, scope, ctx) !>
        }
        class = rett.get_class_or_null()
        if !isset(class) : exp.error("Class init requires a class or struct type before '{'")
    }

    // `.code { field: value, ... }` — error payload construction, not a class/struct init.
    if isset(left_exp) && left_exp.type == EXP.value && left_exp.value_kind == VALUE_KIND.error_code {
        return value_from_error_payload_init_exp(exp, scope, ctx, left_exp) !>
    }

    // Bare `{ … }` with a suggested fixed array → element list (positional only)
    if !isset(left_exp) {
        let suggested_type = ctx.suggested_type
        if isset(suggested_type) {
            let atype = suggested_type.get_non_nullable()
            if atype.is_fixed_array_type() {
                return value_from_array_init_body(exp, body, suggested_type, scope, ctx) !>
            }
        }
    }

    if isset(left_exp) {
        if left_exp.type != EXP.type {
            left_val = value_from_exp(left_exp, scope, ctx.without_suggested_type()) !>
        }
        // `E { field: value, ... }` — rethrow keeping E's runtime code, patch payload.
        if isset(left_val) {
            let left_et_t = left_val.rett.get_non_nullable()
            if left_et_t.kind == TYPE.error_code && left_et_t.error_is_instance {
                return value_from_error_rethrow_payload_exp(exp, scope, ctx, left_val) !>
            }
            class = left_val.as_class_for_init()
            if !isset(class) {
                exp.error("Class init requires a class or struct type before '{'")
            }
            if left_val.type == VAL.type {
                let explicit = left_val.get_of_type()
                if isset(explicit) : rett = explicit
            }
        }
    } else {
        // Bare `{ fields }` — need a suggested type (assign / prop field / arg / return)
        let suggested_type = ctx.suggested_type
        if !isset(suggested_type) {
            exp.error("You cannot use '{' here. It can only be used for defining the starting value for inline types. E.g. let myvar: [u8 x 2] = { 6, 7 }")
        }
        // Suggested class type (e.g. Array[String] default `.{}`) — hook/field init
        let suggested_class = suggested_type.get_non_nullable().get_class_or_null()
        if isset(suggested_class) {
            class = suggested_class
            rett = suggested_type.get_non_nullable()
        } else {
            class = class_from_inline_init_type(suggested_type)
            if !isset(class) {
                exp.error("The '{' token can only be used to define initialization values for inline struct, fixed arrays, or class types. Not for: " + suggested_type.compat_display_name())
            }
            // Keep the suggested type (e.g. `IIS1`) so assign/compat match without extra convert
            rett = suggested_type
        }
    }

    let target_class = class ?! exp.bug("Class initializer has no resolved class")
    let items_exp = parse_body_as_class_init_fields(body) !>
    let expanded = ClassInitItems {}
    append_class_init_item_range(expanded, items_exp, 0, 4294967295, scope, ctx) !>
    let elem_exps = expanded.elems
    let pair_exps = expanded.pairs
    let field_exps = expanded.fields

    let has_fields = field_exps.length > 0
    let has_elems = elem_exps.length > 0
    let has_pairs = pair_exps.length > 0
    if has_fields && (has_elems || has_pairs) {
        exp.error("Cannot mix property fields with list/map entries in the same class init")
    }
    if has_elems && has_pairs {
        exp.error("Cannot mix list entries and map entries (`=>`) in the same class init")
    }

    if target_class.is_generic_base {
        exp.error("Generic type '" + target_class.name + "' requires type arguments")
    }
    if target_class.kind == CLASS_KIND.interface {
        exp.error("Interfaces cannot be constructed directly")
    }
    if !isset(rett) {
        rett = target_class.get_type() !>
    } else if rett.get_non_nullable().is_shared {
        rett = target_class.get_type() !>
    }
    let init_class = target_class
    while init_class.kind == CLASS_KIND.mode {
        let for_class = init_class.mode_for_class
        if !isset(for_class) : exp.bug("Mode target is unresolved during class initialization")
        init_class = for_class
    }

    // Field-only (or empty) structural / default construct
    if !has_elems && !has_pairs {
        let fvals = HashMap[Prop, Value]{}
        each field_exps as fexp, field_i {
            let field_scope = expanded.field_scopes.get(field_i) ! exp.bug("Missing class-init field scope")
            let fname = fexp.str1
            let field_idf = field_scope.get_idf(fname)
            let prop: ?Prop = null
            if isset(field_idf) && field_idf.type == IDF.property {
                let loop_prop = field_idf.prop
                if !isset(loop_prop) : fexp.bug("Property idf is missing Prop")
                let loop_name = loop_prop.name
                if isset(loop_name) {
                    fname = loop_name
                    prop = init_class.get_prop_by_name(loop_name) !? null
                } else if loop_prop.class == init_class {
                    prop = loop_prop
                }
            }
            if !isset(prop) : prop = init_class.get_prop_by_name(fname) !? null
            if !isset(prop) {
                fexp.error("There's no property named: '" + fname + "' on '" + init_class.pretty_name() + "'")
            }
            if fvals.has(prop) {
                fexp.error("Property '" + (prop.name ?? fname) + "' is already set in this class initializer")
            }
            if prop.is_slice_data : fexp.error("Slice data is computed and cannot be initialized")
            act_check(fexp, prop.act, prop.exp, true, "property") !>
            require_sequence_metadata_write(prop, fexp, field_scope) !>
            let vexp = fexp.get(VALUE_EXP.left)
            if !isset(vexp) : fexp.bug("class_init field is missing value")
            let field_type = prop.type
            let fv = value_from_exp(vexp, field_scope, ctx.with_suggested_type(field_type).for_direct_init()) !>
            if fv.type != VAL.undefined {
                fv = value_try_convert(fv, field_scope, ctx, field_type) !>
                field_type.compat_check(fv.rett, vexp) !>
                if fv.is_implicit_borrow() {
                    vexp.error("An implicit pointer borrow cannot be stored in an object; use '@ref' explicitly")
                }
            }
            fvals.set(prop, fv)
        }

        return class_init_construct(init_class, rett, fvals, scope, ctx, exp) !>
    }

    // List / map init: construct empty, then $append / $set_key
    return value_from_class_init_hooks(target_class, rett, elem_exps, pair_exps, scope, ctx, exp) !>
}

// The core `slice &[T]` class specialized for one element type: the methods
// and the storage layout behind every bare `&[T]`.
fn bare_slice_class(b: Build, element: Type, at: Exp) Class !Error {
    let idf = valk_idf(b, "core", "&") !>
    let base = idf.class ?! at.bug("core '&' is not a class")
    return base.specialize(Array[Type]{ element }, at) !>
}

// A value typed as the `slice &[T]` class becomes the plain `&mut [T]` it is
fn value_retype_bare_slice(value: Value, rett: Type, at: Exp) Value {
    let retyped = vgen_cast(value, value.rett.is_nullable_type() ? rett.nullable() : rett)
    retyped.exp = at
    return retyped
}

// Zero-filled storage of `count` elements, owned by the result.
fn bare_slice_alloc(class: Class, rett: Type, count: Value, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let fields = HashMap[Prop, Value]{}
    let length_prop = class.get_prop_by_name("length") ! at.bug("Bare slice class has no 'length'")
    fields.set(length_prop, count)
    let init = class_init_construct(class, class.get_type() !>, fields, scope, ctx, at) !>
    return value_retype_bare_slice(init, rett, at)
}

// All-zero bytes are a valid value: numbers, and aggregates of them.
fn type_zero_is_valid(type: Type) bool {
    let t = type.get_non_nullable()
    if type.is_nullable_type() : return true
    return !t.has_gc_refs() && !t.is_pointer() && t.kind != TYPE.interface && t.kind != TYPE.closure
}

// `[T]{ a, b }` lists the elements, `[T]{ v x n }` repeats v n times,
// `[T]{ null x n }` leaves the storage zeroed and `[T]{ owner:, data:, length: }`
// describes existing storage. Every form is a fresh `&mut [T]`.
fn value_from_slice_init_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let type_exp = exp.get(VALUE_EXP.left) ?! exp.bug("slice_init is missing its element type")
    let body = exp.get(VALUE_EXP.right) ?! exp.bug("slice_init is missing its body")
    let element = type_from_exp(type_exp, scope) !>
    type_validate_sequence_element(element, type_exp) !>
    let class = bare_slice_class(b, element, exp) !>
    let class_type = class.get_type() !>
    let rett = element.ref_slice().mut_type()
    let uint_t = type_from_core(b, "uint") !>

    let items_exp, count_exp = parse_body_as_slice_init(body) !>
    let expanded = ClassInitItems {}
    append_class_init_item_range(expanded, items_exp, 0, 4294967295, scope, ctx) !>
    if expanded.pairs.length > 0 : exp.error("A slice has no map entries")
    let elems = expanded.elems
    let fields = expanded.fields

    if fields.length > 0 {
        if elems.length > 0 : exp.error("Cannot mix fields with list entries in a slice initializer")
        if isset(count_exp) : count_exp.error("A view over existing storage has no count; drop 'x n'")
        require_unsafe(exp, scope, "A view over existing storage") !>
        let fvals = HashMap[Prop, Value]{}
        each fields as fexp, i {
            let field_scope = expanded.field_scopes.get(i) ! exp.bug("Missing slice-init field scope")
            let name = fexp.str1
            if name != "owner" && name != "data" && name != "length" {
                fexp.error("A view is described by 'owner', 'data' and 'length', not '" + name + "'")
            }
            let prop = class.get_prop_by_name(name) ! fexp.bug("Bare slice class is missing '" + name + "'")
            if fvals.has(prop) : fexp.error("'" + name + "' is already set in this view")
            let vexp = fexp.get(VALUE_EXP.left) ?! fexp.bug("slice-init field is missing its value")
            let fv = value_from_exp(vexp, field_scope, ctx.with_suggested_type(prop.type).for_direct_init()) !>
            fv = value_try_convert(fv, field_scope, ctx, prop.type) !>
            prop.type.compat_check(fv.rett, vexp) !>
            fvals.set(prop, fv)
        }
        if fvals.length != 3 : exp.error("A view over existing storage needs 'owner', 'data' and 'length'")
        let view = vgen_class_init(ctx, class, fvals, class_type, exp) !>
        return value_retype_bare_slice(view, rett, exp)
    }

    if isset(count_exp) {
        let count = value_from_exp(count_exp, scope, ctx.with_suggested_type(uint_t)) !>
        count = value_try_convert(count, scope, ctx, uint_t) !>
        uint_t.compat_check(count.rett, count_exp) !>
        if elems.length > 1 {
            exp.error("'[T]{ v x n }' repeats one value; list the elements without 'x n' instead")
        }
        let elem_exp = elems.get(0) ! exp.bug("slice count without a value")
        let vexp = elem_exp.get(VALUE_EXP.left) ?! elem_exp.bug("element is missing its value")
        let fill: ?Value = null
        if vexp.type == EXP.value && vexp.value_kind == VALUE_KIND.null && !element.is_nullable_type() {
            // Zeroed storage: valid only when a zero slot is a valid element
            if !type_zero_is_valid(element) {
                require_unsafe(exp, scope, "Zeroed storage of '" + element.compat_display_name() + "' elements") !>
            }
        } else {
            let ev = value_from_exp(vexp, scope, ctx.with_suggested_type(element)) !>
            ev = value_try_convert(ev, scope, ctx, element) !>
            element.compat_check(ev.rett, vexp) !>
            // Fresh storage is already zero
            let is_zero = (ev.type == VAL.int && ev.int_val == 0) || (ev.type == VAL.float && ev.float_val == 0.0) || ev.type == VAL.null
            if !is_zero : fill = ev
        }
        if !isset(fill) : return bare_slice_alloc(class, rett, count, scope, ctx, exp) !>
        let fill_f = class.get_func("fill") ?! exp.bug("Bare slice class has no 'fill'")
        if !fill_f.parsed_types : parse_func_types(fill_f) !>
        let callee = vgen_func(fill_f)
        let args = Array[Value]{ count, fill }
        value_check_call_args(fill_f, callee, args, scope, ctx, exp) !>
        let call = vgen_call(ctx, callee, args, fill_f.get_rett(), scope)
        call.exp = exp
        return value_retype_bare_slice(call, rett, exp)
    }

    let storage = bare_slice_alloc(class, rett, vgen_int(elems.length.to(int), uint_t), scope, ctx, exp) !>
    if elems.length == 0 : return storage
    let write_f = class.get_func("write_init") ?! exp.bug("Bare slice class has no 'write_init'")
    if !write_f.parsed_types : parse_func_types(write_f) !>
    let setup = Array[Value]{}
    let rooted = value_bind_once(storage, ctx, exp, setup, true)
    each elems as eexp, i {
        let vexp = eexp.get(VALUE_EXP.left) ?! eexp.bug("element is missing its value")
        let ev = value_from_exp(vexp, scope, ctx.with_suggested_type(element)) !>
        ev = value_try_convert(ev, scope, ctx, element) !>
        element.compat_check(ev.rett, vexp) !>
        let receiver = vgen_cast(rooted, class_type)
        receiver.exp = exp
        let callee = vgen_func(write_f, receiver)
        let args = Array[Value]{ vgen_int(i.to(int), uint_t), ev }
        value_check_call_args(write_f, callee, args, scope, ctx, eexp) !>
        let call = vgen_call(ctx, callee, args, write_f.get_rett(), scope)
        call.exp = eexp
        setup.append(call)
    }
    return vgen_scoped_value(setup, rooted, exp)
}

// `Array[T]{ a, b }` / `Map[T]{ k => v }` — empty construct + hook calls.
fn value_from_class_init_hooks(class: Class, rett: Type, elem_exps: Array[Exp], pair_exps: Array[Exp], scope: Scope, ctx: Context, at: Exp) Value !Error {
    let construct = class_default_construct(class, rett, scope, ctx, at) !>
    let chain = class_init_hook_base(ctx, construct)

    let hooks = Array[Value]{}
    if elem_exps.length > 0 {
        let append_f = class.get_append_hook()
        if !isset(append_f) {
            at.error("Type '" + class.pretty_name() + "' has no '$append' hook (needed for list-style class init)")
        }
        if !append_f.parsed_types : parse_func_types(append_f) !>
        each elem_exps as eexp {
            let vexp = eexp.get(VALUE_EXP.left)
            if !isset(vexp) : eexp.bug("element missing value")
            let ect = ctx.without_suggested_type()
            let atype = class_hook_param_type(append_f, 0)
            if isset(atype) : ect = ctx.with_suggested_type(atype)
            let ev = value_from_exp(vexp, scope, ect) !>
            if isset(atype) && !ev.rett.is_void_type() {
                ev = value_try_convert(ev, scope, ctx, atype) !>
            }
            let callee = vgen_func(append_f, class_init_hook_receiver(chain))
            let args = Array[Value]{ ev }
            value_fill_trailing_defaults(append_f, call_receiver_skip(callee, append_f), args, scope, ctx, eexp) !>
            value_check_call_args(append_f, callee, args, scope, ctx, eexp) !>
            let call = vgen_call(ctx, callee, args, append_f.get_rett(), scope)
            call.exp = eexp
            hooks.append(call)
            // Chain when append returns SELF
            if class_init_hook_chains(chain, call, rett) : chain = call
        }
    } else {
        let set_f = class.get_set_key_hook()
        if !isset(set_f) {
            at.error("Type '" + class.pretty_name() + "' has no '$set_key' hook (needed for map-style class init)")
        }
        if !set_f.parsed_types : parse_func_types(set_f) !>
        each pair_exps as pexp {
            let kexp = pexp.get(VALUE_EXP.left)
            if !isset(kexp) : pexp.bug("pair missing key")
            let vexp = pexp.get(VALUE_EXP.right)
            if !isset(vexp) : pexp.bug("pair missing value")
            let kt = class_hook_param_type(set_f, 0)
            let vt = class_hook_param_type(set_f, 1)
            let kctx = ctx.without_suggested_type()
            let vctx = ctx.without_suggested_type()
            if isset(kt) : kctx = ctx.with_suggested_type(kt)
            if isset(vt) : vctx = ctx.with_suggested_type(vt)
            let kv = value_from_exp(kexp, scope, kctx) !>
            let vv = value_from_exp(vexp, scope, vctx) !>
            if isset(kt) && !kv.rett.is_void_type() : kv = value_try_convert(kv, scope, ctx, kt) !>
            if isset(vt) && !vv.rett.is_void_type() : vv = value_try_convert(vv, scope, ctx, vt) !>
            let callee = vgen_func(set_f, class_init_hook_receiver(chain))
            let args = Array[Value]{ kv, vv }
            value_fill_trailing_defaults(set_f, call_receiver_skip(callee, set_f), args, scope, ctx, pexp) !>
            value_check_call_args(set_f, callee, args, scope, ctx, pexp) !>
            let call = vgen_call(ctx, callee, args, set_f.get_rett(), scope)
            call.exp = pexp
            hooks.append(call)
            if class_init_hook_chains(chain, call, rett) : chain = call
        }
    }

    let v = Value {
        type: VAL.class_init
        value1: chain
        values: hooks
        rett: rett
        exp: at
    }
    v.set_class_ref(class)
    return v
}

fn class_init_construct(class: Class, rett: Type, fields: HashMap[Prop, Value], scope: Scope, ctx: Context, at: Exp) Value !Error {
    let missing = Array[String]{}
    each class.props as prop {
        if prop.is_slice_data || fields.has(prop) : continue
        let field_type = prop.type
        let default_body = prop.default
        if prop.zero_default && !isset(default_body) : continue
        let field: ?Value = null
        let default_at = prop.exp
        if isset(default_body) {
            let default_exp = parse_body_as_value_exp(default_body) !>
            let default_scope = prop.class.scope.sub_scope()
            default_scope.func = scope.func
            field = value_from_exp(default_exp, default_scope, ctx.with_suggested_type(field_type).for_direct_init()) !>
            default_at = default_exp
        } else if type_has_generated_default(field_type, ctx.build) {
            field = value_generate_default(field_type, scope, ctx, prop.exp) !>
        } else {
            let name = prop.name
            if isset(name) : missing.append(name)
            continue
        }
        let value = field ?! prop.exp.bug("Property default did not produce a value")
        if value.type == VAL.undefined : continue
        value = value_try_convert(value, scope, ctx, field_type) !>
        field_type.compat_check(value.rett, default_at) !>
        fields.set(prop, value)
    }
    if missing.length > 0 : class_init_missing_fields_error(at, class, missing) !>
    return vgen_class_init(ctx, class, fields, rett, at) !>
}

fn class_init_missing_fields_error(at: Exp, class: Class, missing: Array[String]) !Error {
    let fields = ""
    each missing as name, i {
        if i > 0 : fields += ", "
        fields += "'" + name + "'"
    }
    let noun = missing.length == 1 ? "field" : "fields"
    at.error("Missing " + noun + " in class initializer for '" + class.pretty_name() + "': " + fields + ". Provide " + (missing.length == 1 ? "it" : "them") + ", or declare a default on the property")
}

// Empty instance used as the base for list/map hooks.
fn class_default_construct(class: Class, rett: Type, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let init_class = class
    while init_class.kind == CLASS_KIND.mode {
        let for_class = init_class.mode_for_class
        if !isset(for_class) : at.bug("Mode target is unresolved during class initialization")
        init_class = for_class
    }
    return class_init_construct(init_class, rett, HashMap[Prop, Value]{}, scope, ctx, at) !>
}

// Param type of a method after implicit `this` (instance) or without (static).
fn class_hook_param_type(f: Func, i: uint) ?Type {
    let params = f.args
    let skip: uint = 0
    if isset(f.class) && !f.is_static : skip = 1 // this
    let pi = skip + i
    if pi >= params.length : return null
    let p = params.get(pi) !? null
    if !isset(p) : return null
    if p.is_generic : return null
    return p.type
}

// Bare `{ v0, v1, ... }` for fixed arrays — body re-parsed as element values.
fn value_from_array_init_body(exp: Exp, body: Exp, suggested_type: Type, scope: Scope, ctx: Context) Value !Error {
    let atype = suggested_type.get_non_nullable()
    let sub = atype.get_sub_type()
    if !isset(sub) : exp.bug("Array type is missing sub_type")
    let size = atype.length
    if size < 1 : exp.error("Array size is 0 (bug)")

    let elems_exp = parse_body_as_array_init_elements(body) !>
    let values = Array[Value]{}
    let last_start: uint = 0
    let last_len: uint = 0
    // `v...`: a single value fills the rest without being repeated per element
    let fill = false
    let i: uint = 0
    while true {
        let eexp = elems_exp.get(i)
        if !isset(eexp) : break
        if eexp.type == EXP.value && eexp.value_kind == VALUE_KIND.repeat {
            if last_len == 0 : eexp.error("No value to repeat")
            if last_len == 1 {
                fill = values.length < size
                i++
                continue
            }
            while values.length < size {
                let k = last_start
                while k < last_start + last_len {
                    if values.length == size : break
                    values.append(values.get(k) ! break)
                    k++
                }
            }
            i++
            continue
        }
        if fill || values.length >= size {
            eexp.error("Too many values")
        }
        let ev = value_from_exp(eexp, scope, ctx.with_suggested_type(sub).for_direct_init()) !>
        let unrolled = ev.unroll()
        last_start = values.length
        let appended: uint = 0
        each unrolled as item {
            if values.length >= size {
                eexp.error("Too many values")
            }
            if item.type != VAL.undefined {
                item = value_try_convert(item, scope, ctx, sub) !>
                let at = item.exp
                if isset(at) : sub.compat_check(item.rett, at) !>
                else : sub.compat_check(item.rett, eexp) !>
            }
            values.append(item)
            appended++
        }
        last_len = appended
        i++
    }
    if !fill && values.length < size {
        exp.error("Missing array values. Expected amount '" + size + "', but found '" + values.length + "'")
    }

    return vgen_array_init(ctx, values, suggested_type, exp, fill)
}

////////////////////////////
// Match
////////////////////////////

// Compare enum constants when their equality can be decided while compiling.
// -1 means unknown, keeping exhaustiveness conservative for values such as
// pointers whose identity is only known at runtime.
fn enum_match_values_equal(left: Value, right: Value) int {
    if left.type == VAL.int && right.type == VAL.int : return left.int_val == right.int_val ? 1 : 0
    if left.type == VAL.float && right.type == VAL.float : return left.float_val == right.float_val ? 1 : 0
    if left.type == VAL.bool && right.type == VAL.bool : return left.bool_val == right.bool_val ? 1 : 0
    if left.type == VAL.string && right.type == VAL.string : return left.get_string() == right.get_string() ? 1 : 0
    if left.type == VAL.null && right.type == VAL.null : return 1

    if left.type == VAL.array_init && right.type == VAL.array_init {
        let left_values = left.values
        let right_values = right.values
        if !isset(left_values) || !isset(right_values) : return -1
        if left.bool_val != right.bool_val : return -1
        if left_values.length != right_values.length : return 0
        each left_values as left_value, i {
            let right_value = right_values.get(i) !? null
            if !isset(right_value) : return -1
            let equal = enum_match_values_equal(left_value, right_value)
            if equal < 0 : return -1
            if equal == 0 : return 0
        }
        return 1
    }

    return -1
}

fn enum_match_is_exhaustive(enu: Enum, covered: Array[Value], ctx: Context, at: Exp) bool !Error {
    each enu.items as enum_item {
        let declared = value_from_enum_item(enu, enum_item.name, at, enu.scope, ctx) !>
        let found = false
        each covered as case_value {
            let equal = enum_match_values_equal(declared, case_value)
            if equal == 1 {
                found = true
                break
            }
        }
        if !found : return false
    }
    return enu.items.length > 0
}

// Lower `match on [: Type] { case => ... }`.
fn value_from_match_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let on_exp = exp.get(MATCH_EXP.on)
    if !isset(on_exp) : exp.bug("match is missing its input value")
    let body = exp.get(MATCH_EXP.body)
    if !isset(body) : exp.bug("match is missing its body")

    let on = value_from_exp(on_exp, scope, ctx.without_suggested_type()) !>
    let result_type_exp = exp.get(MATCH_EXP.type)
    let result_type: ?Type = null
    if isset(result_type_exp) : result_type = type_from_exp(result_type_exp, scope) !>

    let pos = body.pos
    if !isset(pos) : body.bug("match body is missing a position")
    let p = pos.copy().init_parser()
    let cases = Array[Value]{}
    let found_default = false
    let covered_union_members = Array[Type]{}
    let covered_union_null = false
    let union_type_before_null: ?Exp = null
    let union_type: ?Type = null
    let enum_type = on.rett.get_non_nullable().get_enum()
    let subject_nullable = on.rett.is_nullable_type() && !value_stores_non_null(on, scope)
    let covered_enum_values = Array[Value]{}
    let covered_null_case = false
    let on_non_nullable = on.rett.get_non_nullable()
    let error_type: ?ErrorType = on_non_nullable.kind == TYPE.error_code && !on_non_nullable.error_is_instance ? on_non_nullable.get_error_type() : null
    let covered_error_codes = Array[String]{}
    let covered_true = false
    let covered_false = false
    if on_non_nullable.kind == TYPE.union : union_type = on_non_nullable
    let all_cases_return = true

    while true {
        let t = p.tok(true, true, false) !>
        if t == TOK.end || (t == TOK.sign && p.word_is("}")) {
            if t != TOK.end : p.tok(true, true) !>
            break
        }
        if t == TOK.comment {
            p.tok(true, true) !>
            continue
        }
        if found_default {
            p.error("You cannot define cases after the 'default' case. You should move the 'default' case to the bottom of the list.")
        }

        let cond: ?Value = null
        let bound_name: ?String = null
        let bound_type: ?Type = null
        let is_default = t == TOK.word && p.word_is("default")
        if is_default {
            p.tok(true, true) !>
            found_default = true
        } else {
            let raw_item_exp: ?Exp = null
            let raw_item: ?Value = null
            if t == TOK.sign && (p.word_is("<") || p.word_is("[") || p.word_is("*") || p.word_is("&")) {
                let type_exp = read_type_exp(p, true, true) !>
                let item_type = type_from_exp(type_exp, scope) !>
                raw_item_exp = type_exp
                raw_item = vgen_type(item_type, ctx.build)
                raw_item.exp = type_exp
            } else {
                let value_exp = read_value_exp(p, true, false) !>
                let is_null_case = value_exp.type == EXP.value && value_exp.value_kind == VALUE_KIND.null
                let item_ctx = isset(union_type) && !is_null_case ? ctx.without_suggested_type() : ctx.with_suggested_type(on.rett)
                raw_item_exp = value_exp
                raw_item = value_from_exp(value_exp, scope, item_ctx) !>
            }
            let item_exp = raw_item_exp ?! exp.bug("Union match case is missing its expression")
            let item = raw_item ?! exp.bug("Union match case is missing its value")
            let is_null_case = item_exp.type == EXP.value && item_exp.value_kind == VALUE_KIND.null
            if isset(union_type) && is_null_case {
                // Covers the union's `null` alternative and, for a nullable subject, the missing value
                let null_tag = union_type.union_null_tag()
                if !subject_nullable && !isset(null_tag) {
                    item_exp.error("This tagged union has no 'null' alternative and cannot be null")
                }
                if covered_union_null : item_exp.error("Duplicate tagged-union null case")
                covered_union_null = true
                let bool_t = type_bool(ctx.build)
                if isset(null_tag) {
                    let members = union_type.get_members() ?! exp.bug("Tagged union is missing alternatives")
                    covered_union_members.append(members.get(null_tag) ! exp.bug("Tagged union null alternative is missing"))
                    cond = vgen_union_is(on, null_tag, item_exp)
                    if subject_nullable {
                        let missing = vgen_unary(vgen_isset_val(on, bool_t), VALUE_OP.not, bool_t)
                        cond = vgen_op(missing, cond, VALUE_OP.or, bool_t)
                    }
                } else {
                    cond = vgen_op(on, item, VALUE_OP.eq, bool_t)
                }
                cond.exp = item_exp
            } else if isset(union_type) && item.type == VAL.type {
                if on.rett.is_nullable_type() && !covered_union_null && !isset(union_type_before_null) {
                    union_type_before_null = item_exp
                }
                let member = item.get_of_type()
                if !isset(member) : item_exp.bug("Union match type is missing its represented type")
                let tag = union_type.union_member_index(member) !? null
                if !isset(tag) {
                    item_exp.error("Type '" + member.compat_display_name() + "' is not an alternative of '" + union_type.compat_display_name() + "'")
                }
                each covered_union_members as covered {
                    if type_identity_key(covered) == type_identity_key(member) {
                        item_exp.error("Duplicate tagged-union match case: " + member.compat_display_name())
                    }
                }
                covered_union_members.append(member)
                bound_type = member
                cond = vgen_union_is(on, tag, item_exp)

                let next = p.tok(true, true, false) !>
                if next == TOK.word && p.word_is("as") {
                    p.tok(true, true) !>
                    bound_name = p.read_word(true, true) !>
                }
            } else if isset(union_type) {
                item_exp.error("Tagged unions must be matched by type")
            } else if is_null_case && on.rett.is_nullable_type() {
                if covered_null_case : item_exp.error("Duplicate match case: null is already handled")
                covered_null_case = true
                cond = vgen_unary(vgen_isset_val(on, type_bool(ctx.build)), VALUE_OP.not, type_bool(ctx.build))
                cond.exp = item_exp
            } else {
                // A nullable subject matches a value only when it holds one
                let subject = on.rett.is_nullable_type() ? value_force_remove_nullable(on) : on
                item = value_try_convert(item, scope, ctx, subject.rett) !>
                subject.rett.compat_check(item.rett, item_exp) !>
                if subject.rett.is_bool_type() && item.type == VAL.bool {
                    if item.bool_val : covered_true = true
                    else : covered_false = true
                }
                if isset(enum_type) {
                    each covered_enum_values as covered {
                        let equal = enum_match_values_equal(covered, item)
                        if equal == 1 {
                            item_exp.error("Duplicate enum match case: this value is already handled")
                        }
                    }
                    covered_enum_values.append(item)
                }
                if isset(error_type) && item.type == VAL.error_value && item.get_string().length > 0 {
                    let code = item.get_string()
                    if covered_error_codes.contains(code) {
                        item_exp.error("Duplicate error code match case: '" + code + "' is already handled")
                    }
                    covered_error_codes.append(code)
                }
                cond = value_compare_values(subject, item, VALUE_OP.eq, scope, ctx, item_exp) !>
                if subject != on {
                    let bool_t = type_bool(ctx.build)
                    cond = vgen_op(vgen_isset_val(on, bool_t), cond, VALUE_OP.and, bool_t)
                }
                cond.exp = item_exp
            }
        }

        p.expect("=>", true, true) !>
        let case_scope = scope.sub_scope()
        let case_prefix = Array[Value]{}
        if isset(bound_name) && bound_name != "_" {
            let member = bound_type ?! exp.bug("Union match binding is missing its type")
            // A payload pulled out of a shared union keeps the shared view
            member = on.rett.storage_view_of(member)
            if case_scope.has_local(bound_name) : exp.error("Name already used: " + bound_name)
            let decl = Decl.new(ctx.get_func() !>, member, false, bound_name)
            decl.exp = exp
            decl.lock_scope = scope_find_lock(scope)
            decl.escape_origins = value_escape_origins(on)
            case_scope.add_decl(decl)
            case_scope.set_idf(bound_name, Idf.for_decl(decl))
            let extracted = vgen_union_extract(on, member, ctx, exp) !>
            case_prefix.append(vgen_assign(vgen_decl(decl), extracted, ASSIGN_OP.set, ctx.build, exp))
        }
        let casev = Value {
            type: VAL.block
            value1: cond
            bool_val: is_default
            rett: result_type ?? type_void(ctx.build)
            exp: exp
            values: case_prefix
        }

        let arm_tok = p.tok(true, true, false) !>
        let arm_exits = arm_tok == TOK.word && (p.word_is("throw") || p.word_is("return") || p.word_is("break") || p.word_is("continue"))
        if isset(result_type) && !arm_exits {
            let value_exp = read_value_exp(p, true, true) !>
            let value = value_from_exp(value_exp, case_scope, ctx.with_suggested_type(result_type)) !>
            if case_scope.did_return {
                // An arm that never yields, like `panic(...)`
                case_prefix.append(value)
            } else {
                // A match used as a value produces one; it does not end the path.
                all_cases_return = false
                value = value_try_convert(value, case_scope, ctx, result_type) !>
                result_type.compat_check(value.rett, value_exp) !>
                casev.value2 = value
            }
        } else {
            let stmt_exp = read_func_stmt_exp(p) !>
            let state = ParseState.new()
            let value = body_value_from_exp(ctx, case_scope, state, stmt_exp) !>
            if isset(value) : case_prefix.append(value)
            if !case_scope.did_return : all_cases_return = false
        }
        scope.absorb_removed(case_scope)
        cases.append(casev)
    }

    if cases.length == 0 {
        exp.error("The match statement must have atleast 1 case")
    }
    if !found_default && covered_union_null && isset(union_type_before_null) {
        let null_order = union_type_before_null ?! exp.bug("Union null-order expression is missing")
        null_order.error("The null case must come before type cases when matching a nullable tagged union")
    }
    if !found_default {
        let exhaustive = false
        if isset(union_type) {
            let union_members = union_type.get_members()
            if isset(union_members) {
                exhaustive = covered_union_members.length == union_members.length
                    && (!subject_nullable || covered_union_null)
            }
        } else if isset(enum_type) {
            exhaustive = (enum_match_is_exhaustive(enum_type, covered_enum_values, ctx, exp) !>)
                && (!subject_nullable || covered_null_case)
        } else if isset(error_type) {
            exhaustive = !subject_nullable || covered_null_case
            each error_type.codes as code {
                if !covered_error_codes.contains(code) : exhaustive = false
            }
        } else if on_non_nullable.is_bool_type() {
            exhaustive = covered_true && covered_false && (!subject_nullable || covered_null_case)
        }
        if !exhaustive {
            exp.error("The match statement must have a 'default' case because it does not cover all values")
        }
        if isset(enum_type) {
            let invalid = value_panic_call(ctx, "Invalid enum value in exhaustive match", exp) !>
            cases.append(Value {
                type: VAL.block
                bool_val: true
                rett: result_type ?? type_void(ctx.build)
                value2: isset(result_type) ? invalid : null
                values: isset(result_type) ? Array[Value]{} : Array[Value]{ invalid }
                exp: exp
            })
        } else {
            let last = cases.get(cases.length - 1) !? null
            if isset(last) : last.bool_val = true
        }
    }
    if all_cases_return : scope.did_return = true

    return Value {
        type: VAL.match
        value1: on
        values: cases
        rett: result_type ?? type_void(ctx.build)
        exp: exp
    }
}

// Class/struct usable for bare `{ fields }` from the suggested type.
fn class_from_inline_init_type(t: Type) ?Class {
    let nn = t.get_non_nullable()
    if nn.is_inline {
        return nn.class
    }
    // Direct structural type (less common for bare init; named form usually used)
    let c = nn.get_class_or_null()
    if !isset(c) : return null
    if c.kind == CLASS_KIND.struct || c.kind == CLASS_KIND.class : return c
    return null
}

// ---- helpers ----

////////////////////////////
// Literals and identifiers
////////////////////////////

fn value_from_int_literal(exp: Exp, b: Build) Value !Error {
    let text = exp.str1.replace("_", "")
    let n: int = 0
    if exp.value_kind == VALUE_KIND.hex {
        let un = text.hex_to_uint() ! exp.error("Invalid hex integer: " + exp.str1)
        n = un.to(int)
    } else if exp.value_kind == VALUE_KIND.octal {
        // Source form is 0c… — strip leading 0c/0C if present for octal_to_int
        let body = text
        if body.starts_with("0c") || body.starts_with("0C") {
            body = body.part(2, body.length - 2)
        }
        let un = body.octal_to_uint() ! exp.error("Invalid octal integer: " + exp.str1)
        n = un.to(int)
    } else {
        let un = text.to_uint() ! exp.error("Invalid integer: " + exp.str1)
        n = un.to(int)
    }
    // A literal past the signed range is unsigned, whatever its radix
    if n < 0 : return vgen_int(n, type_from_core(b, "uint") !>)
    return vgen_int(n, type_int(b))
}

fn value_from_float_literal(exp: Exp, b: Build) Value !Error {
    let text = exp.str1.replace("_", "")
    let n = text.to_float() ! exp.error("Invalid float: " + exp.str1)
    return vgen_float(n, type_float(b) !>)
}

fn value_from_id_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let id = exp.get(VALUE_EXP.left)
    if !isset(id) : exp.bug("Missing id sub-expression")
    let name = id.str1
    let ns_name = id.str2
    let b = ctx.build
    // Optional `Id[T, U]` type arguments (VALUE_EXP.right multi TYPE)
    let gens = exp.get(VALUE_EXP.right)

    if !isset(ns_name) && (name == "__PATH__" || name == "__FILE__" || name == "__DIR__" || name == "__LINE__") {
        if isset(gens) : exp.error("Unexpected type arguments on: " + name)
        if name == "__LINE__" {
            let at = message_resolve(exp.src, exp.i)
            let line = at.src.line_of(at.i).to(uint)
            let v = vgen_int(line.to(int), type_from_core(b, "uint") !>)
            v.exp = exp
            return v
        }
        let path = exp.src.path
        if !isset(path) {
            exp.error("Cannot resolve " + name + " for source without a file path")
        }
        let value = path
        if name == "__DIR__" {
            value = fs.dir_of(path)
        } else if name == "__FILE__" {
            value = fs.basename(path)
            let ns = exp.src.ns
            if isset(ns) {
                let srcdir = ns.pkg.srcdir
                if isset(srcdir) {
                    let prefix = srcdir + fs.PATH_DIV
                    if path.starts_with(prefix) {
                        value = path.part(prefix.length, path.length - prefix.length)
                    }
                }
            }
        }
        let v = vgen_string(value, type_string(b))
        v.exp = exp
        return v
    }

    let idf: ?Idf = null
    if isset(ns_name) {
        let ns_idf = scope.get_scope_idf(ns_name)
        if !isset(ns_idf) : exp.error("Unknown namespace: " + ns_name)
        let ns_scope = ns_idf.scope
        if !isset(ns_scope) : exp.bug("Scope idf is missing scope")
        lsp_complete_scope(exp, ns_scope) !>
        idf = ns_scope.get_idf(name)
        if !isset(idf) : exp.error("Unknown identifier: " + ns_name + "." + name)
    } else {
        lsp_complete_scope(exp, scope) !>
        idf = scope.get_idf(name)
        // Unresolved ids are allowed here (e.g. other value aliases); later stages resolve
    }

    // LSP: jump to whatever the compiler just decided this name refers to
    if isset(idf) : lsp_goto_idf(exp, idf) !>

    // Locals / arguments → VAL.decl with known type
    if isset(idf) && idf.type == IDF.decl {
        if isset(gens) : exp.error("Unexpected type arguments on variable: " + name)
        let d = idf.decl
        if !isset(d) : exp.bug("Decl idf is missing decl")
        // Reads after a publish are checked path-sensitively at the end
        if !ctx.is_store_target : decl_invalidate_unique_dependents(d)
        d.is_used = true
        if !ctx.is_store_target : d.is_read = true
        let current = ctx.func
        if !isset(current) {
            exp.error("A declaration cannot be evaluated outside a function")
        }
        let resolved = func_capture_decl(current, d, name, exp) !>
        resolved.is_used = true
        if !ctx.is_store_target : resolved.is_read = true
        let v = vgen_decl(resolved)
        v.exp = exp
        return v
    }

    // Globals / shared → VAL.global (assignable, typed)
    if isset(idf) && idf.type == IDF.global {
        if isset(gens) : exp.error("Unexpected type arguments on global: " + name)
        let g = idf.global
        if !isset(g) : exp.bug("Global idf is missing global")
        // Stores are checked at the assignment site, where is_assign is known
        act_check(exp, g.act, g.exp, false, "global") !>
        // Known before IR generation: global defaults are ordered by these reads
        let reader = ctx.func
        if isset(reader) : reader.globals_used.append(g, true)
        let v = vgen_global(g) !>
        v.exp = exp
        return v
    }

    // Compile-time class-property metadata from `#loop properties`.
    if isset(idf) && idf.type == IDF.property {
        if isset(gens) : exp.error("Unexpected type arguments on property metadata: " + name)
        let prop = idf.prop
        if !isset(prop) : exp.bug("Property idf is missing Prop")
        let runtime_value = idf.value
        let v = Value {
            type: VAL.property_meta
            rett: isset(runtime_value) ? prop.type : type_string(b)
            prop: prop
            value1: runtime_value
            exp: exp
        }
        v.set_string(prop.name ?? "")
        return v
    }

    // Compile-time constants (macro defs on Build.scope) fold to their Value
    if isset(idf) && idf.type == IDF.value {
        if isset(gens) : exp.error("Unexpected type arguments on value: " + name)
        let bound = idf.value
        if !isset(bound) : exp.bug("Value idf is missing value")
        let bound_type = bound.type == VAL.string
            ? type_string(b)
            : (bound.type == VAL.int ? type_int(b) : bound.rett)
        // Return a shallow copy so we can attach this use's source span
        let v = Value {
            type: bound.type
            rett: bound_type
            exp: exp
            int_val: bound.int_val
            float_val: bound.float_val
            bool_val: bound.bool_val
            decl: bound.decl
        }
        v.set_string(bound.get_string())
        v.set_name(bound.get_name())
        v.set_of_type(bound.get_of_type())
        return v
    }

    // `value Name (expr)` — evaluate the stored expression in this scope
    if isset(idf) && idf.type == IDF.value_expr {
        if isset(gens) : exp.error("Unexpected type arguments on value: " + name)
        let value_exp = idf.value_exp
        if !isset(value_exp) : exp.bug("Value-expr idf is missing value_exp")
        let value_scope = idf.scope ?? scope
        if idf.resolving : exp.error("Circular value definition: " + name)
        idf.resolving = true
        defer idf_resolved(idf)
        // Keep the suggested type type so e.g. a null alias can inherit the use-site expectation
        let v = value_from_exp(value_exp, value_scope, ctx) !>
        let suggested_type = ctx.suggested_type
        if isset(suggested_type) {
            v = value_try_convert(v, scope, ctx, suggested_type) !>
        }
        v.exp = exp
        return v
    }

    if isset(idf) && idf.type == IDF.type {
        let of_type = idf.of_type
        if !isset(of_type) : exp.bug("Type idf is missing of_type")
        // If the bound type is a generic class, allow `T[U]` only when of_type is a class base
        let c = of_type.class
        if isset(gens) {
            if isset(c) && c.is_generic_base {
                let gtypes = value_resolve_type_args(gens, scope) !>
                c = c.specialize(gtypes, exp) !>
                of_type = c.get_type() !>
            } else {
                exp.error("Type is not generic: " + name)
            }
        }
        let v = vgen_type(of_type, b)
        v.exp = exp
        return v
    }
    if isset(idf) && idf.type == IDF.class {
        let c = idf.class
        if isset(c) {
            act_check(exp, c.act, c.exp, false, "class") !>
            if c.is_generic_base {
                if isset(gens) {
                    // Legacy: type args still attached on id (if any)
                    let gtypes = value_resolve_type_args(gens, scope) !>
                    c = c.specialize(gtypes, exp) !>
                } else {
                    let v = Value {
                        type: VAL.type
                        rett: type_string(b)
                    }
                    v.set_class_ref(c)
                    v.set_name(name)
                    v.exp = exp
                    return v
                }
            } else if isset(gens) {
                exp.error("Type is not generic: " + c.name)
            }
            let of_type = c.get_type() !>
            let v = vgen_type(of_type, b)
            v.exp = exp
            return v
        }
    }

    // Enum type as value: `Enum1` / `Enum2` — `.item` resolves via of_type.enum
    if isset(idf) && idf.type == IDF.enum {
        if isset(gens) : exp.error("Unexpected type arguments on enum: " + name)
        let enu = idf.enum
        if !isset(enu) : exp.bug("Enum idf is missing enum")
        act_check(exp, enu.act, enu.exp, false, "enum") !>
        let of_type = enu.get_type() !>
        let v = vgen_type(of_type, b)
        v.exp = exp
        return v
    }

    // Error type as value: `TestError` — `.code` resolves via of_type.error_type
    if isset(idf) && idf.type == IDF.error_type {
        if isset(gens) : exp.error("Unexpected type arguments on error type: " + name)
        let et = idf.error_type
        if !isset(et) : exp.bug("Error-type idf is missing error_type")
        act_check(exp, et.act, et.exp, false, "error type") !>
        parse_error_type(et) !>
        let v = vgen_type(type_error_code(et), b)
        v.exp = exp
        return v
    }

    // Generic free function: `foo[T]` → specialized Func id
    if isset(idf) && idf.type == IDF.func {
        let f = idf.func
        if isset(f) && f.is_generic_base {
            if !isset(gens) {
                // Leave base for call-site specialization when possible
            } else {
                let gtypes = value_resolve_type_args(gens, scope) !>
                f = f.specialize(gtypes, exp) !>
                idf = Idf.for_func(f)
            }
        } else if isset(gens) {
            exp.error("Function is not generic: " + name)
        }
    } else if isset(gens) {
        exp.error("Unexpected type arguments on: " + name)
    }

    if isset(idf) && idf.type == IDF.func {
        let f = idf.func
        if !isset(f) : exp.bug("Function idf is missing func")
        // Specializations carry the base's act and Exp, so this covers `foo[T]` too
        act_check(exp, f.act, f.exp, false, "function") !>
        let v = vgen_func(f)
        v.exp = exp
        return v
    }
    exp.error("Unknown identifier: " + name)
}

// Resolve multi TYPE_KIND type-argument list from a value id's `[T, U]`.
fn value_resolve_type_args(gens: Exp, scope: Scope) Array[Type] !Error {
    let gtypes = Array[Type]{}
    let i: uint = 0
    while true {
        let g = gens.get(i)
        if !isset(g) : break
        gtypes.append(type_from_exp(g, scope) !>)
        i++
    }
    return gtypes
}

// VAL.type for a value's result type (`typeof(v)` / `v.$type`).
fn value_as_rett_type(on: Value, at: Exp, b: Build) Value !Error {
    return vgen_type(on.rett, b)
}

////////////////////////////
// Member access
////////////////////////////

// `left.name` — namespace lookup, type meta, static members, instance methods/getters, or object props.
fn value_from_dot_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let b = ctx.build
    let on_exp = exp.get(VALUE_EXP.left)
    if !isset(on_exp) : exp.bug("Missing dot left value")

    // A scope imported with `use` uses the same dot syntax as member access.
    // Resolve that case before evaluating the left side as a runtime value.
    if on_exp.type == EXP.value && on_exp.value_kind == VALUE_KIND.id {
        let base_id = on_exp.get(VALUE_EXP.left)
        let base_idf = isset(base_id) && !isset(base_id.str2) ? scope.get_idf(base_id.str1) : null
        let namespace_idf = isset(base_id) && !isset(base_id.str2) ? scope.find_scope_idf(base_id.str1) : null
        let namespace_scope = isset(namespace_idf) ? namespace_idf.scope : null
        let namespace_has_name = isset(namespace_scope) && isset(namespace_scope.get_idf(exp.str1))
        if isset(base_id) && ((isset(base_idf) && base_idf.type == IDF.scope) || namespace_has_name) {
            let id = Exp {
                type: EXP.id
                str1: exp.str1
                str2: base_id.str1
                src: exp.src
                i: exp.i
                len: exp.len
            }
            let qualified = Exp {
                type: EXP.value
                kind: VALUE_KIND.id
                src: exp.src
                i: exp.i
                len: exp.len
            }
            qualified.set(VALUE_EXP.left, id)
            return value_from_id_exp(qualified, scope, ctx) !>
        }
    }

    let on = value_from_exp(on_exp, scope, ctx.for_group_receiver()) !>
    let name = exp.str1

    if on.type == VAL.property_meta {
        let prop = on.prop
        if !isset(prop) : exp.bug("Property metadata value is missing Prop")
        if name == "$type" {
            let v = vgen_type(prop.type, b)
            v.exp = exp
            return v
        }
        if name == "name" {
            let v = vgen_string(prop.name ?? "", type_string(b))
            v.exp = exp
            return v
        }
        if name == "$default_value" {
            let body = prop.default
            if isset(body) {
                let default_exp = parse_body_as_value_exp(body) !>
                let default_scope = prop.class.scope.sub_scope()
                default_scope.func = scope.func
                let v = value_from_exp(default_exp, default_scope, ctx.with_suggested_type(prop.type).for_direct_init()) !>
                v = value_try_convert(v, scope, ctx, prop.type) !>
                prop.type.compat_check(v.rett, default_exp) !>
                v.exp = exp
                return v
            }
            if prop.zero_default {
                let v = vgen_null(prop.type)
                v.exp = exp
                return v
            }
            return value_generate_default(prop.type, scope, ctx, exp) !>
        }
        if name == "$has_default_value" {
            let has_default = isset(prop.default) || prop.zero_default || type_has_generated_default(prop.type, b)
            let v = vgen_bool(has_default, type_bool(b))
            v.exp = exp
            return v
        }
        if name == "$has_explicit_default_value" {
            let v = vgen_bool(isset(prop.default), type_bool(b))
            v.exp = exp
            return v
        }
        if name == "$index" {
            let index: uint = 0
            each prop.class.props as item, i {
                if item == prop {
                    index = i
                    break
                }
            }
            let v = vgen_int(index.to(int), type_int(b))
            v.exp = exp
            return v
        }
        if name == "name" || name == "type" || name == "default" {
            exp.error("Property loop values use '$type', '$default_value', '$has_default_value', and '$has_explicit_default_value'; plain '." + name + "' is not supported")
        }
    }

    let selected_group = on.get_group()
    if isset(selected_group) {
        lsp_complete_group(exp, selected_group) !>
        let f = selected_group.funcs.get(name) !? null
        if isset(f) {
            act_check(exp, f.act, f.exp, false, "function") !>
            lsp_goto(exp, f.exp) !>
            let receiver: ?Value = null
            if !f.is_static {
                receiver = on.value1
                if !isset(receiver) : exp.bug("Instance group member is missing its receiver")
                f = func_for_receiver(f, receiver, exp) !>
            }
            if f.is_getter {
                let callee = vgen_func(f, receiver)
                callee.exp = exp
                let args = Array[Value]{}
                value_check_call_args(f, callee, args, scope, ctx, exp) !>
                let call = vgen_call(ctx, callee, args, f.get_rett(), scope)
                call.exp = exp
                if f.can_error {
                    call.can_error = true
                    call.set_error_type(f.error_type)
                }
                return call
            }
            return value_bind_method(f, receiver, ctx, exp)
        }
    }

    // Any value: .$type → VAL.type of its result type (same as typeof)
    if name == "$type" {
        let v = value_as_rett_type(on, exp, b) !>
        v.exp = exp
        return v
    }

    if name == "$default_value" {
        return value_from_default_value(on, scope, ctx, exp) !>
    }

    // Trusted library code can allocate a zeroed aggregate and finish its
    // initialization before publishing it. This is intentionally restricted
    // to files that already opt into unchecked access.
    if name == "$zero_value" {
        if !exp.src.ignore_access {
            exp.error("'$zero_value' is only available in an @ignore_access source")
        }
        let zero_type = value_default_target_type(on, exp) !>
        let zero_class = zero_type.get_non_nullable().get_class_or_null()
        if !isset(zero_class) || !zero_type.get_non_nullable().is_structural_type() {
            exp.error("'$zero_value' requires a structural type")
        }
        return vgen_class_init(ctx, zero_class, HashMap[Prop, Value]{}, zero_type, exp) !>
    }

    if name == "$is_shared_global" {
        if on.type != VAL.global {
            exp.error("The '$is_shared_global' token can only be used on global values")
        }
        let global = on.global
        if !isset(global) : exp.bug("VAL.global is missing its Global")
        let v = vgen_bool(global.shared, type_bool(b))
        v.exp = exp
        return v
    }

    if name == "$has_default_value" {
        let default_type = value_default_target_type(on, exp) !>
        let has_default = type_has_generated_default(default_type, b)
        if on.type == VAL.global {
            let global = on.global
            if !isset(global) : exp.bug("VAL.global is missing its Global")
            if isset(global.default_exp) : has_default = true
        }
        let v = vgen_bool(has_default, type_bool(b))
        v.exp = exp
        return v
    }

    if name == "$has_explicit_default_value" {
        let explicit = false
        if on.type == VAL.global {
            let global = on.global
            if !isset(global) : exp.bug("VAL.global is missing its Global")
            explicit = isset(global.default_exp)
        }
        let v = vgen_bool(explicit, type_bool(b))
        v.exp = exp
        return v
    }

    // Compile-time type meta-properties + static members / enum items
    if on.type == VAL.type {
        let folded = value_type_prop(on, name, exp, b) !>
        if isset(folded) {
            folded.exp = exp
            return folded
        }
        if name.starts_with("$") {
            let v = vgen_dot(on, name, type_bool(b))
            v.exp = exp
            return v
        }
        let t = on.get_of_type()
        if isset(t) {
            // Enum member: `Enum1.a`
            let enu = t.get_enum()
            if isset(enu) {
                lsp_complete_enum(exp, enu) !>
                return value_from_enum_item(enu, name, exp, scope, ctx) !>
            }
            // Error code constant: `TestError.err2`
            let et = t.get_error_type()
            if isset(et) {
                lsp_complete_error_codes(exp, et) !>
                return value_from_error_code_item(et, name, exp) !>
            }
            // Static method / member on a class type (modes fall through to wrapped type)
            let c = t.get_class_or_null()
            if isset(c) {
                lsp_complete_class(exp, c) !>
                let f = c.get_func(name)
                if isset(f) {
                    if c.kind == CLASS_KIND.interface && !f.is_static {
                        exp.error("Interface methods require an interface value")
                    }
                    act_check(exp, f.act, f.exp, false, "function") !>
                    lsp_goto(exp, f.exp) !>
                    let v = vgen_func(f)
                    v.exp = exp
                    return v
                }
                let g = c.get_group(name)
                if isset(g) {
                    let v = vgen_dot(on, name, on.rett)
                    v.set_group(g)
                    v.exp = exp
                    return v
                }
            }
        }
        let v = vgen_dot(on, name, type_void(b))
        v.exp = exp
        return v
    }

    // Soft: unresolved left — keep as open dot
    if on.rett.is_void_type() {
        let v = vgen_dot(on, name, type_void(b))
        v.exp = exp
        return v
    }

    let on_nn = on
    if !value_is_nullable_union_receiver(on) : on_nn = value_try_remove_nullable(on, scope, ctx) !>
    if on_nn.rett.is_nullable_type() && !value_is_nullable_union_receiver(on_nn) {
        exp.error("You cannot access properties/functions on a nullable type: " + on_nn.rett.compat_display_name())
    }
    on_nn = value_read_borrowed_receiver(on_nn, scope, ctx, exp) !>
    if on_nn.rett.is_nullable_type() && !value_is_nullable_union_receiver(on_nn) {
        exp.error("You cannot access properties/functions on a nullable type: " + on_nn.rett.compat_display_name())
    }
    if on_nn.rett.kind == TYPE.ref_slice && !isset(on_nn.rett.class) {
        if name == "length" {
            let length = value_owned_borrow_length(value_root_owned_borrow(on_nn, ctx), ctx) !>
            length.exp = exp
            return length
        }
        // Everything else is declared by core's `slice &[T]` over the same
        // three words; a read-only borrow keeps the methods that write out of reach
        let element = on_nn.rett.get_sub_type() ?! exp.bug("Slice borrow has no element type")
        let slice_class = bare_slice_class(b, element, exp) !>
        let as_slice = vgen_cast(on_nn, slice_class.get_type() !>)
        as_slice.exp = on_nn.exp
        on_nn = as_slice
    }
    // Members of a borrow are the members of T. Properties are read through
    // the address; a method keeps the whole borrow as its receiver so a store
    // through `this` still knows the owner.
    let on_adr = value_through_borrow(on_nn, ctx)

    if name == "$is_shared" {
        return value_from_is_shared(on_nn, exp, b) !>
    }

    let on_et_t = on_nn.rett
    if on_et_t.kind == TYPE.error_code && on_et_t.error_is_instance {
        let et = on_et_t.get_error_type()
        if !isset(et) : exp.bug("Error-instance type is missing error_type")
        // LSP: `E.` offers the payload fields and code constants
        lsp_complete_error_codes(exp, et) !>
        if name == "code" {
            if on_nn.type != VAL.decl : exp.bug("Caught error code is not backed by a declaration")
            let decl = on_nn.decl
            if !isset(decl) : exp.bug("Caught error code declaration is missing")
            let v = vgen_decl(decl)
            v.rett = type_error_code(et)
            v.exp = exp
            return v
        }
        let ptype = et.payload.get(name) !? null
        if isset(ptype) {
            let caught_decl = on_nn.decl
            if isset(caught_decl) {
                let payloads = caught_decl.error_payloads
                if isset(payloads) {
                    let payload_decl = payloads.get(name) !? null
                    if isset(payload_decl) {
                        let v = vgen_decl(payload_decl)
                        v.rett = ptype
                        v.exp = exp
                        return v
                    }
                }
            }
            let global = et.payload_globals.get(name) !? null
            if !isset(global) : exp.bug("Error payload global is missing: " + et.name + "." + name)
            let v = vgen_global(global) !>
            v.rett = ptype
            v.exp = exp
            return v
        }
        if et.codes.contains(name) {
            return value_from_error_code_item(et, name, exp) !>
        }
        exp.error("Error type '" + et.name + "' has no payload field or code named: '" + name + "'")
    }

    let on_class = value_member_class(on_nn.rett)
    if isset(on_class) {
        // LSP: members of the value on the left of the dot
        lsp_complete_class(exp, on_class) !>

        let current_func = ctx.func
        let current_group: ?Group = isset(current_func) ? current_func.group : null
        if isset(current_func) && isset(current_group) && on_nn.type == VAL.decl {
            let receiver_arg = current_func.args.get(0) !? null
            if isset(receiver_arg) && on_nn.decl == receiver_arg.decl {
                let group_func = current_group.funcs.get(name) !? null
                if isset(group_func) {
                    group_func = func_for_receiver(group_func, on_nn, exp) !>
                    if group_func.is_getter {
                        let callee = vgen_func(group_func, on_nn)
                        callee.exp = exp
                        let args = Array[Value]{}
                        value_check_call_args(group_func, callee, args, scope, ctx, exp) !>
                        let call = vgen_call(ctx, callee, args, group_func.get_rett(), scope)
                        call.exp = exp
                        if group_func.can_error {
                            call.can_error = true
                            call.set_error_type(group_func.error_type)
                        }
                        return call
                    }
                    return value_bind_method(group_func, on_nn, ctx, exp)
                }
            }
        }

        // Methods / getters (modes fall through to `mode_for_class`)
        let f = on_class.get_func(name)
        if isset(f) {
            f = func_for_receiver(f, on_nn, exp) !>
            act_check(exp, f.act, f.exp, false, "function") !>
            lsp_goto(exp, f.exp) !>
            if f.is_getter {
                // Getter call with implicit this (no arg list in source)
                let callee = vgen_func(f, on_nn)
                callee.exp = exp
                let args = Array[Value]{}
                value_check_call_args(f, callee, args, scope, ctx, exp) !>
                let call = vgen_call(ctx, callee, args, f.get_rett(), scope)
                call.exp = exp
                if f.can_error {
                    call.can_error = true
                    call.set_error_type(f.error_type)
                }
                return call
            }
            return value_bind_method(f, on_nn, ctx, exp)
        }

        // Properties — assignable VAL.prop (modes fall through to wrapped props)
        let prop = on_class.get_prop_by_name(name) !? null
        if isset(prop) {
            // Read only: a store through this prop is checked at the assignment site
            act_check(exp, prop.act, prop.exp, false, "property") !>
            lsp_goto(exp, prop.exp) !>
            let v = vgen_prop(prop, on_adr, ctx)
            v.rett = on_adr.rett.storage_view_of(prop.type)
            v.exp = exp
            return value_check_borrowed_property(v, ctx, exp) !>
        }

        // Groups: leave as open dot for now (group method calls later)
        let g = on_class.get_group(name)
        if isset(g) {
            let v = vgen_dot(on_nn, name, on_nn.rett)
            v.set_group(g)
            v.exp = exp
            return v
        }
    }

    if name == "length" && type_has_native_sequence_length(on_adr.rett) {
        let length = value_native_sequence_length(on_adr, on_adr.rett, ctx, exp) !>
        if isset(length) {
            length.exp = exp
            return length
        }
    }

    if name == "to" || name == "$to" {
        let v = vgen_dot(on_nn, name, type_void(b))
        v.exp = exp
        return v
    }

    if name == "clone" && on_nn.rett.get_non_nullable().kind == TYPE.closure {
        let v = vgen_dot(on_nn, name, on_nn.rett)
        v.exp = exp
        return v
    }

    if !isset(on_class) {
        exp.error("Cannot access properties/functions on this type: " + on_nn.rett.compat_display_name())
    }
    exp.error("Type '" + on_class.pretty_name() + "' has no property or method named: '" + name + "'")
}

// Whether this storage lives in an element that was copied out of an Array
fn value_is_copied_element(value: Value) bool {
    let meta = value.meta
    if isset(meta) && meta.copied_element : return true
    let kind = value.type
    if kind != VAL.prop && kind != VAL.ptrv && kind != VAL.wrap && kind != VAL.cast : return false
    let inner = value.value1 ?! return false
    if kind == VAL.prop && !inner.rett.is_inline_type() : return false
    return value_is_copied_element(inner)
}

// The `each` variable this storage was copied into, if any
fn value_copied_element_decl(value: Value) ?Decl {
    let kind = value.type
    if kind == VAL.decl {
        let decl = value.decl ?! return null
        return decl.copied_element ? decl : null
    }
    if kind != VAL.prop && kind != VAL.ptrv && kind != VAL.wrap && kind != VAL.cast : return null
    let inner = value.value1 ?! return null
    return value_copied_element_decl(inner)
}

// The captured variable whose private copy this storage is, if any: the
// variable itself or inline storage (struct, fixed array) inside it. An
// object read out of a captured reference is not the capture.
fn value_captured_decl(value: Value) ?Decl {
    let kind = value.type
    if kind == VAL.decl {
        let decl = value.decl ?! return null
        return decl.is_bind ? decl : null
    }
    if kind != VAL.prop && kind != VAL.ptrv && kind != VAL.wrap && kind != VAL.cast && kind != VAL.ptr_of : return null
    let inner = value.value1 ?! return null
    if !inner.rett.is_inline_type() || inner.rett.is_fat_borrow() : return null
    return value_captured_decl(inner)
}

fn value_check_captured_write(value: Value, at: Exp, what: String ("assign to")) !Error {
    let captured = value_captured_decl(value) ?! return
    at.error("Cannot " + what + " captured variable '" + (captured.name ?? "?") + "': closures capture values, not variables; mutate through an object instead")
}

// A writable borrow of a capture's inline storage would write the private copy
fn value_check_captured_borrow(value: Value, at: Exp) !Error {
    if value.rett.is_fat_borrow() : return
    value_check_captured_write(value, at, "take a writable borrow of") !>
}

// A type whose storage may not be written from here: a read-only borrow, or
// an `$immutable` slice outside the class that declares it
fn type_is_readonly_here(type: Type, ctx: Context) bool {
    let nn = type.get_non_nullable()
    if !nn.is_readonly_view() : return false
    let c = nn.class ?! return true
    let func = ctx.func ?? (ctx.get_func() !? null)
    if !isset(func) : return true
    // Only the declaring class itself may write; a mode or an extension from
    // another package is not the class
    let fc = func.class ?! return true
    let rep = c.representation_class()
    if fc.representation_class() != rep : return true
    let src_ns = func.exp.src.ns ?! return true
    return src_ns.pkg != rep.ns.pkg
}

// A property holding inline storage that is read-only from where it is used.
// An object or raw pointer read out of the property is not the property.
fn value_is_readonly_property(value: Value) bool {
    if value.type != VAL.prop : return false
    let prop = value.prop ?! return false
    let at = value.exp ?! return false
    let stored = prop.type.get_non_nullable()
    if stored.is_gc_type() || stored.kind == TYPE.interface || stored.kind == TYPE.closure
        || stored.is_raw_pointer() || stored.kind == TYPE.ptr_of || stored.is_struct_object() : return false
    return act_level(at, prop.act, prop.exp) == ACT.readonly
}

// Whether this storage sits behind a read-only borrow: an element read out
// of one, or something reached through one
fn value_in_readonly_view(value: Value, ctx: Context) bool {
    let meta = value.meta
    if isset(meta) && meta.readonly_element : return true
    // A raw pointer read out of anything is unsafe territory, not a borrow
    let own_nn = value.rett.get_non_nullable()
    if own_nn.is_raw_pointer() || own_nn.kind == TYPE.ptr_of : return false
    let kind = value.type
    if kind == VAL.multi {
        // A borrow split into its words keeps its source in value1
        let source = value.value1
        if isset(source) && value_is_readonly_storage(source, ctx) : return true
        let parts = value.values ?! return false
        each parts as part {
            if value_in_readonly_view(part, ctx) : return true
        }
        return false
    }
    if kind != VAL.prop && kind != VAL.ptrv && kind != VAL.wrap && kind != VAL.cast && kind != VAL.ptr_of : return false
    let inner = value.value1 ?! return false
    // Through a raw pointer the unsafe rules apply instead
    let inner_nn = inner.rett.get_non_nullable()
    if inner_nn.is_raw_pointer() || inner_nn.kind == TYPE.ptr_of : return false
    if kind == VAL.prop {
        // A class object reached through a borrow is an ordinary object;
        // only inline storage (structs, fixed arrays) lives inside the borrow
        let owner_t = inner.rett.get_non_nullable()
        let stored = (owner_t.kind == TYPE.ref_of || owner_t.kind == TYPE.ref_slice || owner_t.kind == TYPE.stack_of)
            ? (owner_t.get_sub_type() ?? owner_t) : owner_t
        if stored.is_gc_type() || stored.kind == TYPE.interface || stored.kind == TYPE.closure : return false
    }
    return value_is_readonly_storage(inner, ctx)
}

// What to tell the writer: a borrow wants `&mut`, an immutable type never changes
fn value_readonly_hint(value: Value, ctx: Context) String {
    let current: ?Value = value
    while isset(current) {
        let nn = current.rett.get_non_nullable()
        let c = nn.class
        if isset(c) && c.immutable && nn.is_readonly_view() : return "'" + c.pretty_name() + "' is an immutable slice type"
        if value_is_readonly_property(current) {
            let prop = current.prop
            return "property '" + (isset(prop) ? (prop.name ?? "?") : "?") + "' is read-only here"
        }
        let kind = current.type
        if kind == VAL.multi : current = current.value1
        else if kind == VAL.prop || kind == VAL.ptrv || kind == VAL.wrap || kind == VAL.cast || kind == VAL.ptr_of : current = current.value1
        else : break
    }
    return "it needs '&mut'"
}

// Whether the value itself is a read-only borrow or lives behind one
fn value_is_readonly_storage(value: Value, ctx: Context) bool {
    if type_is_readonly_here(value.rett, ctx) : return true
    if value_is_readonly_property(value) : return true
    return value_in_readonly_view(value, ctx)
}

fn value_bind_method(func: Func, receiver: ?Value, ctx: Context, at: Exp) Value {
    let receiver_arg = func.args.get(0) !? null
    let frame_bound = false
    if !func.is_static && isset(receiver) && isset(receiver_arg) && receiver_arg.type.is_stack_borrow() {
        if !func.is_generic_base && !func.has_generic_args {
            let copied = value_is_copied_element(receiver)
            let readonly = value_is_readonly_storage(receiver, ctx)
            let captured = receiver.rett.is_fat_borrow() ? null[?Decl] : value_captured_decl(receiver)
            receiver = value_struct_receiver(receiver, receiver_arg.type, ctx)
            if copied : receiver.ensure_meta().copied_element = true
            if readonly : receiver.ensure_meta().readonly_element = true
            if isset(captured) : receiver.ensure_meta().captured_receiver = captured
        }
        frame_bound = true
    }
    let value = vgen_func(func, receiver)
    value.exp = at
    if frame_bound : value.mark_implicit_borrow()
    return value
}

fn value_member_class(type: Type) ?Class {
    let t = type.get_non_nullable()
    let direct = t.get_direct_class_or_null()
    if isset(direct) : return direct
    if t.kind == TYPE.ref_of || t.kind == TYPE.stack_of {
        if t.length != 1 : return null
        let held = t.get_sub_type() ?! return null
        return value_member_class(held)
    }
    if t.kind != TYPE.ptr_of && t.kind != TYPE.adr_of : return t.get_class_or_null()
    let sub = t.get_sub_type()
    // `&T` is a typed borrow, so member lookup remains that of T. Raw `*T`
    // keeps pointer member semantics unless it points at inline storage.
    if t.kind == TYPE.adr_of && isset(sub) : return sub.get_class_or_null()
    if isset(sub) && sub.is_inline : return sub.get_class_or_null()
    return type_ptr(t.build).class
}

////////////////////////////
// Default values
////////////////////////////

fn value_from_default_value(on: Value, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let target = value_default_target_type(on, at) !>

    if on.type == VAL.global {
        let global = on.global
        if !isset(global) : at.bug("VAL.global is missing its Global")
        let init = global.init_func
        if isset(init) {
            if !init.parsed_types : parse_func_types(init) !>
            let call = vgen_call(ctx, vgen_func(init), Array[Value]{}, init.get_rett(), scope)
            call.exp = at
            return call
        }
        let default_body = global.default_exp
        if isset(default_body) {
            let default_exp = parse_body_as_value_exp(default_body) !>
            let default_scope = global.scope.sub_scope()
            default_scope.func = scope.func
            let value = value_from_exp(default_exp, default_scope, ctx.with_suggested_type(target).for_direct_init()) !>
            if value.type != VAL.undefined {
                value = value_try_convert(value, scope, ctx, target, true, false, global.allow_unsafe) !>
                target.compat_check(value.rett, default_exp) !>
            }
            value.exp = at
            return value
        }
    }

    return value_generate_default(target, scope, ctx, at) !>
}

fn value_default_target_type(on: Value, at: Exp) Type !Error {
    if on.type != VAL.type : return on.rett
    let target = on.get_of_type()
    if isset(target) : return target
    let base = on.get_class_ref()
    if isset(base) && base.is_generic_base {
        at.error("Generic type '" + base.pretty_name() + "' requires type arguments before '$default_value'")
    }
    at.bug("Type value is missing its represented type")
}

fn value_generate_default(target: Type, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let value: ?Value = null
    let enu = target.get_enum()
    let target_class = target.get_class_or_null()
    let target_hook = isset(target_class) ? target_class.default_hook : null
    if isset(enu) {
        let first = enu.items.get(0) ! at.error("Cannot generate a default value for an empty enum")
        value = value_from_enum_item(enu, first.name, at, scope, ctx) !>
    } else if target.is_nullable_type() {
        value = vgen_null(target)
    } else if target.is_float_type() {
        value = vgen_float(0, target)
    } else if target.is_integer_type() {
        value = vgen_int(0, target)
    } else if target.is_bool_type() {
        value = vgen_bool(false, target)
    } else if target.is_fixed_array_type() {
        let sub = target.get_sub_type()
        if !isset(sub) : at.bug("Array type is missing sub_type")
        let values = Array[Value]{ value_generate_default(sub, scope, ctx, at) !> }
        value = vgen_array_init(ctx, values, target, at, target.length > 1)
    } else if isset(target_hook) {
        let hook = target_hook
        if !hook.parsed_types : parse_func_types(hook) !>
        let callee = vgen_func(hook)
        let args = value_from_call_args(callee, hook, null, scope, ctx, at) !>
        value_check_call_args(hook, callee, args, scope, ctx, at) !>
        value = vgen_call(ctx, callee, args, hook.get_rett(), scope)
    } else if target.is_inline {
        let class = class_from_inline_init_type(target)
        if !isset(class) : at.bug("Inline default type has no class")
        value = class_init_construct(class, target, HashMap[Prop, Value]{}, scope, ctx, at) !>
    } else {
        let string_class = type_string(ctx.build).get_class_or_null()
        if isset(string_class) && target_class == string_class {
            value = vgen_string("", target)
        }
    }

    if !isset(value) {
        at.error("Cannot generate a default value for type: " + target.compat_display_name())
    }
    value.exp = at
    return value
}

fn type_has_generated_default(target: Type, b: Build) bool {
    return type_has_generated_default_inner(target, b, Array[Class]{})
}

fn type_has_generated_default_inner(target: Type, b: Build, seen: Array[Class]) bool {
    let enu = target.get_enum()
    if isset(enu) : return enu.items.length > 0
    if target.is_nullable_type() : return true
    if target.is_float_type() : return true
    if target.is_integer_type() : return true
    if target.is_bool_type() : return true
    if target.is_fixed_array_type() {
        let sub = target.get_sub_type()
        return isset(sub) && type_has_generated_default_inner(sub, b, seen)
    }
    if target.is_inline {
        let class = class_from_inline_init_type(target)
        if !isset(class) || seen.contains(class) : return false
        let path = seen.copy()
        path.append(class)
        each class.props as prop {
            if prop.is_slice_data || prop.zero_default || isset(prop.default) : continue
            if !type_has_generated_default_inner(prop.type, b, path) : return false
        }
        return true
    }
    let string_class = type_string(b).get_class_or_null()
    let target_class = target.get_class_or_null()
    if isset(string_class) && target_class == string_class : return true
    return isset(target_class) && isset(target_class.default_hook)
}

////////////////////////////
// Errors and enums
////////////////////////////

fn value_from_is_shared(on: Value, at: Exp, b: Build) Value !Error {
    if !on.rett.is_gc_type() {
        at.error("The '$is_shared' token can only be used on 'class' types. Not on: " + on.rett.compat_display_name())
    }
    if on.rett.is_nullable_type() {
        at.error("The '$is_shared' token cannot be used on null-able types. Your type: " + on.rett.compat_display_name())
    }

    let type_u8 = type_from_core(b, "u8") !>
    let offset = vgen_int(-4, type_int(b))
    let flags = vgen_ptrv(on, offset, type_u8, at)
    let shared_bit = vgen_int(4, type_u8)
    let mask = vgen_op(flags, shared_bit, VALUE_OP.bit_and, type_u8)
    mask.exp = at
    let cmp = vgen_op(mask, vgen_int(4, type_u8), VALUE_OP.eq, type_bool(b))
    cmp.exp = at
    return cmp
}

// `.code` / `TypeName.code` / `E.name` — a named, payload-less error code constant.
fn value_from_error_code_item(et: ErrorType, name: String, at: Exp) Value !Error {
    if !et.codes.contains(name) {
        at.error("Error type '" + et.name + "' does not have an error code named: '" + name + "'")
    }
    let result = Value {
        type: VAL.error_value
        rett: type_error_code(et)
        exp: at
    }
    result.set_string(name)
    return result
}

fn value_from_error_payload_init_exp(exp: Exp, scope: Scope, ctx: Context, left_exp: Exp) Value !Error {
    let func = ctx.get_func() !>
    let et = func.error_type
    if !isset(et) : left_exp.error("Throwing an error but your function has no error type")
    let name = left_exp.str1
    if !et.codes.contains(name) {
        left_exp.error("Error type '" + et.name + "' does not have an error code named: '" + name + "'")
    }

    let values = value_parse_error_payload_fields(exp, et, scope, ctx) !>
    let result = Value {
        type: VAL.error_value
        values: values
        rett: type_error_code(et)
        exp: exp
    }
    result.set_string(name)
    value_complete_error_payload(result, scope, ctx, exp) !>
    return result
}

fn value_from_error_rethrow_payload_exp(exp: Exp, scope: Scope, ctx: Context, caught: Value) Value !Error {
    let func = ctx.get_func() !>
    let et = func.error_type
    if !isset(et) : exp.error("Throwing an error but your function has no error type")
    let caught_et = caught.rett.get_non_nullable().get_error_type()
    if !isset(caught_et) : exp.bug("Caught error value is missing its ErrorType")
    if !et.is_compat(caught_et) {
        exp.error("Error types are not compatible: " + et.name + " <> " + caught_et.name)
    }

    let values = value_parse_error_payload_fields(exp, et, scope, ctx) !>
    // value1 carries the runtime code; empty string_val marks a dynamic error value.
    let result = Value {
        type: VAL.error_value
        value1: caught
        values: values
        rett: type_error_code(et)
        exp: exp
    }
    value_complete_error_payload(result, scope, ctx, exp) !>
    return result
}

// Parse `{ field: value, ... }` against an error type's payload map.
fn value_parse_error_payload_fields(exp: Exp, et: ErrorType, scope: Scope, ctx: Context) Array[Value] !Error {
    let body = exp.get(VALUE_EXP.right)
    if !isset(body) : exp.bug("Error payload init is missing body")
    if body.type != EXP.body : exp.bug("Error payload init body must be EXP.body")

    let items_exp = parse_body_as_class_init_fields(body) !>
    let values = Array[Value]{}
    let i: uint = 0
    while true {
        let item = items_exp.get(i)
        if !isset(item) : break
        let ik = item.number.@cast(CLASS_INIT_ITEM)
        if ik != CLASS_INIT_ITEM.field {
            item.error("Error payload fields must be named (e.g. 'field: value')")
        }
        let fname = item.str1
        let ftype = et.payload.get(fname) !? null
        if !isset(ftype) {
            item.error("Error type '" + et.name + "' has no payload field named: '" + fname + "'")
        }
        let vexp = item.get(VALUE_EXP.left)
        if !isset(vexp) : item.bug("Payload field is missing a value")
        let fv = value_from_exp(vexp, scope, ctx.with_suggested_type(ftype)) !>
        if fv.type != VAL.undefined {
            fv = value_try_convert(fv, scope, ctx, ftype) !>
            ftype.compat_check(fv.rett, item) !>
        }
        fv.set_name(fname)
        values.append(fv)
        i++
    }
    return values
}

fn value_complete_error_payload(value: Value, scope: Scope, ctx: Context, at: Exp) !Error {
    let et = value.rett.get_non_nullable().get_error_type()
    if !isset(et) : at.error("This is not an error value")
    if et.payload.length == 0 : return

    let values = value.values ?? Array[Value]{}
    value.values = values
    let provided = Map[bool]{}
    // A payload travels with the error, beyond anything this body can see
    each values as field {
        provided.set(field.get_name(), true)
        value_mark_actual_escape(field)
    }

    let caught_type: ?ErrorType = null
    let caught_decl: ?Decl = null
    let eidf = scope.get_idf("E")
    if isset(eidf) {
        let decl = eidf.decl
        if isset(decl) {
            caught_decl = decl
            caught_type = decl.type.get_non_nullable().get_error_type()
        }
    }

    each et.payload as ptype, name {
        if provided.has(name) : continue
        if isset(caught_type) {
            let to_global = et.payload_globals.get(name) !? null
            let from_global = caught_type.payload_globals.get(name) !? null
            if isset(to_global) && isset(from_global) && to_global == from_global {
                let payloads: ?Map[Decl] = null
                if isset(caught_decl) : payloads = caught_decl.error_payloads
                if isset(payloads) {
                    let payload_decl = payloads.get(name) !? null
                    if isset(payload_decl) {
                        let inherited = vgen_decl(payload_decl)
                        inherited.rett = ptype
                        inherited.set_name(name)
                        values.append(inherited)
                        continue
                    }
                }
                // Global is shared but we have no local snapshot — leave the live global.
                continue
            }
        }
        let default_body = et.payload_defaults.get(name) !? null
        if isset(default_body) {
            let fallback = value_from_error_payload_default(et, name, ptype, default_body, scope, ctx, at) !>
            values.append(fallback)
            continue
        }
        at.error("Missing error payload field '" + name + "' for " + et.name + ". Provide it in the throw, or declare a default on the error type: " + name + ": Type (value)")
    }
}

fn value_from_error_payload_default(et: ErrorType, name: String, ptype: Type, default_body: Exp, scope: Scope, ctx: Context, at: Exp) Value !Error {
    let default_exp = parse_body_as_value_exp(default_body) !>
    let default_scope = et.scope.sub_scope()
    default_scope.func = scope.func
    let fallback = value_from_exp(default_exp, default_scope, ctx.with_suggested_type(ptype)) !>
    if fallback.type != VAL.undefined {
        fallback = value_try_convert(fallback, scope, ctx, ptype) !>
        ptype.compat_check(fallback.rett, default_exp) !>
    }
    fallback.set_name(name)
    fallback.exp = at
    return fallback
}

// `EnumName.item` — resolve the variant to a typed constant value.
fn value_from_enum_item(enu: Enum, name: String, at: Exp, scope: Scope, ctx: Context) Value !Error {
    let item: ?EnumItem = null
    each enu.items as it {
        if it.name == name {
            item = it
            break
        }
    }
    if !isset(item) {
        at.error("Enum " + enu.name + " does not have a value named: '" + name + "'")
    }

    if item.resolving : at.error("Circular enum member initializer: " + enu.name + "." + name)
    item.resolving = true
    defer enum_item_resolved(item)
    return value_resolve_enum_item(enu, item, at, scope, ctx) !>
}

fn idf_resolved(idf: Idf) {
    idf.resolving = false
}

fn enum_item_resolved(item: EnumItem) {
    item.resolving = false
}

fn value_resolve_enum_item(enu: Enum, item: EnumItem, at: Exp, scope: Scope, ctx: Context) Value !Error {
    let name = item.name

    let etype = enu.get_type() !>
    let item_type = enu.get_item_type() !>
    let value_body = item.value
    if isset(value_body) {
        // Explicit `(value)` — parse as a value expression with enum type suggestion
        let pos = value_body.pos
        if !isset(pos) : value_body.bug("Enum item value is missing a Pos")
        let p = pos.copy().init_parser()
        let vexp = read_value_exp(p, true, true) !>
        let v = value_from_exp(vexp, enu.scope, ctx.with_suggested_type(item_type).for_direct_init()) !>
        v = value_try_convert(v, enu.scope, ctx, item_type) !>
        // Tag result type as this enum
        v.rett = etype
        v.exp = at
        return v
    }

    // Implicit integer ordinal: first free ints skipping explicit values (old parse_enum_type)
    let used = Map[bool]{}
    each enu.items as it {
        let vb = it.value
        if !isset(vb) : continue
        // Only pre-scan integer literals for occupancy; full eval is deferred
        let pos = vb.pos
        if !isset(pos) : continue
        let p = pos.copy().init_parser()
        let t = p.tok(true, true, false) !>
        if t == TOK.int || t == TOK.hex || t == TOK.octal {
            let vexp = read_value_exp(p, true, true) !>
            let lit = value_from_exp(vexp, enu.scope, ctx.with_suggested_type(type_int(ctx.build))) !>
            if lit.type == VAL.int {
                used.set("" + lit.int_val, true)
            }
        }
    }
    let i: int = 0
    each enu.items as it {
        if it.name == name {
            while used.has("" + i) {
                i++
            }
            let v = vgen_int(i, etype)
            v.exp = at
            return v
        }
        if !isset(it.value) {
            while used.has("" + i) {
                i++
            }
            used.set("" + i, true)
            i++
        }
    }
    at.bug("Enum item not found after scan: " + name)
}

////////////////////////////
// Compile-time type members
////////////////////////////

fn value_type_prop(on: Value, name: String, at: Exp, b: Build) ?Value !Error {
    let t = on.get_of_type()
    if !isset(t) : at.bug("VAL.type is missing of_type")

    // Type-returning
    if name == "$nonnull" {
        return vgen_type(t.get_non_nullable(), b)
    }

    // Integer: same as size_of(T)
    if name == "$size" {
        let bytes = t.byte_size() !>
        return vgen_int(bytes.to(int), type_int(b))
    }

    // Integer range: `u8.$max`, `i64.$min`. The literal is typed as the integer itself.
    if name == "$max" || name == "$min" {
        if !t.is_integer_type() {
            at.error("'" + name + "' is only defined for integer types, not: " + t.compat_display_name())
        }
        return vgen_int(integer_bound(t.byte_size() !> * 8, t.is_signed_type(), name == "$max"), t)
    }

    // Bool queries
    if name == "$is_inline" {
        let slice = t.get_non_nullable().kind == TYPE.ref_slice
        let inline = t.is_inline_type() && !t.is_number_type() && !slice
        return vgen_bool(inline, type_bool(b))
    }
    if name == "$is_nullable" : return vgen_bool(t.is_nullable_type(), type_bool(b))
    if name == "$is_void" : return vgen_bool(t.is_void_type(), type_bool(b))
    if name == "$is_integer" : return vgen_bool(t.is_integer_type(), type_bool(b))
    if name == "$is_float" : return vgen_bool(t.is_float_type(), type_bool(b))
    if name == "$is_enum" : return vgen_bool(isset(t.get_non_nullable().get_enum()), type_bool(b))
    if name == "$is_bool" : return vgen_bool(t.is_bool_type(), type_bool(b))
    if name == "$is_signed" : return vgen_bool(t.is_signed_type(), type_bool(b))
    if name == "$is_structural" : return vgen_bool(t.is_structural_type(), type_bool(b))
    if name == "$is_gc" : return vgen_bool(t.is_gc_type(), type_bool(b))
    if name == "$has_gc_refs" : return vgen_bool(t.has_gc_refs(), type_bool(b))
    if name == "$is_pointer" : return vgen_bool(t.is_pointer(), type_bool(b))
    if name == "$is_raw_pointer" : return vgen_bool(t.is_raw_pointer(), type_bool(b))
    if name == "$is_typed_pointer" {
        let pointer = t.get_non_nullable()
        return vgen_bool(pointer.kind == TYPE.ptr_of || pointer.is_struct_object(), type_bool(b))
    }
    if name == "$is_borrow" : return vgen_bool(t.is_bare_borrow() || t.get_non_nullable().kind == TYPE.adr_of, type_bool(b))
    if name == "$has_raw_pointer_props" : return vgen_bool(t.has_raw_pointer_props(), type_bool(b))
    // Byte offset of the element area inside sequence storage: a slice's
    // block, or an array's `data` block
    if name == "$slice_data_offset" {
        let class = t.get_non_nullable().get_class_or_null()
        if !isset(class) || (class.kind != CLASS_KIND.slice && class.kind != CLASS_KIND.array) {
            at.error("'$slice_data_offset' is only defined for slice and array types")
        }
        return vgen_int((class.slice_data_offset() !>).to(int), type_int(b))
    }
    if name == "$has_vtable" : return vgen_bool(t.has_vtable(), type_bool(b))
    if name == "$has_hash" : return vgen_bool(t.has_hash(), type_bool(b))
    if name == "$has_eq" : return vgen_bool(t.has_eq(), type_bool(b))
    if name == "$can_order" : return vgen_bool(type_can_order(t) !>, type_bool(b))
    if name == "$property_count" {
        let class = t.get_non_nullable().get_class_or_null()
        if isset(class) : parse_class_properties(class) !>
        return vgen_int(isset(class) ? class.props.length.to(int) : 0, type_int(b))
    }

    // Method-style meta: keep as `.name` so `.(args)` folds via value_type_method
    if name == "$can_convert_to" : return null
    if name == "$is_type_of" : return null
    if name == "$has_method" : return null
    if name == "$is_compatible_with" : return null

    // Unknown `$…` meta on a type
    if name.starts_with("$") {
        at.error("Unknown type property: '" + name + "'")
    }
    // Normal name (e.g. static method `Node.tree`) — not a type meta-prop
    return null
}

// Largest or smallest value of an integer width, as the 64-bit pattern int_val holds.
// A 64-bit unsigned maximum is all ones, which is -1 in that pattern.
fn integer_bound(bits: u32, signed: bool, max: bool) int {
    let one: uint = 1
    if signed {
        let half = one << (bits - 1)
        if max : return (half - 1).to(int)
        if bits == 64 : return half.to(int)
        return 0 - half.to(int)
    }
    if !max : return 0
    if bits == 64 : return (0 - one).to(int)
    return ((one << bits) - 1).to(int)
}

// Type meta-method calls: T.$can_convert_to(U) / T.$is_type_of(U) / T.$has_method(name) / …
fn value_type_method(on: Value, name: String, args: Array[Value], at: Exp, b: Build) Value !Error {
    let t = on.get_of_type()
    if !isset(t) : at.bug("VAL.type is missing of_type")

    if name == "$can_convert_to" {
        if args.length != 1 {
            at.error("'.$can_convert_to' expects 1 type argument, got " + args.length)
        }
        // Other type optional: unresolved → false (conversion hooks not ready)
        let other = (args.get(0) ! at.bug("Missing $can_convert_to arg")).as_type()
        if !isset(other) : return vgen_bool(false, type_bool(b))
        // `$to` hooks are registered while parsing method types; the source
        // class may not have reached that point yet
        let source_class = t.get_non_nullable().get_class_or_null()
        if isset(source_class) : class_parse_own_func_types(source_class) !>
        return vgen_bool(t.can_convert_to(other), type_bool(b))
    }

    if name == "$is_type_of" {
        if args.length != 1 {
            at.error("'.$is_type_of' expects 1 type argument, got " + args.length)
        }
        let other = (args.get(0) ! at.bug("Missing $is_type_of arg")).as_type()
        if !isset(other) : return vgen_bool(false, type_bool(b))
        return vgen_bool(t.is_same_type(other), type_bool(b))
    }

    if name == "$is_compatible_with" {
        if args.length != 1 {
            at.error("'.$is_compatible_with' expects 1 type argument, got " + args.length)
        }
        let other = (args.get(0) ! at.bug("Missing $is_compatible_with arg")).as_type()
        if !isset(other) : return vgen_bool(false, type_bool(b))
        return vgen_bool(t.compat(other), type_bool(b))
    }

    if name == "$has_method" {
        if args.length != 1 {
            at.error("'.$has_method' expects 1 name argument, got " + args.length)
        }
        let mname = (args.get(0) ! at.bug("Missing $has_method arg")).as_name()
        if !isset(mname) : return vgen_bool(false, type_bool(b))
        return vgen_bool(t.has_method(mname), type_bool(b))
    }

    at.error("Unknown type method: '" + name + "'")
}

////////////////////////////
// Constant folding and call checks
////////////////////////////

// Fold a binary op when both sides are compile-time constants. Returns null if not foldable.
fn value_try_fold_op(left: Value, right: Value, op: VALUE_OP, b: Build) ?Value {
    if op == VALUE_OP.and {
        if left.type == VAL.bool && right.type == VAL.bool {
            return vgen_bool(left.bool_val && right.bool_val, type_bool(b))
        }
        return null
    }
    if op == VALUE_OP.or {
        if left.type == VAL.bool && right.type == VAL.bool {
            return vgen_bool(left.bool_val || right.bool_val, type_bool(b))
        }
        return null
    }

    if op == VALUE_OP.eq || op == VALUE_OP.ne {
        // A literal number next to a string literal is compared as its text,
        // a character literal as the character
        if (left.type == VAL.string) != (right.type == VAL.string) {
            let number = left.type == VAL.string ? right : left
            let text = left.type == VAL.string ? left : right
            if number.type == VAL.int && number.rett == type_char(b) {
                let buf = ByteBuffer.new()
                buf.write_byte(number.int_val.to(u8))
                let eq = text.get_string() == buf.to_string()
                return vgen_bool(op == VALUE_OP.eq ? eq : !eq, type_bool(b))
            }
        }
        // String-ish compare (macro defs, bare words like `win` in `OS == win`)
        let ls = left.fold_string()
        let rs = right.fold_string()
        if isset(ls) && isset(rs) {
            let eq = ls == rs
            return vgen_bool(op == VALUE_OP.eq ? eq : !eq, type_bool(b))
        }
        // Integer compare
        if left.type == VAL.int && right.type == VAL.int {
            let eq = left.int_val == right.int_val
            return vgen_bool(op == VALUE_OP.eq ? eq : !eq, type_bool(b))
        }
        // Bool compare
        if left.type == VAL.bool && right.type == VAL.bool {
            let eq = left.bool_val == right.bool_val
            return vgen_bool(op == VALUE_OP.eq ? eq : !eq, type_bool(b))
        }
        return null
    }

    if op == VALUE_OP.lt || op == VALUE_OP.gt || op == VALUE_OP.lte || op == VALUE_OP.gte {
        if left.type == VAL.string && right.type == VAL.string {
            let l = left.get_string()
            let r = right.get_string()
            let ok = false
            if op == VALUE_OP.lt : ok = l < r
            else if op == VALUE_OP.gt : ok = l > r
            else if op == VALUE_OP.lte : ok = l <= r
            else : ok = l >= r
            return vgen_bool(ok, type_bool(b))
        }
        if left.type == VAL.int && right.type == VAL.int {
            if !fold_integer_types_match(left, right) : return null
            if !left.rett.is_signed_type() {
                let l = left.int_val.to(u64)
                let r = right.int_val.to(u64)
                let ok = false
                if op == VALUE_OP.lt : ok = l < r
                else if op == VALUE_OP.gt : ok = l > r
                else if op == VALUE_OP.lte : ok = l <= r
                else : ok = l >= r
                return vgen_bool(ok, type_bool(b))
            }
            let ok = false
            if op == VALUE_OP.lt : ok = left.int_val < right.int_val
            else if op == VALUE_OP.gt : ok = left.int_val > right.int_val
            else if op == VALUE_OP.lte : ok = left.int_val <= right.int_val
            else : ok = left.int_val >= right.int_val
            return vgen_bool(ok, type_bool(b))
        }
        return null
    }

    if left.type == VAL.int && right.type == VAL.int {
        if !fold_integer_types_match(left, right) : return null
        let l = left.int_val
        let r = right.int_val
        let result: int = 0
        if op == VALUE_OP.add : result = l + r
        else if op == VALUE_OP.sub : result = l - r
        else if op == VALUE_OP.mul : result = l * r
        else if op == VALUE_OP.div && r != 0 {
            result = left.rett.is_signed_type() ? l / r : (l.to(u64) / r.to(u64)).to(int)
        } else if op == VALUE_OP.mod && r != 0 {
            result = left.rett.is_signed_type() ? l % r : (l.to(u64) % r.to(u64)).to(int)
        } else if op == VALUE_OP.bit_and : result = l & r
        else if op == VALUE_OP.bit_or : result = l | r
        else if op == VALUE_OP.bit_xor : result = l ^ r
        else if op == VALUE_OP.shl : result = l << r
        else if op == VALUE_OP.shr {
            result = left.rett.is_signed_type() ? l >> r : (l.to(u64) >> r.to(u64)).to(int)
        } else : return null
        return vgen_int(fold_integer_to_width(result, left.rett), left.rett)
    }

    return null
}

fn fold_integer_types_match(left: Value, right: Value) bool {
    return left.rett.is_integer_type() && right.rett.is_integer_type()
        && left.rett.is_signed_type() == right.rett.is_signed_type()
        && left.rett.number_byte_size() == right.rett.number_byte_size()
}

// Does `l op r` leave the range of `type`? Used to reject untyped literal
// arithmetic that would wrap in its destination type.
fn fold_integer_overflows(l: int, r: int, op: VALUE_OP, type: Type) bool {
    if op != VALUE_OP.add && op != VALUE_OP.sub && op != VALUE_OP.mul && op != VALUE_OP.shl : return false
    let bits = type.number_byte_size() * 8
    let signed = type.is_signed_type()
    if bits < 64 {
        let result: int = 0
        if op == VALUE_OP.add : result = l + r
        else if op == VALUE_OP.sub : result = l - r
        else if op == VALUE_OP.mul : result = l * r
        else : result = l << r
        return fold_integer_to_width(result, type) != result
    }
    if signed {
        if op == VALUE_OP.add : return ((l ^ (l + r)) & (r ^ (l + r))) < 0
        if op == VALUE_OP.sub : return ((l ^ r) & (l ^ (l - r))) < 0
        if op == VALUE_OP.mul : return l != 0 && ((l * r) / l != r || (l == -1 && r == (-9223372036854775807 - 1)))
        return ((l << r) >> r) != l
    }
    let ul = l.to(u64)
    let ur = r.to(u64)
    if op == VALUE_OP.add : return ul + ur < ul
    if op == VALUE_OP.sub : return ul < ur
    if op == VALUE_OP.mul : return ul != 0 && (ul * ur) / ul != ur
    return ((ul << ur) >> ur) != ul
}

fn fold_integer_to_width(value: int, type: Type) int {
    let bits = type.number_byte_size() * 8
    if bits == 0 || bits >= 64 : return value
    let modulus = 1 << bits.to(int)
    let narrowed = value & (modulus - 1)
    if type.is_signed_type() {
        let sign = 1 << (bits - 1).to(int)
        if (narrowed & sign) != 0 : return narrowed - modulus
    }
    return narrowed
}

// ---- Operator / call type checks ----

fn value_check_op_compat(left: Value, right: Value, op: VALUE_OP, at: Exp) !Error {
    let lt = left.rett
    let rt = right.rett
    if lt.is_void_type() || rt.is_void_type() : return

    if op == VALUE_OP.and || op == VALUE_OP.or {
        if !lt.is_bool_type() {
            at.error("Left operand of logical operator must be bool")
        }
        if !rt.is_bool_type() {
            at.error("Right operand of logical operator must be bool")
        }
        return
    }

    if !lt.compat(rt) {
        if op == VALUE_OP.eq || op == VALUE_OP.ne || op == VALUE_OP.lt || op == VALUE_OP.gt || op == VALUE_OP.lte || op == VALUE_OP.gte {
            if rt.compat(lt) : return
            // Shared and plain strings are the same immutable value
            let string_class = type_string(lt.build).get_class_or_null()
            if isset(string_class) && lt.get_non_nullable().get_class_or_null() == string_class
                && rt.get_non_nullable().get_class_or_null() == string_class : return
        }
        lt.compat_check(rt, at) !>
    }
}

// A method on locked data may keep what it is handed: anything the callee
// stores into its receiver (or lets escape) must be a unique graph, since it
// is published with the locked data and must not keep an ordinary alias.
fn value_check_locked_call_arg(func: Func, param_i: uint, av: Value, receiver: Value, ctx: Context) !Error {
    let nn = av.rett.get_non_nullable()
    if !av.rett.has_gc_refs() && nn.kind != TYPE.closure : return
    if nn.is_shared || nn.is_locked : return
    // Moved around inside the locked graph it already belongs to
    if value_stays_inside(av, receiver) : return
    let string_class = type_string(func.ns.build).get_class_or_null()
    if isset(string_class) && nn.get_class_or_null() == string_class : return
    let moves_in = !func.unique_summary_ready
        || func_arg_may_escape(func, param_i)
        || func_arg_invalidates_unique(func, param_i)
    if !moves_in {
        let receiver_arg = func.args.get(0) !? null
        if isset(receiver_arg) : decl_resolve_pending_absorbs(receiver_arg.decl) !>
        let absorbs = isset(receiver_arg) ? receiver_arg.absorbs_args : null
        moves_in = isset(absorbs) && absorbs.contains(param_i)
    }
    if !moves_in : return
    if !(value_require_unique_graph(av) !>) {
        // Came in through a parameter of this function: the receiver absorbs
        // it, and the outer call site proves uniqueness instead.
        let sources = value_summary_source_args(av, ctx.get_func() !>)
        if isset(sources) {
            value_mark_summary_absorbs(receiver, sources)
            return
        }
        av.error("Only a provably unique value graph can be handed to a method on locked data; a mutable alias may remain")
    }
    value_publish_unique(av, ctx) !>
}

fn value_check_call_args(func: Func, callee: Value, args: Array[Value], scope: Scope, ctx: Context, at: Exp, check_pointer_args: bool (true), is_coroutine: bool (false)) !Error {
    // Ensure signature types are resolved
    if !func.parsed_types {
        parse_func_types(func) !>
    }
    ensure_func_unique_summary(func) !>

    let params = func.args
    // Instance methods have `this` as first arg; call args are the remaining ones
    let skip: uint = 0
    if isset(func.class) && !func.is_static {
        let on = callee.value1
        if isset(on) && on.type != VAL.type {
            skip = 1
        }
    }

    let receiver: ?Value = null
    let absorbs_known = true
    if skip == 1 {
        receiver = callee.value1
        if isset(receiver) {
            let receiver_param = params.get(0) !? null
            if isset(receiver_param) && receiver_param.type.is_stack_borrow() && value_is_copied_element(receiver)
                && (func_writes_receiver(func) !>)
            {
                at.error("Cannot call a method that changes its receiver on an element copied out of an Array; borrow it with '&mut arr[i]' or store the element back with '.set'")
            }
            // A struct receiver lives inside the borrow and a slice method
            // writes the slice's storage; a class or interface method works
            // on the object the slot refers to
            let func_class = func.class
            let storage_receiver = (isset(receiver_param) && receiver_param.type.is_stack_borrow())
                || (isset(func_class) && func_class.kind == CLASS_KIND.slice)
            if storage_receiver && value_is_readonly_storage(receiver, ctx) && (func_writes_receiver(func) !>) {
                at.error("Cannot call a method that changes its receiver through a read-only borrow; " + value_readonly_hint(receiver, ctx))
            }
            let receiver_meta = receiver.meta
            let captured = isset(receiver_meta) ? receiver_meta.captured_receiver : null
            if isset(captured) && storage_receiver && (func_writes_receiver(func) !>) {
                at.error("Cannot call a method that changes captured variable '" + (captured.name ?? "?") + "': closures capture values, not variables; mutate through an object instead")
            }
            let current = ctx.func
            if isset(current) && !func.is_static && value_reaches_receiver(receiver, ctx) {
                let calls = current.this_calls
                if !isset(calls) {
                    calls = Array[Func]{}
                    current.this_calls = calls
                }
                calls.append(func, true)
            }
            let receiver_arg = params.get(0) !? null
            let owner = ctx.get_func() !>
            // A flag that is already set is final; the rest may still flip
            if func.unique_summary_ready && func_arg_may_escape(func, 0) : value_mark_actual_escape(receiver)
            if func.unique_summary_ready && func_arg_invalidates_unique(func, 0) && !value_is_raw_pointer_arg(receiver) : value_invalidate_unique(receiver)
            else if !(value_absorb_call_args(func, receiver_arg, receiver, args, skip, owner, false) !>) : absorbs_known = false
            if !func_arg_summary_settled(func, 0) && isset(receiver_arg) {
                value_defer_arg_effects(receiver_arg, receiver, at) !>
            }
        }
    }

    let max_params = params.length > skip ? params.length - skip : 0

    let required: uint = 0
    let pi: uint = skip
    while pi < params.length {
        let p = params.get(pi) ! break
        if !isset(p.default) : required = (pi - skip) + 1
        pi++
    }

    if args.length < required {
        at.error("Not enough arguments for '" + func.name + "' (expected at least " + required + ", got " + args.length + ")")
    }
    if !func.infinite_args && args.length > max_params {
        at.error("Too many arguments for '" + func.name + "' (expected at most " + max_params + ", got " + args.length + ")")
    }

    let i: uint = 0
    while i < args.length {
        let param_i = skip + i
        let param: ?Arg = null
        if param_i < params.length : param = params.get(param_i) !? null

        let av = args.get(i) ! break
        av.require_result() !>
        if !isset(param) {
            if !func.infinite_args : break
            args.set(i, value_variadic_extern_arg(av, ctx, at) !>) _
            i++
            continue
        }
        if !param.is_generic {
            if func.is_extern {
                let target = param.type.get_non_nullable()
                let source = av.rett.get_non_nullable()
                let pointer_boundary = target.is_raw_pointer() || target.kind == TYPE.ptr_of || target.is_struct_object()
                let explicit_raw = source.is_raw_pointer() || source.kind == TYPE.ptr_of || source.is_struct_object()
                if pointer_boundary && !explicit_raw {
                    at.error("Extern pointer arguments require an explicit raw pointer; use '@ref(...)'")
                }
            }
            // Convert first (literal int → i32, nullable strip, $to hooks, …)
            // An owned borrow may live anywhere, so an inline property converts to it
            let accepts_borrow = param.type.has_borrowed_refs() || param.type.get_non_nullable().is_owned_borrow()
            // Passing an ordinary graph to a shared parameter is either a
            // borrow or a publish, and only the callee's summary says which.
            // Settle it here when we can: deferring the decision means no
            // publish is emitted, and a callee that turns out to store the
            // value would keep an unpublished object in shared storage.
            if !func.unique_summary_ready
                && param.type.get_non_nullable().is_shared
                && !av.rett.get_non_nullable().is_shared
            {
                ensure_func_unique_return_summary(func) !>
            }
            let settled = func_arg_summary_settled(func, param_i)
            // A coroutine keeps its arguments past the call, so a temporary
            // shared view of ordinary or locked data is not enough
            let accepts_shared_borrow = !is_coroutine && (!func.unique_summary_ready
                || (!func_arg_may_escape(func, param_i) && !func_arg_invalidates_unique(func, param_i)))
            // A borrow accepted on unsettled flags is re-checked once they settle
            let pending_shared_borrow = !settled && accepts_shared_borrow
                && param.type.get_non_nullable().is_shared
                && !av.rett.get_non_nullable().is_shared
            // Part of the locked graph handed back to a method on that graph
            // is a rearrangement, not a new view
            if isset(receiver) && receiver.rett.get_non_nullable().is_locked
                && av.rett.get_non_nullable().is_locked && !param.type.get_non_nullable().is_locked
                && value_stays_inside(av, receiver)
            {
                av = vgen_cast(av, av.rett.plain_type())
            }
            av = value_try_convert(av, scope, ctx, param.type, true, accepts_borrow, false, accepts_shared_borrow) !>
            if isset(receiver) && receiver.rett.get_non_nullable().is_locked {
                value_check_locked_call_arg(func, param_i, av, receiver, ctx) !>
            }
            let owner = ctx.get_func() !>
            if func.unique_summary_ready && func_arg_may_escape(func, param_i) : value_mark_actual_escape(av)
            if func.unique_summary_ready && func_arg_invalidates_unique(func, param_i) && !value_is_raw_pointer_arg(av) : value_invalidate_unique(av)
            else if !(value_absorb_call_args(func, param, av, args, skip, owner, false) !>) : absorbs_known = false
            if !settled : value_defer_arg_effects(param, av, at, pending_shared_borrow) !>
            let source_kind = av.rett.get_non_nullable().kind
            let target_kind = param.type.get_non_nullable().kind
            let accepts_reference_pointer = (source_kind == TYPE.ptr_of || source_kind == TYPE.adr_of || value_is_borrow_address(av))
                && (target_kind == TYPE.ptr_of || param.type.is_raw_pointer())
            if av.is_implicit_borrow() && accepts_reference_pointer && !accepts_borrow {
                // A flag that is already set is final
                if func.unique_summary_ready && param.may_escape {
                    require_unsafe(at, scope, "Passing an implicit pointer borrow to escaping pointer storage") !>
                } else if !func_arg_summary_settled(func, param_i) && !scope.mark_unsafe_used() && !ctx.build.options.ignore_unsafe {
                    if !isset(param.cannot_escape_pos) : param.cannot_escape_pos = Pos { src: at.src, i: at.i }
                }
            }
            // A closure bound to a frame receiver may be handed to a callee
            // that is known not to keep it.
            let frame_closure = av.is_implicit_borrow() && source_kind == TYPE.closure && target_kind == TYPE.closure
            if frame_closure && !func.unique_summary_ready : ensure_func_unique_return_summary(func) !>
            let accepts_frame_closure = frame_closure
                && func.unique_summary_ready && !func_arg_may_escape(func, param_i)
            if frame_closure && !accepts_frame_closure {
                at.error("A method bound to a stack receiver cannot be passed to '" + func.name + "'; it is kept beyond the call through parameter '" + param.name + "'")
            }
            // A provisional flag may still flip; re-checked once it settles
            if frame_closure && !func_arg_summary_settled(func, param_i) && !isset(param.cannot_escape_pos) {
                param.cannot_escape_pos = Pos { src: at.src, i: at.i }
            }
            if av.is_implicit_borrow() && !accepts_borrow && !accepts_reference_pointer && !accepts_frame_closure {
                at.error("An implicit pointer borrow cannot be passed to raw pointer storage; use '@ref' explicitly")
            }
            args.set(i, av) ! at.bug("value_check_call_args: args.set out of range")
            let avt = av.rett
            if !avt.is_void_type() {
                if !param.type.is_nullable_type() && avt.is_nullable_type() {
                    let proven = false
                    if av.type == VAL.decl {
                        let d = av.decl
                        if isset(d) : proven = scope.not_null_check_relied(d)
                    }
                    if !proven : at.error("Incompatible types. Expected '" + param.type.compat_display_name() + "', got '" + avt.compat_display_name() + "'")
                }
                param.type.compat_check(avt, at) !>
            }
        }
        i++
    }
    if !absorbs_known : value_defer_call_absorbs(func, receiver, args, skip, ctx.get_func() !>)
    // Native indexing helpers implement storage access, not a source-level call.
    if check_pointer_args : require_void_pointer_call(func, callee, args, scope, at) !>
}

// A `...` argument keeps its own type; C default promotions apply.
fn value_variadic_extern_arg(av: Value, ctx: Context, at: Exp) Value !Error {
    let t = av.rett.get_non_nullable()
    if t.kind == TYPE.ptr_of || t.is_raw_pointer() : return av
    if t.is_float_type() {
        if t.number_byte_size() < 8 : return vgen_cast(av, type_from_core(ctx.build, "f64") !>)
        return av
    }
    if t.is_integer_type() || t.is_bool_type() {
        if t.number_byte_size() < 4 : return vgen_cast(av, type_from_core(ctx.build, "i32") !>)
        return av
    }
    at.error("Variadic extern arguments must be integers, floats or raw pointers; got '" + av.rett.compat_display_name() + "'")
}

////////////////////////////
// String literals
////////////////////////////

// Strip surrounding quotes from a string/char token (keeps escapes as written).
fn strip_quotes(s: String) String {
    if s.length >= 2 {
        let first = s.get(0)
        let last = s.get(s.length - 1)
        if (first == '"' && last == '"') || (first == '\'' && last == '\'') {
            return s.part(1, s.length - 2)
        }
    }
    return s
}

fn value_from_string_exp(exp: Exp, scope: Scope, ctx: Context) Value !Error {
    let string_type = type_string(ctx.build)
    let inner = strip_quotes(exp.str1)
    if exp.bool1 || !inner.contains("%") {
        let text = exp.bool1 ? inner : string_literal_unescape(inner, exp) !>
        let v = vgen_string(text, string_type)
        v.exp = exp
        return v
    }

    let result = vgen_string("", string_type)
    result.exp = exp
    let literal_start: uint = 0
    let i: uint = 0
    while i < inner.length {
        if inner.get(i) == '\\' && i + 1 < inner.length {
            i += 2
            continue
        }
        if inner.get(i) != '%' {
            i++
            continue
        }

        let marker = i
        let expr_start = i + 1
        let expr_end = expr_start
        if expr_start >= inner.length {
            i++
            continue
        }

        if inner.get(expr_start) == '{' {
            expr_start++
            expr_end = expr_start
            expr_end = interpolation_end(inner, expr_start)
            if expr_end >= inner.length : exp.error("Missing '}' in string interpolation")
        } else {
            let ch = inner.get(expr_end)
            if !ch.is_alpha() && ch != '_' {
                i++
                continue
            }
            expr_end++
            while expr_end < inner.length {
                let next = inner.get(expr_end)
                if !next.is_alpha() && !next.is_number() && next != '_' : break
                expr_end++
            }
        }

        if marker > literal_start {
            let literal = string_literal_unescape(inner.part(literal_start, marker - literal_start), exp) !>
            let added = value_try_add_hook(result, vgen_string(literal, string_type), scope, ctx, exp) !>
            if !isset(added) : exp.bug("String does not have a compatible '$add' hook")
            result = added
        }

        let expression = inner.part(expr_start, expr_end - expr_start)
        let quote_len: u32 = exp.str1.length > inner.length ? 1 : 0
        let source = Source {
            build: ctx.build
            content: expression
            path: exp.src.path
            parent: exp.src
            parent_offset: exp.i + quote_len + expr_start.to(u32)
            ns: exp.src.ns
            id: exp.src.id
            ignore_access: exp.src.ignore_access
        }
        let parser = source.start().init_parser()
        let value_exp = read_value_exp(parser, true, true) !>
        let part = value_from_exp(value_exp, scope, ctx.without_suggested_type()) !>
        part = value_try_convert(part, scope, ctx, string_type) !>
        string_type.compat_check(part.rett, exp) !>
        let added = value_try_add_hook(result, part, scope, ctx, exp) !>
        if !isset(added) : exp.bug("String does not have a compatible '$add' hook")
        result = added

        if inner.get(expr_start - 1) == '{' {
            i = expr_end + 1
        } else {
            i = expr_end
        }
        literal_start = i
    }

    if literal_start < inner.length {
        let literal = string_literal_unescape(inner.part(literal_start, inner.length - literal_start), exp) !>
        let added = value_try_add_hook(result, vgen_string(literal, string_type), scope, ctx, exp) !>
        if !isset(added) : exp.bug("String does not have a compatible '$add' hook")
        result = added
    }
    result.exp = exp
    return result
}

// Index of the '}' closing the `%{` whose body starts at `i`, or the length
// when it is missing. Braces inside nested strings and chars do not count.
fn interpolation_end(inner: String, i: uint) uint {
    let len = inner.length
    let depth: uint = 1
    while i < len {
        let ch = inner.get(i)
        if ch == '"' {
            i++
            while i < len {
                let c = inner.get(i++)
                if c == '\\' : i++
                else if c == '"' : break
            }
            continue
        }
        if ch == '\'' {
            i += (i + 1 < len && inner.get(i + 1) == '\\') ? 4 : 3
            continue
        }
        if ch == '{' : depth++
        else if ch == '}' {
            if --depth == 0 : return i
        }
        i++
    }
    return len
}

// Decodes the escapes of a string literal; the tokenizer only skipped them.
fn string_literal_unescape(text: String, exp: Exp) String !Error {
    if !text.contains("\\") : return text
    let buf = ByteBuffer.new()
    let i: uint = 0
    while i < text.length {
        let ch = text.get(i++)
        if ch != '\\' {
            buf.write_byte(ch)
            continue
        }
        if i >= text.length : exp.error("Missing escape character after '\\' in string")
        let e = text.get(i++)
        if e == 'n' : buf.write_byte('\n')
        else if e == 'r' : buf.write_byte('\r')
        else if e == 't' : buf.write_byte('\t')
        else if e == 'f' : buf.write_byte('\f')
        else if e == 'b' : buf.write_byte('\b')
        else if e == 'v' : buf.write_byte('\v')
        else if e == 'a' : buf.write_byte('\a')
        else if e == 'e' : buf.write_byte(27)
        else if e == '0' : buf.write_byte(0)
        else if e == '\\' || e == '"' || e == '\'' || e == '%' : buf.write_byte(e)
        else if e == 'x' {
            let hex = text.part(i, 2)
            if hex.length != 2 || !hex.get(0).is_hex() || !hex.get(1).is_hex() : exp.error("Escape '\\x' needs two hex digits, like '\\x1f'")
            buf.write_byte((hex.hex_to_uint() ! exp.bug("Invalid hex escape")).to(u8))
            i += 2
        } else if e == 'u' {
            if i >= text.length || text.get(i) != '{' : exp.error("Escape '\\u' needs a code point in braces, like '\\u{1F600}'")
            let end = i + 1
            while end < text.length && text.get(end) != '}' : end++
            if end >= text.length : exp.error("Missing '}' after '\\u{' in string")
            let hex = text.part(i + 1, end - i - 1)
            if hex.length == 0 || hex.length > 6 : exp.error("Escape '\\u{...}' needs 1 to 6 hex digits")
            let code = hex.hex_to_uint() ! exp.error("Invalid hex digits in '\\u{" + hex + "}'")
            if code > 0x10FFFF || (code >= 0xD800 && code <= 0xDFFF) : exp.error("Code point '\\u{" + hex + "}' is not a valid unicode scalar value")
            write_utf8_bytes(buf, code.to(u32))
            i = end + 1
        } else {
            exp.error("Unknown escape sequence '\\" + e.to_ascii_string() + "' in string")
        }
    }
    return buf.to_string()
}

fn write_utf8_bytes(buf: ByteBuffer, code: u32) {
    if code < 0x80 {
        buf.write_byte(code.to(u8))
    } else if code < 0x800 {
        buf.write_byte((0xC0 | (code >> 6)).to(u8))
        buf.write_byte((0x80 | (code & 0x3F)).to(u8))
    } else if code < 0x10000 {
        buf.write_byte((0xE0 | (code >> 12)).to(u8))
        buf.write_byte((0x80 | ((code >> 6) & 0x3F)).to(u8))
        buf.write_byte((0x80 | (code & 0x3F)).to(u8))
    } else {
        buf.write_byte((0xF0 | (code >> 18)).to(u8))
        buf.write_byte((0x80 | ((code >> 12) & 0x3F)).to(u8))
        buf.write_byte((0x80 | ((code >> 6) & 0x3F)).to(u8))
        buf.write_byte((0x80 | (code & 0x3F)).to(u8))
    }
}

// Best-effort single-byte value from a char token like 'a' or '\n'.
fn char_literal_byte(s: String) int {
    let inner = strip_quotes(s)
    if inner.length == 0 : return 0
    if inner.get(0) == '\\' && inner.length >= 2 {
        let e = inner.get(1)
        if e == 'n' : return '\n'
        if e == 't' : return '\t'
        if e == 'r' : return '\r'
        if e == 'b' : return 8
        if e == 'f' : return 12
        if e == 'a' : return 7
        if e == 'v' : return 11
        if e == '0' : return 0
        if e == '\\' : return '\\'
        if e == '\'' : return '\''
        if e == '"' : return '"'
        return e
    }
    return inner.get(0)
}
