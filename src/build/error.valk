
error InitError (error) payload { msg: String }
error Error (error, stop)

enum MSG {
    error
    bug
    warn
}

class Message {
    type: MSG
    msg: String
    line: uint (0)
    col: uint (0)
    src: ?Source
    exp: ?Exp
}