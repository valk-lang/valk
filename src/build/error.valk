
use valk:os

exit_fn build_error(msg: String) {
    println("# Build error")
    println("-----------------------------")
    println(msg)
    println("-----------------------------")
    os:exit(1)
}
