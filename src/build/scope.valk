
class Scope {
    parent: ?Scope
    identifiers: ?Map[Idf]

    static fn new(parent: ?Scope (null)) SELF {
        return SELF {
            parent: parent
        }
    }

    fn sub_scope() SELF {
        return SELF {
            parent: this
        }
    }
}