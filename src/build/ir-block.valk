
use valk:utils

class IRBlock {
    name: String
    code: utils:ByteBuffer (utils:ByteBuffer.new(128))

    static fn new(name: String) IRBlock {
        return IRBlock {
            name: name
        }
    }
}
