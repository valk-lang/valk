
class Target {
    arch: String
    os: String
    host_arch: String
    host_os: String

    static fn new() SELF {
        return SELF {
            arch: "x64"
            os: "linux"
            host_arch: "x64"
            host_os: "linux"
        }
    }

    fn set(t: String) {
    }
}