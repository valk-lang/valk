
fn stage_types(b: Build) {

    each b.units as unit {
        each unit.functions as func {
            parse_func_args(func)
        }
    }
}

fn parse_func_args(func: Func) {
    let p = Parser.new(func.chunk_args, func.unit, null)
}
