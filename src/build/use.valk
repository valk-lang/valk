class Use {
    unit: Unit
    ns: Namespace
    exp: Exp
    // Target id expression (namespace / path)
    id: ?Exp (null)
    // Optional `as alias` name
    alias: ?String (null)
    is_used: bool (false)
}
