
class Str {
    unit: Unit
    body: String
    nr: uint
    is_used: bool (false)

    fn used_by_scope(scope: Scope) {
        let func = scope.get_func() ! return
        func.strings.append(this)
    }
}
