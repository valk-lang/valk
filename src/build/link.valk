// Linker input collected from a top-level `link` declaration.
// `type` is LINK_KIND (auto is resolved to dynamic/static at parse time).
class Link {
    unit: Unit
    ns: Namespace
    exp: Exp
    type: LINK_KIND
    lib: String
}
