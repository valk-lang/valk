
class Context {
    build: Build
    func: Func
    fast: FuncAst

    static fn for_func(func: Func) SELF {
        let ctx = SELF {
            build: func.build
            func: func
            fast: func.ast()
        }
        return ctx
    }

    // fn uses_func(func: Func) {
    //     let infunc = this.func
    //     let fast = infunc.ast()
    // }
}
