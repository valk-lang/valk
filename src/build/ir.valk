// Function IR generation boundary.
//
// The LLVM implementation will live here. Keeping this call in the pipeline
// now gives function ASTs a clear lifetime: semantic lowering finishes, IR is
// emitted, and the body graph can then be released.
fn generate_func_ir(func: Func) !Error {
    // Placeholder until the new LLVM backend is implemented.
}

