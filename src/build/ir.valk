
struct IR {
    unit: Unit
    build: Build
    functions: Array[IRFunc] (array[IRFunc]{})

    static fn new(u: Unit) IR {
        let ir = IR {
            unit: u
            build: u.build
        }
        return ir
    }
}
