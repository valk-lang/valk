// Declaration macros (`macro name scope(...) …`) were removed.
// Collection init uses class_init forms + `$append` / `$set_key` hooks instead.
//
// Compile-time `#if` / `#elif` / `#else` / `#end` / `#loop` / … live in compile-macro.valk.
