
fn stage_props(b: Build) {

    each b.units as unit {
        each unit.classes as class {
            class_parse_props(class)
        }
    }
}


fn class_parse_props(class: Class) {
    let p = Parser.new(class.chunk, class.unit, null)
}
