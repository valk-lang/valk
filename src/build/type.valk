// Resolved type kind (from EXP.type forms and class kinds).
enum TYPE {
    void
    class      // class / struct / slice / mode / etc. (class field set)
    int
    float
    bool
    ptr        // *T or *[]
    ref        // &T or &[]
    nullable   // ?T
    inline     // <T>
    array      // [T x N]
    multi      // (T, U, ...)
    union      // A | B (inline tagged value)
    fn         // fn(...)(...)
    fnptr      // fnptr(...)(...)
    co         // co(...)
    any
    // Error code value: `.code`, `TypeName.code`, or a caught error binding (`E`).
    // error_type identifies which ErrorType; error_is_instance distinguishes a caught
    // error binding (payload fields readable) from a plain comparable code value.
    error_code
}

// Compiler Type object produced from EXP.type (and similar).
//
// Immutable after creation: type-defining fields are never reassigned.
// Only cache_* / size slots may be filled later so wrappers and sizes can be reused.
class Type {
    type: TYPE
    build: Build
    // Linked class when type is a class/struct/integer/float/boolean/pointer/mode
    class: ?Class (null)
    // Linked enum when this Type is an enum (or item type tagged as enum)
    enum: ?Enum (null)
    // TYPE.error_code: which ErrorType, and whether this is a caught-error binding
    // (payload fields readable) vs a plain comparable code value.
    error_type: ?ErrorType (null)
    error_is_instance: bool (false)
    // Wrapper / element type (?, *, &, inline, array, ...)
    sub_type: ?Type (null)
    // Multi / generics / fn args list
    sub_types: ?Array[Type] (null)
    // Fixed array size, or pointer-info size when relevant
    array_size: u32 (0)
    // Display name (e.g. class name)
    custom_name: String ("")
    // True for *[] / &[] forms
    is_slice_ptr: bool (false)
    // ---- size cache (filled once by size()) ----
    cached_size: u32 (0)
    size_known: bool (false)
    size_computing: bool (false) // true while resolving; detects recursive deps
    // ---- GC-root count cache (filled once the type graph is complete) ----
    cached_gc_root_count: uint (0)
    gc_root_count_known: bool (false)
    // ---- managed-reference cache (same lifetime rule as the size cache) ----
    cached_has_gc_refs: bool (false)
    has_gc_refs_known: bool (false)
    // ---- immutable string conversions ----
    cached_display_name: ?String (null)
    cached_identity_key: ?String (null)
    cached_ir_type: ?String (null)
    cached_error_abi_type: ?String (null)
    // ---- wrapper caches (mutable slots; the Type itself stays fixed) ----
    cache_nullable: ?Type (null)
    cache_pointer: ?Type (null)
    cache_ref: ?Type (null)
    cache_inline: ?Type (null)

    // Target pointer size in bytes
    fn ptr_size() u32 {
        return this.build.options.target.ptr_size.to(u32)
    }

    // Byte size of a value of this type. Cached after the first successful resolve.
    // Throws if a class layout cannot be resolved due to recursive dependencies.
    fn size() u32 !Error {
        if this.size_known : return this.cached_size
        if this.size_computing {
            this.build.error("Cannot determine type size due to recursive type dependencies" + this.size_error_label())
        }
        this.size_computing = true
        let s = this.compute_size() !>
        this.cached_size = s
        this.size_known = true
        this.size_computing = false
        return s
    }

    fn size_error_label() String {
        if this.custom_name.length > 0 : return ": " + this.custom_name
        let c = this.class
        if isset(c) : return ": " + c.name
        return ""
    }

    // Compute size without caching (caller handles cache / recursion flag).
    fn compute_size() u32 !Error {
        let t = this.type
        let psz = this.ptr_size()

        if t == TYPE.void : return 0
        if t == TYPE.any : return psz

        // Pointer-sized type kinds
        if t == TYPE.ptr || t == TYPE.ref || t == TYPE.fnptr || t == TYPE.co {
            return psz
        }
        if t == TYPE.fn : return psz * 2

        // Tagged unions are ordinary value aggregates. Their fixed header is a
        // u64 tag, followed by an optional direct-GC slot and an optional byte
        // payload large enough for the largest remaining alternative.
        if t == TYPE.union {
            let size: u32 = 8
            if this.union_has_gc_payload() : size += psz
            size += this.union_other_payload_size() !>
            if size % psz != 0 : size += psz - (size % psz)
            return size
        }

        if t == TYPE.nullable {
            let sub = this.sub_type
            if !isset(sub) : return psz
            // Nullable of a pointer-sized type stays pointer-sized
            if sub.is_pointer_sized() : return psz
            // Closures use a null function pointer as their empty state.
            if sub.type == TYPE.fn : return sub.size() !>
            // The presence field occupies a pointer-sized header even though
            // LLVM represents the flag itself as i1. Round the complete value
            // like LLVM's natural struct layout so fixed-array strides agree.
            let size = psz + (sub.size() !>)
            let alignment = this.ir_alignment() !>
            if alignment > 1 : size = size.round_up(alignment)
            return size
        }

        if t == TYPE.inline {
            let sub = this.sub_type
            if !isset(sub) : this.build.bug("Inline type is missing sub_type")
            let c = sub.get_class_or_null()
            if isset(c) && c.kind == CLASS_KIND.mode {
                return c.compute_size() !>
            }
            if isset(c) && (c.kind == CLASS_KIND.struct || c.kind == CLASS_KIND.class) {
                return c.compute_body_size() !>
            }
            return sub.size() !>
        }

        if t == TYPE.array {
            let sub = this.sub_type
            if !isset(sub) : this.build.bug("Array type is missing sub_type")
            let n = this.array_size
            if n == 0 : return 0
            return sub.size() !> * n
        }

        if t == TYPE.multi {
            let subs = this.sub_types
            if !isset(subs) : this.build.bug("Multi type is missing sub_types")
            let total: u32 = 0
            each subs as st {
                total = st.ir_align_offset(total) !>
                total += st.size() !>
            }
            let alignment = this.ir_alignment() !>
            if alignment > 1 : total = total.round_up(alignment)
            return total
        }

        // Class-backed types (class / struct / int / float / bool / pointer / …)
        let c = this.class
        if isset(c) {
            // Named class/struct values are pointers. Their Class still owns and
            // computes the full object layout for allocation and `<T>`.
            if c.kind == CLASS_KIND.struct || c.kind == CLASS_KIND.class || c.kind == CLASS_KIND.mode : return psz
            return c.compute_size() !>
        }

        // TYPE.int / float / bool / class without class pointer — should not happen
        if t == TYPE.int || t == TYPE.float || t == TYPE.bool {
            return psz
        }
        if t == TYPE.class {
            return psz
        }

        return 0
    }

    // Whether this union needs the dedicated pointer field used by alternatives
    // whose value is itself a managed reference. Keeping it separate from the
    // raw payload lets the collector visit it without interpreting scalar bits.
    fn union_has_gc_payload() bool {
        let t = this.get_non_nullable()
        if t.type != TYPE.union : return false
        let members = t.sub_types
        if !isset(members) : return false
        each members as member {
            if member.is_gc_type() : return true
        }
        return false
    }

    fn union_has_other_payload() bool {
        let t = this.get_non_nullable()
        if t.type != TYPE.union : return false
        let members = t.sub_types
        if !isset(members) : return false
        each members as member {
            if !member.is_gc_type() : return true
        }
        return false
    }

    // Size of the shared byte payload. Managed references use their dedicated
    // pointer field; every other alternative is copied into this region.
    fn union_other_payload_size() u32 !Error {
        let t = this.get_non_nullable()
        if t.type != TYPE.union : return 0
        let members = t.sub_types
        if !isset(members) : return 0
        let largest: u32 = 0
        each members as member {
            if member.is_gc_type() : continue
            let size = member.size() !>
            if size > largest : largest = size
        }
        return largest
    }

    fn union_gc_payload_offset() u32 {
        return 8
    }

    fn union_other_payload_offset() u32 {
        let t = this.get_non_nullable()
        return 8 + (t.union_has_gc_payload() ? t.ptr_size() : 0)
    }

    fn union_gc_payload_index() uint {
        return 1
    }

    fn union_other_payload_index() uint {
        let t = this.get_non_nullable()
        return t.union_has_gc_payload() ? 2 : 1
    }

    // ABI alignment of the LLVM value produced by ir_type(). Keep this
    // separate from class-body layout: inline class bodies are emitted as byte
    // arrays and therefore have LLVM alignment 1, while a natural multi-value
    // must use the exact alignment LLVM assigns to each member.
    fn ir_alignment() u32 !Error {
        let t = this.get_non_nullable()
        let psz = this.ptr_size()

        if t.is_pointer_sized() || t.type == TYPE.fn : return psz
        if t.type == TYPE.void || t.type == TYPE.bool || t.type == TYPE.inline : return 1
        if t.type == TYPE.error_code : return 4

        if t.type == TYPE.union {
            let alignment = psz < 8 ? psz : 8
            if t.union_has_gc_payload() && psz > alignment : alignment = psz
            return alignment
        }
        if t.type == TYPE.array {
            let sub = t.sub_type
            if !isset(sub) : return 1
            return sub.ir_alignment() !>
        }
        if t.type == TYPE.multi {
            let members = t.sub_types
            if !isset(members) : return 1
            let largest: u32 = 1
            each members as member {
                let alignment = member.ir_alignment() !>
                if alignment > largest : largest = alignment
            }
            return largest
        }

        let size = t.size() !>
        if size == 0 : return 1
        let alignment: u32 = 1
        while alignment < size && alignment < psz : alignment *= 2
        return alignment > psz ? psz : alignment
    }

    // Align a byte offset exactly as LLVM does before placing this type in a
    // natural struct. LLVM alignments are powers of two, but round_up also
    // keeps this helper correct for every target-supported scalar size.
    fn ir_align_offset(offset: u32) u32 !Error {
        let alignment = this.ir_alignment() !>
        if alignment <= 1 : return offset
        return offset.round_up(alignment)
    }

    // Preferred alignment when the compiler controls a byte-backed buffer
    // (GC frames, coroutine storage, globals). Unlike ir_alignment(), this
    // looks through inline byte arrays so embedded managed pointers can still
    // land on naturally aligned addresses whenever their field offsets allow.
    fn storage_alignment() u32 !Error {
        // Resolve the byte size first so invalid recursive inline layouts fail
        // with the normal diagnostic instead of recursing through this walk.
        this.size() !>
        let t = this.get_non_nullable()
        if t.type == TYPE.inline {
            let sub = t.sub_type
            if !isset(sub) : return 1
            let class = sub.get_class_or_null()
            if !isset(class) : return sub.storage_alignment() !>
            let largest: u32 = 1
            each class.props as prop {
                let alignment = prop.type.storage_alignment() !>
                if alignment > largest : largest = alignment
            }
            return largest
        }
        if t.type == TYPE.array {
            let sub = t.sub_type
            if !isset(sub) : return 1
            return sub.storage_alignment() !>
        }
        if t.type == TYPE.multi {
            let members = t.sub_types
            if !isset(members) : return 1
            let largest: u32 = 1
            each members as member {
                let alignment = member.storage_alignment() !>
                if alignment > largest : largest = alignment
            }
            return largest
        }
        if t.type == TYPE.union {
            let largest = this.ir_alignment() !>
            let members = t.sub_types
            if isset(members) {
                each members as member {
                    let alignment = member.storage_alignment() !>
                    if alignment > largest : largest = alignment
                }
            }
            return largest
        }
        return this.ir_alignment() !>
    }

    // True if this type is stored as a pointer-sized word (no recursive layout).
    fn is_pointer_sized() bool {
        let t = this.type
        if t == TYPE.ptr || t == TYPE.ref || t == TYPE.fnptr || t == TYPE.co || t == TYPE.any {
            return true
        }
        if t == TYPE.nullable {
            let sub = this.sub_type
            if isset(sub) : return sub.is_pointer_sized()
            return true
        }
        // Named class/struct/slice/mode/pointer values are reference-sized.
        let c = this.class
        if isset(c) {
            let kind = c.kind
            if kind == CLASS_KIND.class || kind == CLASS_KIND.interface || kind == CLASS_KIND.struct || kind == CLASS_KIND.slice || kind == CLASS_KIND.mode || kind == CLASS_KIND.pointer {
                return true
            }
        }
        return false
    }

    // Alignment unit used by the classic class layout algorithm. Structural
    // inline values align to their largest field (capped at pointer size);
    // arrays align to their element, and scalar/reference values to their own
    // size (also capped at pointer size).
    fn largest_property_offset() u32 !Error {
        let psz = this.ptr_size()
        let largest = this.size() !>

        if !this.is_pointer_sized() {
            if this.type == TYPE.inline {
                let sub = this.sub_type
                if isset(sub) {
                    let c = sub.get_class_or_null()
                    if isset(c) {
                        largest = 0
                        each c.props as prop {
                            let prop_size = prop.type.size() !>
                            if prop_size > largest : largest = prop_size
                        }
                    }
                }
            } else if this.type == TYPE.array {
                let sub = this.sub_type
                if isset(sub) : largest = sub.size() !>
            }
        }

        if largest > psz : return psz
        return largest
    }

    // ---- compile-time meta queries (`T.$is_*`, `T.$has_*`, …) ----

    // Strip one nullable layer (`?T` → `T`). Used by `T.$notnull`.
    fn get_non_nullable() Type {
        if this.type != TYPE.nullable : return this
        let sub = this.sub_type
        if isset(sub) : return sub
        return this
    }

    // Class backing this type when known (unwraps nullable / inline / one ptr-ref layer).
    fn get_class_or_null() ?Class {
        let t = this.get_non_nullable()
        let c = t.class
        if isset(c) : return c
        // `<Struct>` stores the class on the inner type
        if t.type == TYPE.inline {
            let sub = t.sub_type
            if isset(sub) : return sub.get_class_or_null()
        }
        if t.type == TYPE.ptr || t.type == TYPE.ref {
            let sub = t.sub_type
            if isset(sub) : return sub.get_class_or_null()
        }
        return null
    }

    // True if this type is nullable (`?T`). Used by `T.$is_nullable`.
    fn is_nullable_type() bool {
        return this.type == TYPE.nullable
    }

    // True if values of this type are stored inline (not a pointer-sized reference).
    // Used by compile-time meta-properties such as `T.$is_inline`.
    fn is_inline_type() bool {
        let t = this.type
        if t == TYPE.array || t == TYPE.inline || t == TYPE.multi || t == TYPE.union : return true
        if t == TYPE.int || t == TYPE.float || t == TYPE.bool : return true
        if t == TYPE.fn : return true
        if t == TYPE.ptr || t == TYPE.ref || t == TYPE.fnptr || t == TYPE.co || t == TYPE.any || t == TYPE.void {
            return false
        }
        if t == TYPE.nullable {
            let sub = this.sub_type
            if isset(sub) : return sub.is_inline_type()
            return false
        }
        // Named structs/classes are pointer-backed. Their explicit `<T>` wrapper
        // is handled above as TYPE.inline.
        let c = this.class
        if isset(c) {
            let kind = c.kind
            if kind == CLASS_KIND.integer || kind == CLASS_KIND.float || kind == CLASS_KIND.boolean : return true
            // class / slice / mode / pointer are reference types
            return false
        }
        return false
    }

    // `T.$is_inline` compile-time meta-property, as used by the standard library
    // (e.g. `Array.sort`'s `#if !T.$is_inline`). Unlike `is_inline_type()` — the
    // internal "stored by value, not GC/pointer-boxed" concept, which also covers plain
    // scalars — this means "a large-ish value type that isn't a trivially-swapped
    // scalar": struct / fixed array / inline, but not int / float / bool.
    fn is_meta_inline_type() bool {
        let t = this.type
        if t == TYPE.int || t == TYPE.float || t == TYPE.bool : return false
        if t == TYPE.nullable {
            let sub = this.sub_type
            if isset(sub) : return sub.is_meta_inline_type()
            return false
        }
        let c = this.class
        if isset(c) {
            let kind = c.kind
            if kind == CLASS_KIND.integer || kind == CLASS_KIND.float || kind == CLASS_KIND.boolean : return false
        }
        return this.is_inline_type()
    }

    fn is_void_type() bool {
        return this.get_non_nullable().type == TYPE.void
    }

    fn is_integer_type() bool {
        let t = this.get_non_nullable()
        if t.type == TYPE.int : return true
        let c = t.class
        return isset(c) && c.kind == CLASS_KIND.integer
    }

    fn is_float_type() bool {
        let t = this.get_non_nullable()
        if t.type == TYPE.float : return true
        let c = t.class
        return isset(c) && c.kind == CLASS_KIND.float
    }

    fn is_bool_type() bool {
        let t = this.get_non_nullable()
        if t.type == TYPE.bool : return true
        let c = t.class
        return isset(c) && c.kind == CLASS_KIND.boolean
    }

    // Signed integer (or other class with `signed` set).
    fn is_signed_type() bool {
        let c = this.get_class_or_null()
        if isset(c) : return c.signed
        return false
    }

    // struct or class (layout-bearing object types).
    fn is_structural_type() bool {
        let c = this.get_class_or_null()
        if !isset(c) : return false
        return c.kind == CLASS_KIND.struct || c.kind == CLASS_KIND.class
    }

    // GC-managed object reference.
    //
    // A bare named class/struct type is the language's managed pointer to that
    // object's storage. Explicit pointer/reference wrappers are borrows or raw
    // addresses, not managed references themselves:
    //
    //   MyClass    GC-managed object reference
    //   <MyClass>  inline object body
    //   *MyClass   explicit/raw pointer to a MyClass value
    //   &MyClass   borrowed reference to a MyClass value
    //
    // The legacy compiler represented the first case as TYPE.ptr(TYPE.class).
    // The rewrite represents it directly as TYPE.class, so do not recursively
    // inherit GC-ness through TYPE.ptr / TYPE.ref.
    fn is_gc_type() bool {
        let t = this.get_non_nullable()
        // A coroutine handle is represented by a managed `coro:Coro` object
        // even though it has its own surface type. Containers and locals that
        // hold `co` values must therefore trace/root the pointer just like a
        // class reference.
        if t.type == TYPE.co : return true
        if t.type != TYPE.class : return false
        let c = t.class
        if !isset(c) : return false
        let kind = c.kind
        if kind == CLASS_KIND.class || kind == CLASS_KIND.interface || kind == CLASS_KIND.slice : return true
        if kind == CLASS_KIND.mode {
            let mode_for = c.mode_for_class
            if isset(mode_for) : return mode_for.kind == CLASS_KIND.class
        }
        return false
    }

    // True when a value's representation contains one or more managed
    // pointers. Unlike is_gc_type(), this also covers by-value aggregates such
    // as closures, inline objects, fixed arrays, and tagged unions.
    // Asked for nearly every resolved value and every field an IR walk visits,
    // and answering it walks the whole aggregate.
    fn has_gc_refs() bool {
        if this.has_gc_refs_known : return this.cached_has_gc_refs
        let result = this.compute_has_gc_refs()
        this.cached_has_gc_refs = result
        this.has_gc_refs_known = true
        return result
    }

    fn compute_has_gc_refs() bool {
        if this.is_gc_type() : return true
        let t = this.get_non_nullable()
        if t.type == TYPE.fn : return true
        if t.type == TYPE.union || t.type == TYPE.multi {
            let members = t.sub_types
            if !isset(members) : return false
            each members as member {
                if member.has_gc_refs() : return true
            }
            return false
        }
        if t.type == TYPE.array {
            let sub = t.sub_type
            return isset(sub) && t.array_size > 0 && sub.has_gc_refs()
        }
        if t.type != TYPE.inline : return false
        let sub = t.sub_type
        if !isset(sub) : return false
        let class = sub.get_class_or_null()
        if !isset(class) : return sub.has_gc_refs()
        each class.props as prop {
            if prop.type.has_gc_refs() : return true
        }
        return false
    }

    // Maximum number of simultaneous GC roots held by this value. A tagged
    // union uses the maximum alternative count because exactly one tag is
    // active; ordinary aggregates sum their independent fields.
    fn gc_root_count() uint !Error {
        if this.gc_root_count_known : return this.cached_gc_root_count
        let count = this.compute_gc_root_count() !>
        this.cached_gc_root_count = count
        this.gc_root_count_known = true
        return count
    }

    fn compute_gc_root_count() uint !Error {
        if this.is_gc_type() : return 1
        let t = this.get_non_nullable()
        if t.type == TYPE.fn : return 1
        if t.type == TYPE.union {
            let members = t.sub_types
            if !isset(members) : return 0
            let largest: uint = 0
            each members as member {
                let count = member.gc_root_count() !>
                if count > largest : largest = count
            }
            return largest
        }
        if t.type == TYPE.array {
            let sub = t.sub_type
            if !isset(sub) : return 0
            return (sub.gc_root_count() !>) * t.array_size.to(uint)
        }
        if t.type == TYPE.multi {
            let members = t.sub_types
            if !isset(members) : return 0
            let count: uint = 0
            each members as member : count += member.gc_root_count() !>
            return count
        }
        if t.type != TYPE.inline : return 0
        let sub = t.sub_type
        if !isset(sub) : return 0
        let class = sub.get_class_or_null()
        if !isset(class) : return sub.gc_root_count() !>
        let count: uint = 0
        each class.props as prop : count += prop.type.gc_root_count() !>
        return count
    }

    // Pointer-like storage: explicit ptr/ref/fn, or reference-class kinds.
    fn is_pointer_type() bool {
        let t = this.type
        if t == TYPE.ptr || t == TYPE.ref || t == TYPE.fn || t == TYPE.fnptr || t == TYPE.co {
            return true
        }
        if t == TYPE.nullable {
            let sub = this.sub_type
            if isset(sub) : return sub.is_pointer_type()
            return false
        }
        let c = this.class
        if isset(c) {
            let kind = c.kind
            return kind == CLASS_KIND.class || kind == CLASS_KIND.interface || kind == CLASS_KIND.struct || kind == CLASS_KIND.slice || kind == CLASS_KIND.mode || kind == CLASS_KIND.pointer
        }
        return false
    }

    // Bare `ptr` (CLASS_KIND.pointer / void-style pointer), not `*T`.
    fn is_raw_pointer_type() bool {
        let t = this.get_non_nullable()
        if t.type == TYPE.ptr {
            // `*T` / `*[T]` always carry a pointed-to sub_type.
            return !isset(t.sub_type)
        }
        // Named pointer classes such as `cstring` have the same raw pointer
        // representation and can fall back to pointer compatibility after
        // conversion hooks have had a chance to run.
        let c = t.class
        return isset(c) && c.kind == CLASS_KIND.pointer
    }

    // True if a `_VTABLE` property is already present on the class.
    fn has_vtable() bool {
        let c = this.get_class_or_null()
        if !isset(c) : return false
        each c.props as prop {
            let n = prop.name
            if isset(n) && n == "_VTABLE" : return true
        }
        return false
    }

    // Same type identity for compile-time `T.$is_type_of(U)`. Class-backed
    // values retain the original mode-aware behavior; anonymous structural
    // types such as tagged unions need their canonical identity key because
    // they have no Class object to compare. Named unions use their Class.
    fn is_type_of_class(other: Type) bool {
        let ca = this.compat_class()
        let cb = other.compat_class()
        if isset(ca) || isset(cb) : return isset(ca) && isset(cb) && ca == cb
        return type_identity_key(this) == type_identity_key(other)
    }

    // Class used for identity checks — unwraps `mode X for Y` to Y.
    fn compat_class() ?Class {
        let c = this.get_class_or_null()
        if !isset(c) : return null
        let mode_for = c.mode_for_class
        if isset(mode_for) : return mode_for
        return c
    }

    // True for integer / float / bool (including class-backed number types).
    fn is_number_type() bool {
        return this.is_integer_type() || this.is_float_type() || this.is_bool_type()
    }

    // Byte size used for number compatibility (no full layout walk).
    // `*` integer/float sizes use pointer size.
    fn number_byte_size() u32 {
        let t = this.get_non_nullable()
        let c = t.class
        if isset(c) {
            let kind = c.kind
            if kind == CLASS_KIND.integer || kind == CLASS_KIND.float {
                let s = c.number_size
                if s == 0 : return t.ptr_size()
                return s
            }
            if kind == CLASS_KIND.boolean : return 1
        }
        if t.type == TYPE.bool : return 1
        // Bare TYPE.int / float without class — treat as pointer-sized host int
        if t.type == TYPE.int || t.type == TYPE.float : return t.ptr_size()
        // Fallback: cached/compute size when available
        let s = t.size() ! return 0
        return s
    }

    // Can a value of type `other` be passed where `this` is expected?
    // (Mirrors old `Type.compat` — assignability / parameter matching.)
    //
    // Numbers: same kind (int/float/bool) and same byte size; integers also need
    // matching signedness (old rule). Classes compare by identity (modes unwrap).
    fn compat(other: Type, ignore_imut: bool (false)) bool {
        if this == other : return true

        // `any` is permissive (docs / incomplete generics)
        if this.type == TYPE.any || other.type == TYPE.any : return true

        let base = this
        let typ = other

        // Expected `?X` accepts non-null `X`
        if base.is_nullable_type() && !typ.is_nullable_type() {
            base = base.get_non_nullable()
        }

        // Both nullable: compare inners
        if base.is_nullable_type() {
            if !typ.is_nullable_type() : return false
            let bs = base.sub_type
            let ts = typ.sub_type
            if !isset(bs) || !isset(ts) : return false
            return bs.compat(ts, ignore_imut)
        }

        // Expected non-null `X` does not accept `?X`. The value has to be narrowed
        // first (isset / ?? / ?!). Without this the checks below reach a nullable
        // through compat_class() / compat_numbers(), both of which unwrap it, so
        // every `?T` → `T` assignment would silently pass.
        if typ.is_nullable_type() : return false

        // A union accepts one of its alternatives, or the same structural
        // union. Union-to-non-union narrowing is never implicit.
        if base.type == TYPE.union {
            if typ.type == TYPE.union : return base.union_accepts_union(typ)
            return isset(base.union_member_for(typ))
        }
        if typ.type == TYPE.union : return false

        // Bare `ptr` is the void-pointer-style type. It is compatible with typed
        // pointers and references in either direction. When both sides carry
        // pointee metadata, the pointer comparison below still validates it.
        if (base.is_raw_pointer_type() && typ.is_pointer_type())
            || (typ.is_raw_pointer_type() && base.is_pointer_type())
        {
            return true
        }

        // Numbers: same numeric kind + same byte size (not class identity)
        if base.is_number_type() && typ.is_number_type() {
            return base.compat_numbers(typ)
        }

        // Kind mismatch — allow &T ↔ *T (same as old)
        if base.type != typ.type {
            // A single-element `(T)` multi (e.g. a `fn()(T)` return clause) is just `T`.
            if base.type == TYPE.multi {
                let bs = base.sub_types
                if isset(bs) && bs.length == 1 {
                    let only = bs.get(0) !? null
                    if isset(only) : return only.compat(typ, ignore_imut)
                }
            }
            if typ.type == TYPE.multi {
                let ts = typ.sub_types
                if isset(ts) && ts.length == 1 {
                    let only = ts.get(0) !? null
                    if isset(only) : return base.compat(only, ignore_imut)
                }
            }
            if base.type == TYPE.ref && typ.type == TYPE.ptr {
                // fall through to pointer-style sub compare
            } else if base.type == TYPE.ptr && typ.type == TYPE.ref {
                // fall through
            } else if base.type == TYPE.class || typ.type == TYPE.class {
                // class vs non-class already failed number path
                let bc = base.compat_class()
                let tc = typ.compat_class()
                if isset(bc) && isset(tc) && bc == tc : return true
                return false
            } else {
                return false
            }
        }

        // Enums: when the *expected* type is an enum, actual must be the same enum
        // (old rule). A plain underlying type on the expected side still accepts
        // enum-tagged values (e.g. String accepts Enum2:String).
        let be = base.enum
        if isset(be) {
            if be != typ.enum : return false
        }

        // *T / &T — compare pointed-to types
        if base.type == TYPE.ptr || base.type == TYPE.ref {
            let bs = base.sub_type
            let ts = typ.sub_type
            if !isset(bs) || !isset(ts) : return false
            // Pointer-info bounds (`&[T x N]`, `ptr[T]`) do not affect pointer
            // compatibility. They are retained for offset/load checks only.
            return bs.compat(ts, ignore_imut)
        }

        // Inline <T>
        if base.type == TYPE.inline {
            let bs = base.sub_type
            let ts = typ.sub_type
            if !isset(bs) || !isset(ts) : return false
            return bs.compat(ts, ignore_imut)
        }

        // Fixed arrays [T x N]
        if base.type == TYPE.array {
            let bs = base.sub_type
            let ts = typ.sub_type
            if !isset(bs) || !isset(ts) : return false
            // If both sizes known and non-zero, base must not require a larger array
            if base.array_size > 0 && typ.array_size > 0 && base.array_size > typ.array_size {
                return false
            }
            return bs.compat(ts, ignore_imut)
        }

        // Multi (T, U, …)
        if base.type == TYPE.multi {
            let bs = base.sub_types
            let ts = typ.sub_types
            if !isset(bs) || !isset(ts) : return false
            if bs.length != ts.length : return false
            each bs as bt, i {
                let ot = ts.get(i) ! return false
                if !bt.compat(ot, ignore_imut) : return false
            }
            return true
        }

        // Function types — arity and arg/rett compatibility (simplified)
        if base.type == TYPE.fn || base.type == TYPE.fnptr {
            if typ.type != base.type : return false
            let bs = base.sub_types
            let ts = typ.sub_types
            if isset(bs) || isset(ts) {
                if !isset(bs) || !isset(ts) : return false
                if bs.length != ts.length : return false
                // First N-1 are args (contravariant: expected arg accepts actual), last is rett
                // Without a fixed split, require pairwise compat both ways for now
                each bs as bt, i {
                    let ot = ts.get(i) ! return false
                    if !bt.compat(ot, ignore_imut) && !ot.compat(bt, ignore_imut) : return false
                }
            }
            return true
        }

        // co(…) — compare the wrapped return type when both sides declare one
        if base.type == TYPE.co {
            if typ.type != TYPE.co : return false
            let bs = base.sub_type
            let ts = typ.sub_type
            if isset(bs) && isset(ts) : return bs.compat(ts, ignore_imut)
            return true
        }

        // Error code values: same ErrorType (regardless of instance/plain-code flavor)
        if base.type == TYPE.error_code {
            if typ.type != TYPE.error_code : return false
            return base.error_type == typ.error_type
        }

        // Class / struct / slice / mode / pointer-kind: same class identity
        let base_class = base.compat_class()
        let type_class = typ.compat_class()
        if isset(base_class) || isset(type_class) {
            if isset(base_class) && base_class.kind == CLASS_KIND.interface {
                return isset(type_class) && class_implements_interface(type_class, base_class)
            }
            if base_class != type_class : return false
            return true
        }

        // void / other primitives with no class
        if base.type == TYPE.void : return typ.type == TYPE.void

        return true
    }

    // Number-only branch of compat: same kind family + same byte size.
    // e.g. int/i32/u32 of equal width are compatible; float widths must match.
    // (Signedness is not required — only storage size.)
    fn compat_numbers(other: Type) bool {
        let a = this.get_non_nullable()
        let b = other.get_non_nullable()

        if a.is_bool_type() || b.is_bool_type() {
            return a.is_bool_type() && b.is_bool_type()
        }
        if a.is_float_type() || b.is_float_type() {
            if !a.is_float_type() || !b.is_float_type() : return false
            return a.number_byte_size() == b.number_byte_size()
        }
        // integers — size only
        if !a.is_integer_type() || !b.is_integer_type() : return false
        return a.number_byte_size() == b.number_byte_size()
    }

    // Alias used by meta `$is_compatible_with` and payload checks.
    fn is_compatible_with(other: Type) bool {
        return this.compat(other)
    }

    // Report incompatible types at `at` (expected = this, got = other).
    fn compat_check(other: Type, at: Exp) !Error {
        if this.compat(other) : return
        at.error("Incompatible types. Expected '" + this.compat_display_name() + "', got '" + other.compat_display_name() + "'")
    }

    fn compat_display_name() String {
        let cached = this.cached_display_name
        if isset(cached) : return cached
        let name = this.compute_compat_display_name()
        this.cached_display_name = name
        return name
    }

    fn compute_compat_display_name() String {
        // Class-backed types: always prefer pretty name (`Array[String]`, not mangled id)
        let c = this.class
        if isset(c) {
            // Pointer-info types (`ptr[u8]`, `cstring[u8 x 64]`) keep the element
            // type in sub_type and any fixed size in array_size. Class.get_type()
            // never sets sub_type, so this only ever fires for those, and the bare
            // class name would otherwise drop part of the written type.
            let sub = this.sub_type
            if isset(sub) {
                let n = this.array_size
                if n > 0 : return c.pretty_name() + "[" + sub.compat_display_name() + " x " + n + "]"
                return c.pretty_name() + "[" + sub.compat_display_name() + "]"
            }
            return c.pretty_name()
        }
        if this.custom_name.length > 0 : return this.custom_name
        if this.type == TYPE.void : return "void"
        if this.type == TYPE.error_code {
            let et = this.error_type
            return isset(et) ? et.name : "error_code"
        }
        if this.type == TYPE.any : return "any"
        if this.type == TYPE.nullable {
            let sub = this.sub_type
            if isset(sub) : return "?" + sub.compat_display_name()
            return "?"
        }
        if this.type == TYPE.ptr {
            let sub = this.sub_type
            if isset(sub) : return "*" + sub.compat_display_name()
            return "*"
        }
        if this.type == TYPE.ref {
            let sub = this.sub_type
            if isset(sub) : return "&" + sub.compat_display_name()
            return "&"
        }
        if this.type == TYPE.inline {
            let sub = this.sub_type
            if isset(sub) : return "<" + sub.compat_display_name() + ">"
            return "<>"
        }
        if this.type == TYPE.array {
            let sub = this.sub_type
            if isset(sub) {
                let n = this.array_size
                if n > 0 : return "[" + sub.compat_display_name() + " x " + n + "]"
                return "[" + sub.compat_display_name() + "]"
            }
            return "[]"
        }
        if this.type == TYPE.multi {
            let subs = this.sub_types
            if isset(subs) {
                let parts = Array[String]{}
                each subs as st {
                    parts.append(st.compat_display_name())
                }
                return "(" + parts.join(", ") + ")"
            }
            return "()"
        }
        if this.type == TYPE.union {
            let subs = this.sub_types
            if isset(subs) {
                let parts = Array[String]{}
                each subs as st : parts.append(st.compat_display_name())
                return parts.join(" | ")
            }
            return "union"
        }
        if this.type == TYPE.fn || this.type == TYPE.fnptr {
            let name = this.type == TYPE.fn ? "fn" : "fnptr"
            let args = this.sub_types
            let parts = Array[String]{}
            if isset(args) {
                each args as at : parts.append(at.compat_display_name())
            }
            let rett = this.sub_type
            let rett_name = "()"
            if isset(rett) && !rett.is_void_type() {
                // Multi types already include their own parentheses.
                rett_name = rett.type == TYPE.multi
                    ? rett.compat_display_name()
                    : "(" + rett.compat_display_name() + ")"
            }
            return name + "(" + parts.join(", ") + ")" + rett_name
        }
        if this.type == TYPE.co {
            let rett = this.sub_type
            return "co(" + (isset(rett) ? rett.compat_display_name() : "") + ")"
        }
        return "type"
    }

    fn has_method(name: String) bool {
        let class = this.get_non_nullable().get_class_or_null()
        if !isset(class) : return false
        return isset(class.get_func(name))
    }

    // True if a value of this type can convert (via `compat` or a `$to` hook) to `other`.
    fn can_convert_to(other: Type) bool {
        let a = this.get_non_nullable()
        let b = other.get_non_nullable()
        // Dynamic conversion of an active union alternative is not supported.
        // Claiming the union can convert merely because it accepts `other`
        // would instantiate invalid generic methods.
        if a.type == TYPE.union {
            return b.type == TYPE.union && a.union_accepts_union(b)
        }
        let source_class = a.get_class_or_null()
        let target_class = b.get_class_or_null()
        if isset(source_class) && source_class.kind == CLASS_KIND.class
            && isset(target_class) && target_class.kind == CLASS_KIND.interface
            && class_implements_interface(source_class, target_class)
        {
            return true
        }
        // Same type or already assignable counts as a free conversion
        if a.compat(b) : return true
        let class = source_class
        if !isset(class) : return false
        let hook = class_get_to_hook(class, b, false)
        return isset(hook)
    }

    // ?T — cached on this type
    fn nullable() Type {
        // Nullability is idempotent: `?T` and `??T` describe the same type.
        // This matters for named unions that already include `null`, such as
        // json:Value, when a caller writes `?json:Value` explicitly.
        if this.type == TYPE.nullable : return this
        let cache = this.cache_nullable
        if isset(cache) : return cache
        let t = Type {
            type: TYPE.nullable
            build: this.build
            sub_type: this
        }
        this.cache_nullable = t
        return t
    }

    // *T — cached on this type
    fn pointer() Type {
        let cache = this.cache_pointer
        if isset(cache) : return cache
        let t = Type {
            type: TYPE.ptr
            build: this.build
            sub_type: this
        }
        this.cache_pointer = t
        return t
    }

    // &T — cached on this type
    fn reference() Type {
        let cache = this.cache_ref
        if isset(cache) : return cache
        let t = Type {
            type: TYPE.ref
            build: this.build
            sub_type: this
        }
        this.cache_ref = t
        return t
    }

    // <T> — cached on this type
    fn inline() Type {
        let cache = this.cache_inline
        if isset(cache) : return cache
        let t = Type {
            type: TYPE.inline
            build: this.build
            sub_type: this
        }
        this.cache_inline = t
        return t
    }

    // Exact alternative lookup. Union construction deliberately uses type
    // identity rather than broad representation compatibility: otherwise a
    // value could silently select the wrong tag in unions such as int | uint.
    fn union_member_for(other: Type) ?Type {
        let t = this.get_non_nullable()
        if t.type != TYPE.union : return null
        let members = t.sub_types
        if !isset(members) : return null
        let key = type_identity_key(other.get_non_nullable())
        each members as member {
            if type_identity_key(member) == key : return member
        }
        return null
    }

    fn union_member_index(other: Type) ?uint {
        let t = this.get_non_nullable()
        if t.type != TYPE.union : return null
        let members = t.sub_types
        if !isset(members) : return null
        let key = type_identity_key(other.get_non_nullable())
        each members as member, i {
            if type_identity_key(member) == key : return i
        }
        return null
    }

    fn union_accepts_union(other: Type) bool {
        let own_class = this.get_non_nullable().class
        let other_class = other.get_non_nullable().class
        if isset(own_class) || isset(other_class) {
            return isset(own_class) && isset(other_class) && own_class == other_class
        }
        let members = other.get_non_nullable().sub_types
        if !isset(members) : return false
        let own = this.get_non_nullable().sub_types
        if !isset(own) : return false
        if own.length != members.length : return false
        each members as member {
            if !isset(this.union_member_for(member)) : return false
        }
        return true
    }
}

// ---- Build-level singleton types ----

////////////////////////////
// Type constructors
////////////////////////////

fn type_void(b: Build) Type {
    let cache = b.cache.type_void
    if isset(cache) : return cache
    let t = Type {
        type: TYPE.void
        build: b
    }
    b.cache.type_void = t
    return t
}

// Multi-return / multi-value type `(T, U, …)`. Single element (unless force) folds to that type.
fn type_multi(b: Build, types: Array[Type], force_multi: bool (false)) Type {
    if types.length == 0 && !force_multi : return type_void(b)
    if types.length == 1 && !force_multi {
        return types.get(0) !? type_void(b)
    }
    return Type {
        type: TYPE.multi
        build: b
        sub_types: types
    }
}

fn type_identity_key(type: Type) String {
    let cached = type.cached_identity_key
    if isset(cached) : return cached
    let out = ByteBuffer.new()
    type_append_key(type, out)
    let key = out.to_string()
    type.cached_identity_key = key
    return key
}

// Normalize a structural tagged union: flatten nested unions, lift nullable
// members to the whole union, remove exact duplicates, and sort by stable type
// identity so A | B and B | A use the same runtime tag ordering.
fn type_union(b: Build, input: Array[Type], at: Exp, owner: ?Class (null)) Type !Error {
    let members = Array[Type]{}
    let nullable = false

    each input as original {
        let member = original
        if member.is_nullable_type() {
            nullable = true
            member = member.get_non_nullable()
        }
        // Anonymous unions are structural and flatten into a surrounding union.
        // A named union is one nominal alternative and retains its own tag/identity.
        if member.type == TYPE.union && !isset(member.class) {
            let nested = member.sub_types
            if isset(nested) {
                each nested as item : members.append(item)
            }
        } else {
            if member.is_void_type() : at.error("A union cannot contain void")
            if member.type == TYPE.any : at.error("A union cannot contain any")
            // Every statically-sized value can be a union alternative. Values
            // are copied into the union's shared inline payload; layout
            // resolution is deferred until all class bodies have been parsed.
            members.append(member)
        }
    }

    let sorted = Array[Type]{}
    each members as member {
        let key = type_identity_key(member)
        let duplicate = false
        each sorted as existing {
            if type_identity_key(existing) == key {
                duplicate = true
                break
            }
            // Distinct alternatives are allowed even when their assignment
            // representations are compatible. The runtime tag preserves the
            // source type identity (for example, `int` and `uint` remain
            // distinguishable); union construction resolves implicit numeric
            // conversions separately and refuses ambiguous cases.
        }
        if duplicate : continue

        sorted.append(member)
        let i = sorted.length - 1
        while i > 0 {
            let previous = sorted.get(i - 1) !? member
            if type_identity_key(previous) <= key : break
            sorted.set(i, previous) _
            sorted.set(i - 1, member) _
            i--
        }
    }

    if sorted.length == 0 : at.error("A union must contain at least one type")
    if sorted.length == 1 && !isset(owner) {
        let only = sorted.get(0) !? type_void(b)
        return nullable ? only.nullable() : only
    }
    let result = Type {
        type: TYPE.union
        build: b
        sub_types: sorted
        class: owner
        custom_name: isset(owner) ? owner.pretty_name() : ""
    }
    if nullable {
        let wrapped = result.nullable()
        if isset(owner) : wrapped.custom_name = owner.pretty_name()
        return wrapped
    }
    return result
}

// Resolve the alternatives of a named union. Unlike an anonymous `A | B`, a
// named union remains a TYPE.union even when only one non-null alternative is
// left after normalization (`union Maybe : String | null`).
fn type_named_union(owner: Class, exp: Exp) Type !Error {
    if exp.type != EXP.type || exp.kind != TYPE_KIND.union {
        exp.bug("Named union alternatives are not a union type expression")
    }
    let members = Array[Type]{}
    let has_null = false
    let i: uint = 0
    while true {
        let member_exp = exp.get(i)
        if !isset(member_exp) : break
        if member_exp.kind == TYPE_KIND.null {
            has_null = true
        } else {
            members.append(type_from_exp(member_exp, owner.scope) !>)
        }
        i++
    }
    let result = type_union(owner.ns.build, members, exp, owner) !>
    if has_null && !result.is_nullable_type() {
        let wrapped = result.nullable()
        wrapped.custom_name = owner.pretty_name()
        return wrapped
    }
    return result
}

fn type_any(b: Build) Type {
    let cache = b.cache.type_any
    if isset(cache) : return cache
    let t = Type {
        type: TYPE.any
        build: b
    }
    b.cache.type_any = t
    return t
}

// Primitive types (no Class). Used before valk:core classes are registered.
fn type_prim_int(b: Build) Type {
    let cache = b.cache.type_prim_int
    if isset(cache) : return cache
    let t = Type {
        type: TYPE.int
        build: b
        custom_name: "int"
    }
    b.cache.type_prim_int = t
    return t
}

fn type_prim_string(b: Build) Type {
    let cache = b.cache.type_prim_string
    if isset(cache) : return cache
    let t = Type {
        type: TYPE.class
        build: b
        custom_name: "String"
    }
    b.cache.type_prim_string = t
    return t
}

fn type_prim_bool(b: Build) Type {
    let cache = b.cache.type_prim_bool
    if isset(cache) : return cache
    let t = Type {
        type: TYPE.bool
        build: b
        custom_name: "bool"
    }
    b.cache.type_prim_bool = t
    return t
}

////////////////////////////
// Named core types
////////////////////////////

// Look up a class by name in valk:core (always loaded in Build.run) and return its Type.
// Results are cached on BuildCache.core_types by name. Missing classes are a compiler bug.
fn type_from_core(b: Build, name: String) Type !Error {
    let cached = b.cache.core_types.get(name) !? null
    if isset(cached) : return cached

    let idf = valk_idf(b, "core", name) !>
    if idf.type != IDF.class : b.bug("Identifier in valk:core is not a class: " + name)
    let c = idf.class
    if !isset(c) : b.bug("Class idf is missing class for: " + name)
    let t = c.get_type() !>
    b.cache.core_types.set(name, t)
    return t
}

// Look up a concrete class in a namespace from the built-in `valk` package.
fn type_from_valk_namespace(b: Build, ns_name: String, name: String) Type !Error {
    let idf = valk_idf(b, ns_name, name) !>
    if idf.type != IDF.class : b.bug("Identifier in valk:" + ns_name + " is not a class: " + name)
    let c = idf.class
    if !isset(c) : b.bug("Class idf is missing class for: " + ns_name + ":" + name)
    return c.get_type() !>
}

// Soft core lookup: returns null if the class is not registered yet (no error).
fn type_from_core_or_null(b: Build, name: String) ?Type {
    let cached = b.cache.core_types.get(name) !? null
    if isset(cached) : return cached

    // $global core classes live on Build.scope once parse_class_exp has run
    let idf = b.scope.get_idf(name)
    if !isset(idf) : return null
    if idf.type != IDF.class : return null
    let c = idf.class
    if !isset(c) : return null
    if c.is_generic_base : return null
    let t = c.get_type() ! return null
    b.cache.core_types.set(name, t)
    return t
}

// Prefer real core class; fall back to primitive stand-in before core is parsed.
fn type_int(b: Build) Type {
    let t = type_from_core_or_null(b, "int")
    if isset(t) : return t
    return type_prim_int(b)
}
fn type_float(b: Build) Type !Error {
    return type_from_core(b, "float") !>
}
fn type_bool(b: Build) Type {
    let t = type_from_core_or_null(b, "bool")
    if isset(t) : return t
    return type_prim_bool(b)
}
fn type_string(b: Build) Type {
    let t = type_from_core_or_null(b, "String")
    if isset(t) : return t
    return type_prim_string(b)
}

// `.code` / `TypeName.code` (is_instance false) or a caught-error binding `E` (true).
fn type_error_code(et: ErrorType, is_instance: bool (false)) Type {
    return Type {
        type: TYPE.error_code
        build: et.ns.build
        error_type: et
        error_is_instance: is_instance
    }
}

// Raw `ptr` (void-style pointer class from core, or a bare TYPE.ptr stand-in).
fn type_ptr(b: Build) Type {
    let t = type_from_core_or_null(b, "ptr")
    if isset(t) : return t
    let cache = b.cache.type_prim_ptr
    if isset(cache) : return cache
    let prim = Type {
        type: TYPE.ptr
        build: b
        custom_name: "ptr"
    }
    b.cache.type_prim_ptr = prim
    return prim
}

////////////////////////////
// Type expressions
////////////////////////////

// Convert a structured EXP.type into a Type using `scope` for name lookup.
// `.` resolves to `suggested_type` when the surrounding value context provides one.
fn type_from_exp(exp: Exp, scope: Scope, suggested_type: ?Type (null)) Type !Error {
    if exp.type != EXP.type : exp.bug("Expected EXP.type")
    let kind = exp.kind
    let b = exp.src.build

    if kind == TYPE_KIND.void : return type_void(b)

    if kind == TYPE_KIND.null {
        exp.error("'null' can only be used as an alternative in a tagged union")
    }

    if kind == TYPE_KIND.nullable {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for '?'")
        return type_from_exp(sub_exp, scope) !>.nullable()
    }

    if kind == TYPE_KIND.ptr {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for '*'")
        return type_from_exp(sub_exp, scope) !>.pointer()
    }
    if kind == TYPE_KIND.ptr_slice {
        // Slice pointers are not cached (size/element vary); build fully initialized.
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for '*[]'")
        let size = resolve_type_size(exp, scope) !>
        return Type {
            type: TYPE.ptr
            build: b
            sub_type: type_from_exp(sub_exp, scope) !>
            array_size: size
            is_slice_ptr: true
        }
    }

    if kind == TYPE_KIND.ref {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for '&'")
        return type_from_exp(sub_exp, scope) !>.reference()
    }
    if kind == TYPE_KIND.ref_slice {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for '&[]'")
        let size = resolve_type_size(exp, scope) !>
        return Type {
            type: TYPE.ref
            build: b
            sub_type: type_from_exp(sub_exp, scope) !>
            array_size: size
            is_slice_ptr: true
        }
    }

    if kind == TYPE_KIND.inline {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for inline")
        let sub = type_from_exp(sub_exp, scope) !>
        let class = sub.get_non_nullable().get_class_or_null()
        if isset(class) && class.kind == CLASS_KIND.interface {
            exp.error("Interfaces do not have an inline representation")
        }
        return sub.inline()
    }

    if kind == TYPE_KIND.notnull {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing inner type for notnull")
        let t = type_from_exp(sub_exp, scope) !>
        // Strip one nullable layer if present
        if t.type == TYPE.nullable {
            let inner = t.sub_type
            if isset(inner) : return inner
        }
        return t
    }

    if kind == TYPE_KIND.typeof {
        // typeof(value) in type position → result type of the value expression
        let val_exp = exp.get(TYPE_EXP.type)
        if !isset(val_exp) : exp.bug("Missing typeof operand")
        let v = value_from_exp(val_exp, scope, Context.for_scope(b, scope)) !>
        return v.rett
    }

    if kind == TYPE_KIND.array {
        let sub_exp = exp.get(TYPE_EXP.type)
        if !isset(sub_exp) : exp.bug("Missing element type for array")
        let size = resolve_type_size(exp, scope) !>
        if size == 0 : exp.error("Array size must be larger than 0")
        return Type {
            type: TYPE.array
            build: b
            sub_type: type_from_exp(sub_exp, scope) !>
            array_size: size
        }
    }

    if kind == TYPE_KIND.multi {
        return type_from_multi_exp(exp, scope) !>
    }

    if kind == TYPE_KIND.union {
        let members = Array[Type]{}
        let has_null = false
        let i: uint = 0
        while true {
            let member_exp = exp.get(i)
            if !isset(member_exp) : break
            if member_exp.kind == TYPE_KIND.null {
                has_null = true
            } else {
                members.append(type_from_exp(member_exp, scope) !>)
            }
            i++
        }
        let result = type_union(b, members, exp) !>
        if has_null && !result.is_nullable_type() : return result.nullable()
        return result
    }

    if kind == TYPE_KIND.fn || kind == TYPE_KIND.fnptr {
        let args_types: ?Array[Type] = null
        let args = exp.get(TYPE_EXP.args)
        if isset(args) {
            // Function arguments are always a list. Preserve empty and single-item
            // lists instead of normalizing them to void/T.
            let args_t = type_from_multi_exp(args, scope, true) !>
            args_types = args_t.sub_types
        }
        let rett_type: ?Type = null
        let rett = exp.get(TYPE_EXP.rett)
        if isset(rett) {
            rett_type = type_from_exp(rett, scope) !>
        }
        // Reuses the same `error_type` field TYPE.error_code uses: the ErrorType this
        // function value can throw, when declared (`fn(...)(...) !ErrorName`).
        let fn_error_type: ?ErrorType = null
        let err_exp = exp.get(TYPE_EXP.error)
        if isset(err_exp) {
            let idf = resolve_idf_from_id(err_exp, scope) !>
            if idf.type != IDF.error_type {
                err_exp.error("Identifier must be an error type")
            }
            fn_error_type = idf.error_type
            if !isset(fn_error_type) : err_exp.bug("Error-type idf is missing error_type")
        }
        return Type {
            type: kind == TYPE_KIND.fn ? TYPE.fn : TYPE.fnptr
            build: b
            sub_type: rett_type
            sub_types: args_types
            error_type: fn_error_type
        }
    }

    if kind == TYPE_KIND.co {
        let rett_type: ?Type = null
        let rett = exp.get(TYPE_EXP.rett)
        if isset(rett) {
            rett_type = type_from_exp(rett, scope) !>
        }
        // Reuses the same `error_type` field TYPE.error_code / TYPE.fn use: the
        // ErrorType this coroutine can throw, when declared (`co(...) !ErrorName`).
        let co_error_type: ?ErrorType = null
        let err_exp = exp.get(TYPE_EXP.error)
        if isset(err_exp) {
            let idf = resolve_idf_from_id(err_exp, scope) !>
            if idf.type != IDF.error_type {
                err_exp.error("Identifier must be an error type")
            }
            co_error_type = idf.error_type
            if !isset(co_error_type) : err_exp.bug("Error-type idf is missing error_type")
        }
        return Type {
            type: TYPE.co
            build: b
            sub_type: rett_type
            error_type: co_error_type
        }
    }

    if kind == TYPE_KIND.suggested {
        if !isset(suggested_type) {
            exp.error("'.' type is not valid here")
        }
        return suggested_type
    }

    if kind == TYPE_KIND.named {
        return type_from_named_exp(exp, scope) !>
    }

    exp.bug("Unknown type kind: " + kind)
}

fn type_from_multi_exp(exp: Exp, scope: Scope, force_multi: bool (false)) Type !Error {
    let b = exp.src.build
    let types = Array[Type]{}
    let i: uint = 0
    while true {
        let sub = exp.get(i)
        if !isset(sub) : break
        types.append(type_from_exp(sub, scope) !>)
        i++
    }
    return type_multi(b, types, force_multi)
}

fn type_from_named_exp(exp: Exp, scope: Scope) Type !Error {
    let id = exp.get(TYPE_EXP.id)
    if !isset(id) : exp.bug("Named type is missing id")

    let name = id.str1
    let ns_name = id.str2

    // Optional namespace prefix: ns:Name — `ns` must be an IDF.scope from `use`
    let idf: ?Idf = null
    if isset(ns_name) {
        let ns_idf = scope.get_scope_idf(ns_name)
        if !isset(ns_idf) : exp.error("Unknown namespace: " + ns_name)
        let ns_scope = ns_idf.scope
        if !isset(ns_scope) : exp.bug("Scope idf is missing scope")
        // LSP: a type position completes from the same scope as a value position;
        // the client filters out what cannot be a type by the kind we report.
        lsp_complete_scope(id, ns_scope) !>
        idf = ns_scope.get_idf(name)
        if !isset(idf) : exp.error("Unknown type: " + ns_name + ":" + name)
    } else {
        lsp_complete_scope(id, scope) !>
        idf = scope.get_idf(name)
        if !isset(idf) : exp.error("Unknown type: " + name)
    }

    let resolved = idf
    if !isset(resolved) : exp.error("Unknown type: " + name)

    // LSP: jump to the declaration of the named type
    lsp_goto_idf(id, resolved) !>
    // LSP: hover in a type position. Reported before the brackets are applied,
    // so the cursor on `Array` in `Array[String]` describes the base.
    lsp_hover_idf(id, resolved) !>

    // Generic parameter or other bound type (e.g. `T` inside `class Foo[T]`)
    if resolved.type == IDF.type {
        let of_type = resolved.of_type
        if !isset(of_type) : exp.bug("Type idf is missing of_type")
        return of_type
    }

    // Property metadata bound by `#loop properties T as prop` is usable as
    // the property's declared type (`Generic[prop]`, casts, declarations, …).
    if resolved.type == IDF.property {
        let prop = resolved.prop
        if !isset(prop) : exp.bug("Property idf is missing Prop")
        return prop.type
    }

    // enum Name { ... }
    if resolved.type == IDF.enum {
        let enu = resolved.enum
        if !isset(enu) : exp.bug("Enum idf is missing enum")
        act_check(exp, enu.act, enu.exp, false, "enum") !>
        return enu.get_type() !>
    }

    if resolved.type != IDF.class {
        exp.error("Identifier is not a type: " + name)
    }
    let class = resolved.class
    if !isset(class) : exp.bug("Class idf is missing class")
    // Naming a type is an access too. Without this, `let x: PrivateClass` and
    // private types in signatures slip past the value-position check. Internal
    // helpers (type_from_core / type_from_valk_namespace) resolve classes
    // directly and never reach here, so they are unaffected.
    act_check(exp, class.act, class.exp, false, "class") !>

    // Brackets after a named type are either:
    //   - generic args: Array[T], Map[K, V]
    //   - pointer-info: ptr[u8], ptr[Array[?Coro]], cstring[u8 x N], Foo[T][u8]
    // AST parse always stores the first `[...]` (without `x N`) in TYPE_EXP.generics.
    // Only at resolve time do we know if the class is generic.
    let gens = exp.get(TYPE_EXP.generics)
    // second `[...]` after generics, or sized `[T x N]` via TYPE_EXP.type / str2
    let ptr_info = exp.get(TYPE_EXP.type)

    // Resolve base class type (specialize generics when present)
    if isset(gens) && class.is_generic_base {
        let gtypes = Array[Type]{}
        let i: uint = 0
        while true {
            let g = gens.get(i)
            if !isset(g) : break
            gtypes.append(type_from_exp(g, scope) !>)
            i++
        }
        class = class.specialize(gtypes, exp) !>
    } else if isset(gens) {
        // Non-generic with `[T]` → pointer-info hint (e.g. ptr[u8], ptr[Array[Foo]])
        let g0 = gens.get(0)
        if !isset(g0) : exp.bug("Empty type brackets")
        if isset(gens.get(1)) {
            exp.error("Type is not generic: " + class.name)
        }
        if isset(ptr_info) {
            exp.error("Unexpected extra type brackets after non-generic type: " + class.name)
        }
        ptr_info = g0
    } else if class.is_generic_base {
        exp.error("Generic type '" + class.name + "' requires type arguments")
    }

    let base = class.get_type() !>

    // Optional pointer-info: sub_type hint and/or fixed size (matches classic read_pointer_info)
    let size_exp = exp.get(TYPE_EXP.size)
    if isset(ptr_info) || isset(size_exp) {
        let size: u32 = 0
        if isset(size_exp) : size = resolve_type_size(exp, scope) !>
        let sub: ?Type = null
        if isset(ptr_info) : sub = type_from_exp(ptr_info, scope) !>
        return Type {
            type: base.type
            build: base.build
            class: class
            custom_name: class.pretty_name()
            sub_type: sub
            array_size: size
        }
    }

    return base
}

// Evaluate TYPE_EXP.size (a value expression) to a non-negative compile-time integer.
fn resolve_type_size(exp: Exp, scope: Scope) u32 !Error {
    let size_exp = exp.get(TYPE_EXP.size)
    if !isset(size_exp) : return 0
    let v = value_from_exp(size_exp, scope, Context.for_scope(exp.src.build, scope)) !>
    if v.type != VAL.int {
        size_exp.error("Type size must be a compile-time integer")
    }
    if v.int_val < 0 {
        size_exp.error("Type size must not be negative")
    }
    return v.int_val.to(u32)
}
