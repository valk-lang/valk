
error InitError (init) payload { msg: String }
error Error (error)

enum MSG {
    error
    warning
}

class Message {
    type: int
    msg: String
}

fn init_error(msg: String) !InitError $throw {
    throw .init { msg: msg }
}
