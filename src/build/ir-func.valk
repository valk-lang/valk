
fn ir_func(func: Func) !Error {
    let ast = func.ast()

    let start = IRBlock {
        name: "start"
    }
    let ctx = IRContext {
        build: func.build
        func: func
        active: start
    }
    ctx.blocks.append(start)

    ir_ast(ctx, ast.scope) !>
}
