
class Unit {
    build: Build
    name: String

    fn new_func() Func {
        let f = Func {
            build: this.build
            unit: this
            scope: Scope.new(SCOPE.default, this.build)
        }
        this.build.functions.append(f)
        return f
    }
}
