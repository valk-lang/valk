
struct Global {
    unit: Unit
    build: Build
    fc: Fc
    act: int
    export_name: String
    display_name: String
    ir_name: String
    type: ?Type (null)
    chunk_type: Chunk
    chunk_value: ?Chunk

    fn get_type() Type {
        let type = this.type
        if !isset(type) : this.build.error("Missing global type (bug)")
        return type
    }
}
