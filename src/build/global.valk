
struct Global {
    unit: Unit
    build: Build
    fc: Fc
    act: int
    type: ?Type (null)
    chunk_type: Chunk
    chunk_value: ?Chunk

    fn get_type() Type {
        let type = this.type
        if !isset(type) : this.build.error("Missing global type (bug)")
        return type
    }
}
