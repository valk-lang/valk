
type FmtUnion(String | int | null)

fn describe_union(value: FmtUnion) String {
    return match value: String {
        null => "none"
        String as text => text
        int as number => number.to(String)
    }
}
