value COMPILE_CONSTANT_BASE (size_of(i64))
value COMPILE_CONSTANT_SIZE (COMPILE_CONSTANT_BASE - size_of(u16))

struct CompileConstantLayout {
    bytes: [u8 x COMPILE_CONSTANT_SIZE]
}

test "Compile-time integer expressions" {
    assert(COMPILE_CONSTANT_SIZE == 6)
}
