# Breaking changes accepted on purpose, one declaration per line, e.g. `http.Server.start`
# or `fs.FileLock` (which also covers its members). Trailing `# comments` explain why.
core.HashMap.HashMap[u32, H2Stream]  # valk doc labeled the integer-key extension by one specialization; it is HashMap[u32, T] now, same methods
http.Response.new  # content_type default gained "; charset=utf-8"; callers compile unchanged
http.ResponseWriter.send  # same charset default
http.HttpParseError  # gained http431; only the private parse_http/parse_headers throw it, so no user code can match on it
http.HttpError  # gained 'status' for http.download on a non-2xx answer (the user asked for download to fail on 4xx/5xx)
core.f32.to_string  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.f32.to_string_in  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.f32.to_string_into  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.f64.to_string  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.f64.to_string_in  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.f64.to_string_into  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.float.to_string  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.float.to_string_in  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
core.float.to_string_into  # decimals is ?uint (null = shortest text, like interpolation); calls with a number compile unchanged
