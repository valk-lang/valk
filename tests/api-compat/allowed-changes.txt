# Breaking changes accepted on purpose, one declaration per line, e.g. `http.Server.start`
# or `fs.FileLock` (which also covers its members). Trailing `# comments` explain why.
core.HashMap.HashMap[String, T]  # valk doc labels HashMap's extensions by the first specialization it meets; sort_keys and from_json_value_auto are unchanged (json added an integer-key extension)
core.HashMap.HashMap[u32, H2Stream]  # same label change
