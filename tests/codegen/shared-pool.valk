use valk:core

class SharedPoolValue {
    value: int
}

fn main() {
    let local = SharedPoolValue { value: 1 }
    let shared: shared SharedPoolValue = .{ value: 2 }
    println(local.value + shared.value)
}
