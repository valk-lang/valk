use valk.gc
use valk.net

fn explicit_collect() {
    gc.collect()
}

fn explicit_collect_shared() {
    gc.collect_shared()
}

fn main() {
    explicit_collect()
    explicit_collect_shared()
    let server = net.tcp_server("127.0.0.1", 0) !!
    server.accept(1) ! {}
}
