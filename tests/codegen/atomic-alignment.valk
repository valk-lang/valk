class AtomicAlignment {
    byte: u8
    half: u16
    word: u32
}

fn atomic_alignment(value: shared AtomicAlignment) u32 {
    return value.byte + value.half + value.word
}

fn main() {
    atomic_alignment(AtomicAlignment { byte: 1, half: 2, word: 3 })
}
