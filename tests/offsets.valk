
test "Offsets" {
    let arr = Array[uint].new()
    arr.push(0)
    arr.push(2)
    arr.push(4)

    assert(arr[0] == 0)
    assert(arr[1] == 2)
    assert(arr[2] == 4)
    assert(arr[0]++ == 0)
    arr[0] += 2
    assert(arr[0] == 3)
}
