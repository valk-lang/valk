use valk.fs
use valk.time

class LazyImport[T] {
    fn uses_import() bool {
        return fs.exists("")
    }
}

class LazyExtension[T] {}

extend LazyExtension[T] {
    fn uses_import() uint {
        return time.unix_ns()
    }
}
