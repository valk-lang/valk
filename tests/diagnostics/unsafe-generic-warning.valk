fn identity(value: $T) T {
    return value
}

fn generic_identity(value: $T) T {
    @unsafe
    return identity(value)
}

fn main() {
    generic_identity(1)

    @unsafe
    generic_identity(0.@cast(ptr))
}
