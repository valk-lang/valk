class Item { n: int (0) }
class Store { list: Array[Item] (.{}) }
fn main() {
    let store: shared Lock[Store] = .new(Store {}) !!
    lock store as s {
        s.list.sort(fn(a: Item, b: Item) bool { return a.n > b.n })
    }
}
