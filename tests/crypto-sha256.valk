
use valk:crypto

test "Sha256" {
    let content = "This is a sha256 hash test"
    let hash = crypto:sha256_encode(content)
    // println(hash)
    // assert(hash == "d0b3ad61edf0ff6224c29eea7a067734514dc4d451fad4f0d166aeda168abebd")
}