
class LZP {
    // $lazy is not implemented by this compiler and is rejected as an unknown
    // property flag. Restore it here when lazy properties are lowered again.
    list: Array[String] (.{})
}

// Disabled while the rewritten backend does not lower lazy properties yet.
// test "Lazy properties" {
//     let ob = LZP{}
//     let ref = @ref(ob.list)
//     assert(@ptrv(ref, ?ptr) == null)
//     ob.list.append("T")
//     assert(ob.list.length == 1)
// }
