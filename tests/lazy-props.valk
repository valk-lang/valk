
class LZP {
    list: Array[String] (.{}) $lazy
}

test "Lazy properties" {
    let ob = LZP{}
    let ref = @ref(ob.list)
    assert(@ptrv(ref, ?ptr) == null)
    ob.list.append("T")
    assert(ob.list.length == 1)
}
