unbound MutableBytes of u8 {
    fn set(index: uint, value: u8) $offset_assign {
        @ptrv(this, u8, index) = value
    }
}

fn main() {
    let values: [u8 x 2] = {1, 2}
    let view = @ref(values).@cast(MutableBytes[x 2])
    let index: uint = 2
    view[index] = 3
}
