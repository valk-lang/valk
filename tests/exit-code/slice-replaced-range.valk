class Holder {
    view: mut &[int] ([int]{7})
    fn replace() uint {
        this.view = [int]{19, 23, 73}
        return 2
    }
}
fn main() {
    let holder = Holder {}
    let view = holder.view[holder.replace()..1]
    println(view[0])
}
