use valk.io

fn main() i32 {
    let buffer = ByteBuffer.new()
    let bytes = io.read(0, buffer, 4, 0) ! return 1
    let sync_bytes = io.read_sync(0, buffer, 4, 0) ! return 1
    if bytes != 4 || sync_bytes != 4 || buffer.to_string() != "pingpong" : return 1
    return 0
}
