use valk.io

fn main() i32 {
    let buffer = Slice[u8].new(8, 0)
    let bytes = io.read(0, buffer.view(0, 4)) ! return 1
    let sync_bytes = io.read_sync(0, buffer.view(4, 4)) ! return 1
    if bytes != 4 || sync_bytes != 4 || buffer.to_string() != "pingpong" : return 1
    return 0
}
