class Holder {
    view: Slice[int] (Slice[int]{7})
    fn replace() uint {
        this.view = Slice[int]{19, 23, 73}
        return 2
    }
}
fn main() {
    let holder = Holder {}
    holder.view[holder.replace()] = 73
}
