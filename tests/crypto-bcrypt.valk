
use valk:crypto

test "Bcrypt" {

    // Verify
    assert(crypto:bcrypt_verify("abc", "$2a$06$.pBSgOJEbKaFY0/jAFsZ4eE5AD8OBZvG299TMJZZL3RHejv.s5kdi"))

    // Create + Verify
    let hash = crypto:bcrypt_hash("test", 6)
    assert(crypto:bcrypt_verify("test", hash))
}
