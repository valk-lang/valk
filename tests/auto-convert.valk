
class AutoConvertSource {
    fn to_string() String $auto {
        return "converted"
    }
}

class AutoConvertTarget {
    value: String
}

test "Auto-convert .to() .$to()" {
    let x = 5.to(String)
    assert(x == "5")
    let y = 257.to(u8)
    assert(y == 1)

    let source = AutoConvertSource{}
    let target = AutoConvertTarget{ value: source }
    assert(target.value == "converted")
}
