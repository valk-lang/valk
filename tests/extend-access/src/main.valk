use owner_pkg

extend owner_pkg:ExtendAccessTarget {
    fn downstream_method() int {
        return 7
    }

    get downstream_value: int {
        return 8
    }
}

fn main() i32 {
    let target = owner_pkg:ExtendAccessTarget {}
    let value = target.visible_from_extension()
    if value != 42 : return 1
    if target.downstream_method() != 7 : return 2
    if target.downstream_value != 8 : return 3
    return 0
}
