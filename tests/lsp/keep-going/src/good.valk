
// Diagnosed with broken.valk in the same package: its own error must still be
// reported, and `Broken`, declared after a syntax error there, must resolve.
fn keep_going_good() {
    let b = Broken { x: 1 }
    println(b.x)
    println(keep_going_missing_xyz)
}
