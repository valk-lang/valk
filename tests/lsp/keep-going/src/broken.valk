
// A declaration header that does not parse: the base parse resyncs at the
// next unindented line, so the class below still exists.
class {}

class Broken {
    x: int
}

fn broken_sig(a: NoSuchType) {}
