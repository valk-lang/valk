



use valk.fs

fn main() {
    let dir_len = 3
    #if OS == "win"
    println((fs.cwd() !? "") + dir_len)
    #end
}






fn cond_flow() uint {
    #if OS == "win"
    return 1
    #else
    return 2
    #end
    println("not reported")
}
