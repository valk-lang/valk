class Thing {
    + name: String
    + fn describe() String {
        return this.name
    }
}

fn find() ?Thing {
    return null
}

fn go() String {
    let t = find()
    return t?.describe() ?? "none"
}

fn complete() {
    let t = find()
    t?.
}
