class Thing {
    + name: String
    + fn describe() String {
        return this.name
    }
    /// Deprecated: use `describe`.
    + fn legacy() String $deprecated {
        return this.name
    }
}

fn go() {
    let t = Thing { name: "x" }
    t.
}
