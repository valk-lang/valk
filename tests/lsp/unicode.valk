// Non-ASCII must survive the wire: an em dash — is three bytes, and a
// vertical tab  has no short JSON escape.
fn greet(name: String) String {
    return "héllo "+name
}

fn main() {
    println(greet("wörld"))
}
