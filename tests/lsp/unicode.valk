// Non-ASCII must survive: an em dash — is three bytes; a vertical tab  has no short JSON escape.

fn greet(name: String) String {
    return "héllo "+name
}

fn main() {
    println(greet("wörld"))
}
