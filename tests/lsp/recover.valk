fn helper(count: uint) String {
    return "x"
}

class Thing {
    + name: String
    + fn describe() String {
        return this.name
    }
}

fn go() {
    let name = helper(3)
    let broken = helper(
    let other = helper(4)
    let wrong = name.no_such_method_xyz()
    let t = Thing { name: other }
    if true {
        let inner = helper(
        let deep = helper(7)
    }
    println(broken + wrong + inner + t.describe())
    let pending =
    let after = helper(8)
    t.
}

fn handled() String {
    let text = helper(1)
    let n = text.to_int() ! {
        let half = 5 +
        return "no"
    }
    let sum = n +
    // a comment before the next statement
    let after = helper(9)
    return after
}
