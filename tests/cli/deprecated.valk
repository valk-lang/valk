use valk.mem

/// Deprecated: use `fresh`.
fn stale() uint $deprecated { return 1 }
fn fresh() uint { return 2 }

fn main() {
    @unsafe
    let a: [u8 x 4] = { 1, 2, 3, 4 }
    println(stale() + fresh())
    println(mem.equal_bytes(&a, &a, 4))
    let unused = 5
}
