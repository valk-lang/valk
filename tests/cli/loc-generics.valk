class Box[T] {
    value: T
}

fn identity[T](value: T) T {
    return value
}

fn main() {
    let a = Box[int]{ value: identity[int](1) }
    let b = Box[u8]{ value: identity[u8](2) }
    let c = Box[int]{ value: identity[int](3) }
}
