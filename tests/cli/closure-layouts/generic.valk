fn closure_layout_generic[T](value: T) fn()(T) {
    return fn() T { return value }
}

fn closure_layout_nested[T](value: T) fn()(fn()(T)) {
    return fn() fn()(T) {
        return fn() T { return value }
    }
}
