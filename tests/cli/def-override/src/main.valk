#if OVERRIDE != "cli"
#error "--def did not override the valk.json define value"
#end

#if VALK_VERSION < "0.3.4"
#error "VALK_VERSION does not match the compiler version"
#end

#if VALK_VERSION < "0.3.2"
#error "VALK_VERSION string comparison selected the wrong branch"
#end

#if VALK_VERSION >= "0.3.2"
#else
#error "VALK_VERSION string comparison selected the wrong branch"
#end

fn main() {}
