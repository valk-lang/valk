@unsafe
@ignore_access

use valk.core

test "Test result rows count UTF-8 characters" {
    let row = core.TestResults.format_row(
        "Json: Value façade provides direct object and array access", true, false)

    assert(row.utf8.length == 68)
    assert(row.ends_with(" OK\n"))
}
