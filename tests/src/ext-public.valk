use valk.ext

test "FFI structs and properties are public" {
    let descriptor = ext.libc_pollfd {
        fd: 3
        events: 1
        revents: 0
    }
    assert(descriptor.fd == 3)
    assert(descriptor.events == 1)
    assert(descriptor.revents == 0)
}
