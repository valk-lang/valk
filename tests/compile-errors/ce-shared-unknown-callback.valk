class SharedCallbackValue {}

fn invoke_callback(callback: fn(SharedCallbackValue)(), value: SharedCallbackValue) {
    callback(value)
}

fn inspect_callback_value(value: SharedCallbackValue) {}

fn main() {
    let value = SharedCallbackValue {}
    invoke_callback(inspect_callback_value, value)
    let published: shared SharedCallbackValue = value
}
