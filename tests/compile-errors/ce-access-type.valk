use valk:core

// core:Closure has no marker, so it is private to the valk package. Naming it in
// a signature is an access, the same as using it as a value.
fn takes(c: ?core:Closure) uint {
    return isset(c) ? 1 : 0
}

fn main() {
    println("%{takes(null)}")
}
