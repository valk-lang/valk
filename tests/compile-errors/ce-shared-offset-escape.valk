global retained_offset: ?Item (null)
class Item {
    number: uint (23)
    fn get(index: uint) uint $offset {
        retained_offset = this
        return this.number
    }
}
fn main() {
    let item = Item {}
    print(item[0])
    let published: shared Item = item
    print(published.number)
}
