use valk.fs

class CompatClass {}
slice CompatBytes of u8 {}
slice OtherBytes of u8 {}
slice CompatItems of String {}
struct CompatStruct {}

fn main() {
    @unsafe
    let a : TYPE1 = null.@cast(TYPE1)
    let b : TYPE2 = null.@cast(TYPE2)
    a = b
}
