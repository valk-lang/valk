// A function with a return type has to return on every path. Falling off the
// end would hand back whatever happened to be in the register.
fn no_return(v: int) int {
    let x = v + 1
}

fn main() {
    println("%{ no_return(1) }")
}
