fn cond() bool {
    return true
}

fn main() {
    let first: ?String = null
    let x: ?String = "a"
    if !isset(x) : return
    while cond() {
        let t: String = first ?? x
        println(t)
        x = null
    }
}
