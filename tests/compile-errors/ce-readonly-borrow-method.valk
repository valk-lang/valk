struct ReadonlyCounter { n: int (0); fn inc() { this.n++ } }
class ReadonlyHolder { counter: ReadonlyCounter (.{}) }
fn main() {
    let holder = ReadonlyHolder {}
    let c: &ReadonlyCounter = &holder.counter
    c.inc()
}
