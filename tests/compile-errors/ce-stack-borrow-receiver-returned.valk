// A method that keeps its receiver in its result: the result may not leave the frame
fn make() ByteReader {
    let bytes: [u8 x 3] = { 7, 8, 9 }
    let view: local &[u8] = &bytes
    return view.reader()
}

fn main() { make() }
