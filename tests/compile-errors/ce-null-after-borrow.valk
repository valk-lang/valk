

class BorrowedNode {
    v: int (1)
}

fn wipe_borrowed_node(p: local mut &?BorrowedNode) {
    p[0] = null
}

fn main() {
    let x: ?BorrowedNode = BorrowedNode {}
    if isset(x) {
        wipe_borrowed_node(&x)
        println(x.v)
    }
}
