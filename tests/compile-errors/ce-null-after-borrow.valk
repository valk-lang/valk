

class BorrowedNode {
    v: int (1)
}

fn wipe_borrowed_node(p: stack ?BorrowedNode) {
    p[0] = null
}

fn main() {
    let x: ?BorrowedNode = BorrowedNode {}
    if isset(x) {
        wipe_borrowed_node(&x)
        println(x.v)
    }
}
