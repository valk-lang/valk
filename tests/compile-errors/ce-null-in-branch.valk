
// Assigning null on one path invalidates the non-null fact for the code after the
// if, even though the other path leaves it set.

fn maybe() bool {
    return true
}

fn main() {
    let x : ?String = "a"
    if maybe() { x = null }
    let t : String = x
    println(t)
}
