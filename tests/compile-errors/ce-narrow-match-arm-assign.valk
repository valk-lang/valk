class C { v: int }
fn pick() int { return 1 }
fn main() {
    let s: ?C = C{ v: 3 }
    if !isset(s) : return
    match pick() {
        1 => { s = null }
        default => {}
    }
    println(s.v)
}
