interface Readable { fn read() int; }
class Counter is Readable {
    fn read() int { return 73 }
}
fn main() {
    let readable: Readable = Counter {}
    let pointer: fnptr()(int) = readable.read
    print(pointer())
}
