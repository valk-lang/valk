class CheckedValue {
    v: int
}
fn main() {
    let value: ?CheckedValue = CheckedValue { v: 3 }
    let outer = 0
    while outer < 2 {
        let inner = 0
        while inner < 2 {
            println(value.v)
            inner++
        }
        value = null
        outer++
    }
}
