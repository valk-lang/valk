global retained_add: ?Item (null)

class Item {
    number: uint (23)
    fn add(value: int) int $add {
        retained_add = this
        return value
    }
}

fn main() {
    let item = Item {}
    let value = item + 1
    let published: shared Item = item
    print(value)
    print(published.number)
}
