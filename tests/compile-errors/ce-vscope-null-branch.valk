
// A declared type is authoritative: a value scope must not widen its result to
// `?String` just because one branch returns null.

fn maybenot() bool {
    return false
}

fn main() {
    let s : String = <{
        if maybenot() : return null
        return "x"
    }
    println(s)
}
