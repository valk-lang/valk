class SharedEachItem { name: String ("a") }

fn mutate(item: SharedEachItem) { item.name = "b" }

fn main() {
    let items: shared Array[SharedEachItem] = Array[SharedEachItem]{ SharedEachItem {} }
    each items as item : mutate(item)
}
