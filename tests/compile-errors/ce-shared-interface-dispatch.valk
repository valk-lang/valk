interface SharedSink { fn write(text: String); }

class SharedSinkImpl is SharedSink {
    last: String ("")
    fn write(text: String) { this.last = text }
}

fn make() SharedSink { return SharedSinkImpl {} }

fn main() {
    let sink: shared SharedSink = make()
    sink.write("x")
}
