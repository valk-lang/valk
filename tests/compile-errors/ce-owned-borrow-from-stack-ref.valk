class Holder {
    value: &int
}

global saved: ?Holder (null)

fn keep(value: &int) Holder {
    return Holder { value: value }
}

fn main() {
    let value = 1
    let borrowed: local mut &int = &value
    // The holder may exist while the frame lives; storing it away may not
    saved = keep(borrowed)
}
