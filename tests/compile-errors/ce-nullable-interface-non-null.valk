interface NullableTarget {
    fn value() int;
}

class NullableImpl is NullableTarget {
    fn value() int { return 1 }
}

fn take(value: NullableTarget) {}

fn main() {
    let value: ?NullableImpl = null
    take(value)
}
