use valk.mem
class Managed {
    value: int
}
fn make() *[Managed x 2] {
    return mem.new[[Managed x 2]](.{ Managed { value: 11 }, Managed { value: 22 } })
}
fn main() {
    let pointer = make()
    println(pointer[0].value)
}
