use valk:thread

class SharedCaptureValue {
    value: String
}

fn main() {
    let value = SharedCaptureValue { value: "mutable" }
    thread:start(fn() {
        value.value = "changed"
    }) !!
}
