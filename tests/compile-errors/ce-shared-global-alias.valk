class SharedGlobalValue {}

global ordinary_shared_source: Array[SharedGlobalValue] (Array[SharedGlobalValue]{})
shared aliased_shared_target: Array[SharedGlobalValue] (Array[SharedGlobalValue]{})

fn main() {
    aliased_shared_target = ordinary_shared_source
}
