class Counter {
    number: int (73)
    fn read() int { return this.number }
}
fn main() {
    let counter = Counter {}
    let callback: shared fn()(int) = counter.read
    counter.number = 19
    print(callback())
}
