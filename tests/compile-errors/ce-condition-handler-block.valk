// A `{ ... }` error handler in a condition takes the next block, so a loop written with one and
// no body of its own is reported with the parentheses that would fix it.
fn next() bool !LookupError {
    return false
}

fn main() {
    while next() ! { println("failed") }
    println("after")
}
