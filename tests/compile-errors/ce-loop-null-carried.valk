




fn maybe() ?String {
    return null
}

fn main() {
    let x = maybe()
    if !isset(x) : return
    let y : ?String = null
    let i = 0
    while i < 3 {
        y = x
        x = maybe()
        println(y.bytes)
        i++
    }
}
