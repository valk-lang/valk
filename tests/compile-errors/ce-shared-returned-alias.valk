class SharedReturnedAlias { n: int (0) }

fn passthrough(value: SharedReturnedAlias) SharedReturnedAlias { return value }

fn main() {
    let value = SharedReturnedAlias {}
    let kept = passthrough(value)
    let published: shared SharedReturnedAlias = value
    kept
}
