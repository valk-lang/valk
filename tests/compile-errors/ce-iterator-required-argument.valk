class Steps {
    fn _next(index: uint, extra: int) int !IterError {
        if index >= 1 : throw .end
        return extra
    }
}
fn main() { let steps = Steps {}; each steps as value : println(value) }
