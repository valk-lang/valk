class Inner { items: Array[int] (.{}) }
class Box { objs: [Inner x 1] ({ .{} }) }
fn main() {
    let b: shared Box = Box {}
    let inner = Inner {}
    b.objs[0] = inner
    inner.items.append(1)
}
