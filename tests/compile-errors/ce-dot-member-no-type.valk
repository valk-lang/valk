// A `.name` takes the type it is assigned or passed to. Here there is none, so the message says
// that rather than talking about error codes.
fn main() {
    let value = .something
    println(value)
}
