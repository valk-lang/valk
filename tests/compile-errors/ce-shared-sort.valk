class SharedSortItem { key: int (0) }

fn main() {
    let values: shared Array[SharedSortItem] = Array[SharedSortItem]{ SharedSortItem { key: 2 }, SharedSortItem { key: 1 } }
    values.sort(fn(a: SharedSortItem, b: SharedSortItem) bool { return a.key < b.key })
}
