class Item { number: int }
shared saved: ?Item (null)
fn main() {
    let source = Item { number: 73 }
    while true {
        saved = source
        continue
    }
    println((saved ?? panic("missing")).number)
}
