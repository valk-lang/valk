class SharedCoViewValue { name: String ("a") }
fn shared_co_view_read(v: shared SharedCoViewValue) { println(v.name) }
fn main() {
    let v = SharedCoViewValue {}
    let t = co shared_co_view_read(v)
    v.name = "mutated"
    await t
}
