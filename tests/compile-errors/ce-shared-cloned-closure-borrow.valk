class Cell { number: int (1) }
class Holder { callback: fn()(int) }

fn main() {
    let cell = Cell {}
    let borrowed = &cell.number
    let original = fn() int { return borrowed[0] }
    let published: shared Holder = Holder { callback: original.clone() }
    cell.number = 7
    print(published.callback())
}
