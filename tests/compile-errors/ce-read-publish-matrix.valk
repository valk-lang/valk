type Row ([int x 2])
class Item { numbers: [Row x 2] ({ .{ 73, 79 }, .{ 83, 89 } }) }
shared retained: ?Item (null)
fn publish(item: shared Item) int { retained = item; return 0 }
fn main() {
    let source = Item {}
    let number = source.numbers[0][publish(source)]
    println(number)
}
