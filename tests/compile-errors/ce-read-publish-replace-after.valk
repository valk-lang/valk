class Item { numbers: [int x 2] ({ 73, 79 }) }
shared retained: ?Item (null)
fn publish(item: shared Item) int { retained = item; return 0 }
fn main() {
    let source = Item {}
    let number = source.numbers[<{ let index = publish(source); source = Item {}; return index }]
    println(number)
}
