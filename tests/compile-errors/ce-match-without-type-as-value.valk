fn main() {
    let n = 2
    let text = match n {
        1 => "one"
        2 => <{
            let s = "tw"
            return s + "o"
        }
        default => "many"
    }
    println(text)
}
