struct Cell {
    count: uint (0)
    group stats {
        fn increment() { this.count++ }
    }
}
fn escaped() fn()() {
    let cell = Cell {}
    return cell.stats.increment
}
fn main() { escaped()() }
