use valk.core
class LockEntry { id: uint (0) }
class LockValue { entry: ?LockEntry (null) }
fn main() {
    let state: shared Lock[LockValue] = .new(LockValue {}) !!
    let entry = LockEntry {}
    let alias = entry
    lock state as s {
        s.entry = entry
    }
    alias.id = 2
}
