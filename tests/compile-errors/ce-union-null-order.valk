use valk.core

union Value : String | int | null {}

fn describe(value: Value) String {
    return match value : String {
        String as text => text
        null => "missing"
        int as number => number.to(String)
    }
}

fn main() {}
