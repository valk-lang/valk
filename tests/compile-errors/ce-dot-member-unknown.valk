// The expected type is known, but has no member of that name.
class Tool {
    size: uint (1)
    static fn of_size(size: uint (1)) Tool {
        return Tool { size: size }
    }
}

fn main() {
    let tool: Tool = .not_a_member
    println(tool.size)
}
