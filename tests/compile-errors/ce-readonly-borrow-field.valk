struct ReadonlyPoint { x: int (0) }
class ReadonlyPointHolder { point: ReadonlyPoint (.{}) }
fn main() {
    let holder = ReadonlyPointHolder {}
    let p: &ReadonlyPoint = &holder.point
    p.x = 5
}
