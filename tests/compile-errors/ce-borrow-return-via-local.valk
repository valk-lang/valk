class Inner {
    n: uint (3)
}
class Box {
    inner: Inner (.{})
}
fn bad(b: &Box) &Inner {
    let i = b.inner
    let local = Box{}
    i = local.inner
    return i
}
fn main() {
    let b = Box{}
    println(bad(&b).n)
}
