class SharedElementItem { name: String ("a") }

fn mutate(item: SharedElementItem) { item.name = "b" }

fn main() {
    let items: shared Array[SharedElementItem] = Array[SharedElementItem]{ SharedElementItem {} }
    mutate(items.get(0) !!)
}
