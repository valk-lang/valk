// `while true` only diverges when nothing breaks out of it. This one does, so
// control reaches the end of the function.
fn breaks_out(v: int) int {
    while true {
        if v > 0 : break
        return 1
    }
}

fn main() {
    println("%{ breaks_out(1) }")
}
