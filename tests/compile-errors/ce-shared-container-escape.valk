class SharedContainerItem { v: int (0) }

fn main() {
    let items = Array[SharedContainerItem]{}
    let value = SharedContainerItem {}
    items.append(value)
    let published: shared SharedContainerItem = value
    published
}
