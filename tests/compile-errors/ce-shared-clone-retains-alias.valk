class Entry {
    count: uint (0)
    fn clone() SELF $clone { return this }
}
class State { entry: Entry }

fn main() {
    let entry = Entry {}
    let source = State { entry: entry }
    let published: shared State = $clone(source)
    entry.count++
    print(published.entry.count)
}
