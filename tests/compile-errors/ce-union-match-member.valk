use valk:core

union Value : String | int {}

fn describe(value: Value) String {
    return match value : String {
        bool as enabled => enabled ? "yes" : "no"
        default => "other"
    }
}

fn main() {}
