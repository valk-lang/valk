enum First: ?int { value (7) }
enum Second: ?int { value (19) }
class Holder { value: First }
fn main() {
    let values = Array[First]{ First.value }
    let alias: Array[Second] = values
    alias[0] = Second.value
}
