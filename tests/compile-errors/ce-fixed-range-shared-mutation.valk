class Child { number: int }
class Item { child: Child (.{ number: 7 }) }
class Holder { values: [Item x 2] ({ Item {}, Item {} }) }
fn main() {
    let holder: shared Holder = .{}
    let copy = holder.values[0..2]
    copy[0].child = Child { number: 73 }
}
