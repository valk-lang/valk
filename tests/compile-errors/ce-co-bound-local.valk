struct Acc {
    total: int (0)
    fn add(n: int) int { this.total += n; return this.total }
}
fn make() co(int) {
    let acc = Acc{ total: 5 }
    let add = acc.add
    return co add(1)
}
fn main() {
    println(await make())
}
