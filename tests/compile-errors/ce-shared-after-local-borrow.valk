struct LocalBorrowPayload {
    value: int
}

class LocalBorrowOwner {
    payload: LocalBorrowPayload
}

fn main() {
    let owner = LocalBorrowOwner { payload: LocalBorrowPayload { value: 1 } }
    let view = &owner.payload
    let published: shared LocalBorrowOwner = owner
    view.value = 2
}
