class State { count: uint (0) }
fn identity(s: locked State) locked State { return s }
fn steal(state: shared Lock[State]) locked State {
    lock state as s { return identity(s) }
}
fn main() {
    let state: shared Lock[State] = .new(State {}) !!
    steal(state)
}
