interface Speaker {
    fn speak() int;
}
class Talker is Speaker {
    fn speak() int { return 4 }
}
class Holder {
    speaker: Speaker
}
fn main() {
    let holder = Holder { speaker: Talker {} }
    let borrow: &Speaker = holder.speaker
    println(borrow is_a Talker)
}
