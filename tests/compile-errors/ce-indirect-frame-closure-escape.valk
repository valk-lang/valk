struct Counter {
    number: int
    fn increment() { this.number++ }
}
global stored: ?fn()() (null)
fn save(callback: fn()()) { stored = callback }
fn main() {
    let counter = Counter { number: 0 }
    let save_callback: fn(fn()())() = save
    save_callback(counter.increment)
}
