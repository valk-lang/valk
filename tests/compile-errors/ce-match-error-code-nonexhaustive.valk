error MatchBase (base_a, base_b)
error MatchCodes (own_a) extends (MatchBase)

fn my_func() !MatchCodes { throw .own_a }

fn main() {
    my_func() ! {
        match E.code {
            E.own_a => println("own")
            E.base_a => println("base")
        }
    }
}
