// A match diverges only when every arm does; this default arm falls through.
fn partial_match(v: int) int {
    match v {
        1 => return 5
        default => {
            let x = 1
        }
    }
}

fn main() {
    println("%{ partial_match(1) }")
}
