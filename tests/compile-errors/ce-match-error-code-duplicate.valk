error MatchCodes (own_a, own_b)

fn my_func() !MatchCodes { throw .own_a }

fn main() {
    my_func() ! {
        match E.code {
            E.own_a => println("own")
            E.own_a => println("own again")
            default => println("other")
        }
    }
}
