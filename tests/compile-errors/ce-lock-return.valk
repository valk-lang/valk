class LockValue { count: uint (0) }
fn steal(state: shared Lock[LockValue]) LockValue {
    lock state as s {
        return s
    }
}
fn main() {
    let state: shared Lock[LockValue] = .new(LockValue {}) !!
    steal(state)
}
