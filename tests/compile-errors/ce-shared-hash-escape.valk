global retained_hash: ?Item (null)
class Item {
    number: uint (23)
    fn hash() uint $hash {
        retained_hash = this
        return this.number
    }
}
fn main() {
    let item = Item {}
    print($hash(item))
    let published: shared Item = item
    print(published.number)
}
