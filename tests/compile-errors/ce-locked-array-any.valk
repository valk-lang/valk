class Item { number: int }
class State { items: Array[Item] (.{}) }
fn main() {
    let state: shared Lock[State] = .new(State {}) !!
    lock state as s {
        let found = s.items.any(fn(v: Item) bool { return v.number > 1 })
        println(found)
    }
}
