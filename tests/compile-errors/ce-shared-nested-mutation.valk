class SharedNestedItems { items: Array[int] (.{}) }

class SharedNestedOwner {
    inner: SharedNestedItems (.{})
    fn add(value: int) { this.inner.items.append(value) }
}

fn main() {
    let owner: shared SharedNestedOwner = .{}
    owner.add(1)
}
