struct ExternRefValue {
    value: int
}

extern fn inspect_ref(value: *ExternRefValue);

extend ExternRefValue {
    fn inspect() {
        @unsafe
        inspect_ref(@ref(this))
    }
}

fn main() {
    let value = ExternRefValue { value: 1 }
    value.inspect()
}
