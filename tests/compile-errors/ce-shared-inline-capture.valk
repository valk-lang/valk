use valk:thread

class SharedInlineValue {}

struct SharedInlineCapture {
    value: SharedInlineValue
}

fn main() {
    let data: <SharedInlineCapture> = { value: .{} }
    thread:start(fn() {
        println(data.value)
    }) !!
}
