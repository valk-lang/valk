use valk.gc

value collect (gc.collect)

fn collect_indirectly() {
    collect()
}

class CollectingFinalizer {
    fn gc_free() {
        collect_indirectly()
    }
}

fn main() {
    CollectingFinalizer {}
}
