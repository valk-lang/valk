class Item {
    number: uint (23)
    fn clone() SELF $clone { return this }
}
class Owner { item: Item (.{}) }
fn main() {
    let owner = Owner {}
    let published: shared Item = $clone(&owner.item)
    owner.item.number++
    print(published.number)
}
