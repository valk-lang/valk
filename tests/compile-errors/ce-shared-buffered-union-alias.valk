class Entry { count: uint (0) }
union Item : Entry | uint {}
class State { item: ?Item }
fn make(entry: Entry) State { return State { item: entry } }
fn main() {
    let entry = Entry {}
    let alias = entry
    let published: shared State = make(entry)
    alias.count++
    print(isset(published.item))
}
