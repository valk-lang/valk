class LockChild { count: uint (0) }
class LockValue { child: LockChild (.{}) }
fn touch(c: locked LockChild) { c.count++ }
fn main() {
    let state: shared Lock[LockValue] = .new(LockValue {}) !!
    lock state as s {
        let t = co touch(s.child)
        await t
    }
}
