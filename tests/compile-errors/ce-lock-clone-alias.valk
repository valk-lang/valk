class Entry {
    count: uint (0)
    fn clone() SELF $clone { return this }
}
fn copy(state: shared Lock[Entry]) Entry {
    lock state as s { return $clone(s) }
}
fn main() {
    let state: shared Lock[Entry] = .new(Entry {}) !!
    copy(state)
}
