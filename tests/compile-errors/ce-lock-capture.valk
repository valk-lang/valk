use valk.core
class LockValue { count: uint (0) }
fn main() {
    let state: shared Lock[LockValue] = .new(LockValue {}) !!
    lock state as s {
        let f = fn() { s.count++ }
        f()
    }
}
