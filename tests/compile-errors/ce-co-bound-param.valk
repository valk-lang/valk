struct Acc {
    total: int (0)
    fn add(n: int) int { this.total += n; return this.total }
}
fn run(f: fn(int)(int)) co(int) { return co f(1) }
fn make() co(int) {
    let acc = Acc{ total: 5 }
    return run(acc.add)
}
fn main() {
    println(await make())
}
