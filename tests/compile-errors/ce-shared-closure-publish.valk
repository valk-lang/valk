class SharedJob { run: fn()() }

fn make() SharedJob {
    let counter = 0
    let ref: *int = @ref(counter)
    return SharedJob { run: fn() { ref[0]++ } }
}

fn main() {
    let job: shared SharedJob = make()
    job.run()
}
