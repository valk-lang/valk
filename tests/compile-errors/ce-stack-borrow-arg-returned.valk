// The callee hands the borrow back; the frame may use it but not return it
fn same(data: &[u8]) &[u8] {
    return data
}

fn view() &[u8] {
    let bytes: [u8 x 4] = { 1, 2, 3, 4 }
    let kept = same(bytes)
    return kept
}

fn main() { view() }
