// The callee stores the borrow, so frame storage may not be lent to it
fn keep(reader: ByteReader, data: &[u8]) {
    reader.source = data
}

fn main() {
    let bytes: [u8 x 4] = { 1, 2, 3, 4 }
    let reader = ByteReader.new("x")
    keep(reader, bytes)
}
