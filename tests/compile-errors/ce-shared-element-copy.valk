class SharedCopyItem { name: String ("a") }

fn mutate(item: SharedCopyItem) { item.name = "b" }

fn main() {
    let items: shared Array[SharedCopyItem] = Array[SharedCopyItem]{ SharedCopyItem {} }
    mutate(items.copy().get(0) !!)
}
