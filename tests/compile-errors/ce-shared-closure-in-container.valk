class SharedJobList { jobs: Array[fn()()] }

fn make() SharedJobList {
    @unsafe
    let counter = 0
    let ref: *int = @ref(counter)
    return SharedJobList { jobs: Array[fn()()]{ fn() { ref[0]++ } } }
}

fn main() {
    let list: shared SharedJobList = make()
    list
}
