// Array.each_slice is declared `~`, so it reads fine from another package but
// cannot be stored to.
fn main() {
    let a = Array[uint]{}
    println("%{isset(a.each_slice)}")
    a.each_slice = null
}
