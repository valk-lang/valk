struct ReceiverBorrowEscape {
    value: int

    fn borrow() local ReceiverBorrowEscape {
        return this
    }
}

fn main() {
    let value = ReceiverBorrowEscape { value: 1 }
    value.borrow()
}
