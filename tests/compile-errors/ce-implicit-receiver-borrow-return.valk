struct ReceiverBorrowEscape {
    value: int

    fn borrow() local mut &ReceiverBorrowEscape {
        return this
    }
}

fn main() {
    let value = ReceiverBorrowEscape { value: 1 }
    value.borrow()
}
