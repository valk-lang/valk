struct Counter {
    number: int (73)
    fn add(value: int) int { return this.number + value }
}
fn main() {
    let counter = Counter {}
    let pointer: fnptr(int)(int) = counter.add
    print(pointer(19))
}
