
// The non-null check before the loop only covers the first iteration: the body
// assigns null, so the use at the top of the next iteration is not guaranteed.

fn cond() bool {
    return true
}

fn main() {
    let x : ?String = "a"
    while cond() {
        let t : String = x
        println(t)
        x = null
    }
}
