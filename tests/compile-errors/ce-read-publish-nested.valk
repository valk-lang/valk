struct Part { numbers: [int x 2] ({ 73, 79 }) }
class Item { part: Part ({}) }
shared retained: ?Item (null)
fn publish(item: shared Item) int { retained = item; return 0 }
fn main() {
    let source = Item {}
    let number = source.part.numbers[publish(source)]
    println(number)
}
