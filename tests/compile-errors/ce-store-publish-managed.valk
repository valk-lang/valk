class Child { number: int }
class Item { child: ?Child (null) }
shared retained: ?Item (null)
fn publish(item: shared Item) Child { retained = item; return Child { number: 79 } }
fn main() {
    let source = Item {}
    source.child = publish(source)
}
