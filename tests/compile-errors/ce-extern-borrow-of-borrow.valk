struct ExternBorrowValue {
    value: int
}

extern fn inspect_borrow(value: *ExternBorrowValue);

extend ExternBorrowValue {
    fn inspect() {
        inspect_borrow(&this)
    }
}

fn main() {
    let value = ExternBorrowValue { value: 1 }
    value.inspect()
}
