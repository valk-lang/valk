global allocation_sink: uint

class AllocatingFinalizer {
    value: uint

    fn gc_free() {
        let allocated = AllocatingFinalizer { value: this.value }
        allocation_sink = allocated.value
    }
}

fn main() {
    let value = AllocatingFinalizer { value: 7 }
    allocation_sink = value.value
}
