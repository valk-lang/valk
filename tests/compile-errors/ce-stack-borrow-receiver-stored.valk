// Through a local parameter as well: the reader holds the caller's storage
global saved: ?ByteReader (null)

fn keep(data: local &[u8]) {
    saved = data.reader()
}

fn main() {
    let bytes: [u8 x 3] = { 7, 8, 9 }
    keep(bytes)
}
