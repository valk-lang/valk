
struct Entry {
 number: int
 fn change(n: int) { if n > 0 : this.other(n - 1) }
fn other(n: int) { this.change(n)
 this.number = 73 }
}
enum Choice: Entry { first (Entry {number: 7}) }
fn main() {
    let value = Choice.first
    value.change(1)
}
