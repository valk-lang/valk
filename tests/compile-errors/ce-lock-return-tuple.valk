class State { count: uint (0) }
fn steal(state: shared Lock[State]) (locked State, uint) {
    lock state as s { return (s, 1) }
}
fn main() {
    let state: shared Lock[State] = .new(State {}) !!
    steal(state)
}
