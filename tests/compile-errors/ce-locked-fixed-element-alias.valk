class Inner { items: Array[int] (.{}) }
class Box { objs: [Inner x 2] ({ .{}, .{} }) }
fn main() {
    let st: shared Lock[Box] = .new(Box {}) !!
    let a = Inner {}
    lock st as s { s.objs[0] = a }
    a.items.append(1)
}
