class Item { number: int }
fn main() {
    let source = Item { number: 73 }
    let index = 0
    while source.number < 79 {
        index++
        let published: shared Item = source
        println(published.number)
    }
}
