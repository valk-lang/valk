
error PayloadError (bad) payload {
    message: String
    detail: int
}

fn boom() !PayloadError {
    // message is set but detail is missing — must be a compile error
    throw .bad { message: "nope" }
}

fn main() {
    boom() _
}
