class Item { number: uint (23) }
fn main() {
    let items = Array[Item]{}
    let item = Item {}
    items[0] = item
    let published: shared Item = item
    items[0].number++
    print(published.number)
}
