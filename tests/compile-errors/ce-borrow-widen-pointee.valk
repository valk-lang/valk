class Item { count: int (7) }
class Holder { value: Item (.{}) }
fn clear(value: &?Item) { value[0] = null }
fn main() {
    let holder = Holder {}
    clear(&holder.value)
    print(holder.value.count)
}
