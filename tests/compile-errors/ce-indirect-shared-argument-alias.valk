class Box { number: int }
global saved: ?shared Box (null)
fn save(value: shared Box) { saved = value }
fn main() {
    let value = Box { number: 1 }
    let alias = value
    let callback: fn(shared Box)() = save
    callback(value)
    alias.number++
}
