slice Bytes of u8 {
    static fn of(source: &mut [u8]) SELF {
        @unsafe
        return SELF { owner: source.owner, data: source.data, length: source.length }
    }
}
slice OtherBytes of u8 {}
fn main() {
    let bytes = Bytes.of([u8]{ 1 x 2 })
    let other: OtherBytes = bytes
}
