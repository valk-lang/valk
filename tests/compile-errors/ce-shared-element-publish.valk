class SharedElementPublish { v: int (0) }

fn main() {
    let items = Array[SharedElementPublish]{ SharedElementPublish {} }
    let element = items.get(0) !!
    let published: shared SharedElementPublish = element
    published
}
