enum Alias {
    zero (0)
    none (0)
    one (1)
}

fn main() {
    let value = Alias.one
    match value {
        Alias.zero => println("zero")
        Alias.none => println("none")
        Alias.one => println("one")
    }
}
