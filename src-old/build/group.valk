// `group name { ... }` inside a class — methods live on the group for dotted calls.
class Group {
    class: Class
    name: String
    act: ACT
    exp: Exp
    // Methods declared inside this group (name → Func)
    funcs: Map[Func] (.{})
}
