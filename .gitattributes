# GitHub has no Valk grammar; Rust's is the closest. Not detectable, so the
# repository's language statistics do not count Valk sources as Rust
*.valk linguist-language=Rust linguist-detectable=false
